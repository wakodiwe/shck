{"SC1000":{"code":"SC1000","title":"`$` is not used specially and should therefore be escaped.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1000","rationale":"`$` is special in double quotes, but there are some cases where it's interpreted literally: 1. Following a backslash: `echo \"\\$\"` 2. In a context where the shell can't make sense of it, such as at the","problematicCode":"echo \"$\"","correctCode":"echo \"\\$\""},"SC1001":{"code":"SC1001","title":"This `\\o` will be a regular 'o' in this context.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1001","rationale":"You have escaped something that has no special meaning when escaped. The backslash will be simply be ignored. If the backslash was supposed to be literal, single quote or escape it. If you wanted it t","problematicCode":"# Want literal backslash\necho Yay \\o/\n# Want other characters\nbell=\\a","correctCode":"echo 'Yay \\o/'\nbell=\"$(printf '\\a')\""},"SC1003":{"code":"SC1003","title":"Want to escape a single quote? `echo 'This is how it'\\''s done'`.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1003","rationale":"In POSIX shell, the shell cares about nothing but another single-quote to terminate the quoted segment. Not even backslashes are interpreted. [POSIX.1 Shell Command Language § 2.2.2 Single Quotes](htt","problematicCode":"echo 'This is not how it\\'s done'.","correctCode":"echo 'This is how it'\\''s done'."},"SC1004":{"code":"SC1004","title":"This backslash+linefeed is literal. Break outside single quotes if you just want to break the line.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1004","rationale":"You have a single-quoted string containing a backslash followed by a linefeed (newline). Unlike double-quotes or unquoted strings, this has no special meaning. The string will contain a literal backsl","problematicCode":"var='This is long \\\npiece of text'","correctCode":"var='This is a long '\\\n'piece of text'"},"SC1007":{"code":"SC1007","title":"Remove space after `=` if trying to assign a value (or for empty string, use `var=\"\"` ... ).","fix":"Fix syntax error | Disable: # shellcheck disable=SC1007","rationale":"It's easy to think that `LANGUAGE= nl` would assign `\"nl\"` to the variable `LANGUAGE`. It doesn't. Instead, it runs `nl` (the \"number lines\" command) and sets `LANGUAGE` to an empty string in its envi","problematicCode":"# I want programs to show text in Dutch!\nLANGUAGE= nl","correctCode":"# I want programs to show text in Dutch!\nLANGUAGE=nl"},"SC1008":{"code":"SC1008","title":"This shebang was unrecognized. ShellCheck only supports sh/bash/dash/ksh. Add a 'shell' directive to specify.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1008","rationale":"You have specified a shebang that ShellCheck doesn't recognize. This can be due to invoking the script via a wrapper, specifying a dummy like `#!/bin/false` to prevent execution, or trying to check a ","problematicCode":"#!/bin/mywrapper\necho \"Hello World\"","correctCode":"#!/bin/mywrapper\n# shellcheck shell=bash\necho \"Hello World\""},"SC1009":{"code":"SC1009","title":"The mentioned parser error was in ...","fix":"Fix syntax error | Disable: # shellcheck disable=SC1009","rationale":"","problematicCode":"","correctCode":""},"SC1010":{"code":"SC1010","title":"Use semicolon or linefeed before `done` (or quote to make it literal).","fix":"Fix syntax error | Disable: # shellcheck disable=SC1010","rationale":"ShellCheck found a keyword like `done`, `then`, `fi`, `esac`, etc used as the argument of a command. This means that the shell will interpret it as a literal string rather than a shell keyword. To be ","problematicCode":"for f in *; do echo \"$f\" done","correctCode":"for f in *; do echo \"$f\"; done"},"SC1011":{"code":"SC1011","title":"This apostrophe terminated the single quoted string!","fix":"Fix syntax error | Disable: # shellcheck disable=SC1011","rationale":"When writing a string in single-quotes, you have to make sure that any apostrophes in the text don't accidentally terminate the single-quoted string prematurely. Escape them properly (see the correct ","problematicCode":"echo 'Nothing so needs reforming as other peoples' habits.'","correctCode":"echo 'Nothing so needs reforming as other peoples'\\'' habits.'"},"SC1012":{"code":"SC1012","title":"`\\t` is just literal `t` here. For tab, use `\"$(printf '\\t')\"` instead.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1012","rationale":"ShellCheck has found a `\\t`, `\\n` or `\\r` in a context where they just become regular letters `t`, `n` or `r`. Most likely, it was intended as a tab, newline or carriage return. To generate such chara","problematicCode":"# Want tab\n$ var=foo\\tbar\n$ printf '<%s>\\n' \"$var\"\n<footbar>\n$ var=foo\\\\tbar\n$ printf '<%s>\\n' \"$var\"\n<foo\\tbar>","correctCode":"$ var=\"foo$(printf '\\t')bar\"  # As suggested in warning\n$ printf '<%s>\\n' \"$var\"\n<foo\tbar>\n$ var=\"$(printf 'foo\\tbar')\"  # Equivalent alternative\n$ printf '<%s>\\n' \"$var\"\n<foo\tbar>"},"SC1014":{"code":"SC1014","title":"Use `if cmd; then ..` to check exit code, or `if [ \"$(cmd)\" = .. ]` to check output.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1014","rationale":"`[ ... ]` as shell syntax is a simple command that tests for whether certain conditions are true or false, such as whether the value assigned to a variable has a non-zero length (`[ -n \"${foo}\" ]`) or","problematicCode":"# WRONG\nif [ grep -q pattern file ]\nthen\n  echo \"Found a match\"\nfi","correctCode":"if grep -q pattern file\nthen\n  echo \"Found a match\"\nfi"},"SC1015":{"code":"SC1015","title":"This is a Unicode double quote. Delete and retype it.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1015","rationale":"Blog software and word processors frequently replaces ASCII quotes `\"\"` with fancy Unicode quotes, `“”`. To Bash, Unicode quotes are considered regular literals and not quotes at all. Simply delete th","problematicCode":"echo “hello world”","correctCode":"echo \"hello world\""},"SC1016":{"code":"SC1016","title":"This is a Unicode single quote. Delete and retype it.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1016","rationale":"Some software, like macOS, Microsoft Word, and WordPress, may automatically replace your regular quotes with slanted Unicode quotes. Try deleting and retyping them, and/or disable “smart quotes” in yo","problematicCode":"echo ‘hello world’","correctCode":"echo 'hello world'"},"SC1017":{"code":"SC1017","title":"Literal carriage return. Run script through `tr -d '\\r'` .","fix":"Fix syntax error | Disable: # shellcheck disable=SC1017","rationale":"The script uses Windows/MS-DOS style `\\r\\n` line terminators instead of Unix-style `\\n` terminators. The additional `\\r` aka `^M` aka carriage return characters will be treated literally, and results ","problematicCode":"$ cat -v myscript\n#!/bin/sh^M\necho \"Hello World\"^M","correctCode":"$ cat -v myscript\n#!/bin/sh\necho \"Hello World\""},"SC1018":{"code":"SC1018","title":"This is a Unicode non-breaking space. Delete it and retype as space.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1018","rationale":"","problematicCode":"","correctCode":""},"SC1019":{"code":"SC1019","title":"Expected this to be an argument to the unary condition.","fix":"Fix syntax error | Disable: # shellcheck disable=SC1019","rationale":"ShellCheck has found a unary test operator that does not appear to be followed by a valid shell word. This could be because of a misplaced `]`, `)`, or a missing space before the `]`. Check the syntax","problematicCode":"[ -x ]","correctCode":"[ -x \"myfile\" ]"},"SC1020":{"code":"SC1020","title":"You need a space before the `]` or `]]`","fix":"Bourne shells are very whitespace sensitive. Adding or removing spaces can drastically alter the mea","rationale":"Bourne shells are very whitespace sensitive. Adding or removing spaces can drastically alter the meaning of a script. In these cases, ShellCheck has noticed that you're missing a space at the position","problematicCode":"if [ \"$STUFF\" = \"\"]; then","correctCode":"if [ \"$STUFF\" = \"\" ]; then"},"SC1026":{"code":"SC1026","title":"If grouping expressions inside `[[..]]`, use `( .. )`.","fix":"`[ .. ]` should not be used to group subexpressions inside `[[ .. ]]` or `[ .. ]` statements. For `[","rationale":"`[ .. ]` should not be used to group subexpressions inside `[[ .. ]]` or `[ .. ]` statements. For `[[ .. ]]`, use regular parentheses. For `[ .. ]`, either use escaped parentheses, or preferably rewri","problematicCode":"[[ [ a || b ] && c ]]\n[ [ a -o b ] -a c ]]","correctCode":"[[ ( a || b ) && c ]]\n[ \\( a -o b \\) -a c ]]  # or  { [ a ] || [ b ]; } && [ c ]"},"SC1027":{"code":"SC1027","title":"Expected another argument for this operator.","fix":"ShellCheck found a `test` operator without an operand. This could be a copy-paste fail, bad linebrea","rationale":"ShellCheck found a `test` operator without an operand. This could be a copy-paste fail, bad linebreak, or trying to use `<>` instead of `!=` or `-ne`.","problematicCode":"[ $a -ne ]","correctCode":"[ $a -ne $b ]"},"SC1028":{"code":"SC1028","title":"In `[..]` you have to escape `\\( \\)` or preferably combine `[..]` expressions.","fix":"`[` is implemented as a regular command, so `(` is not special. The preferred way is not to group in","rationale":"`[` is implemented as a regular command, so `(` is not special. The preferred way is not to group inside `[ .. ]` and instead compose multiple `[ .. ]` statments using the shell's `&&`, `||` and `{ ..","problematicCode":"[ -e ~/.bashrc -a ( -x /bin/dash -o -x /bin/ash ) ]","correctCode":"[ -e ~/.bashrc ] &&  { [ -x /bin/dash ] || [ -x /bin/ash ]; }"},"SC1029":{"code":"SC1029","title":"In `[[..]]` you shouldn't escape `(` or `)`.","fix":"You don't have to -- and can't -- escape `(` or `)` inside a `[[ .. ]]` expression like you do in `[","rationale":"You don't have to -- and can't -- escape `(` or `)` inside a `[[ .. ]]` expression like you do in `[ .. ]`. Just remove the escaping.","problematicCode":"[[ -e ~/.bashrc && \\( -x /bin/dash || -x /bin/ash \\) ]]","correctCode":"[[ -e ~/.bashrc && ( -x /bin/dash || -x /bin/ash ) ]]"},"SC1033":{"code":"SC1033","title":"Test expression was opened with double `[[` but closed with single `]`. Make sure they match.","fix":"ShellCheck found a test expression `[ ... ]` (POSIX) or `[[ ... ]]` (ksh/bash), but where the openin","rationale":"ShellCheck found a test expression `[ ... ]` (POSIX) or `[[ ... ]]` (ksh/bash), but where the opening and closing brackets did not match (i.e. `[[ .. ]` or `[ .. ]]`). The brackets need to match up to","problematicCode":"[[ -z \"$var\" ]","correctCode":"[[ -z \"$var\" ]]"},"SC1034":{"code":"SC1034","title":"Test expression was opened with single `[` but closed with double `]]`. Make sure they match.","fix":"Test expression was opened with single `[` but closed with double `]]`. Make sure they match.","rationale":"","problematicCode":"","correctCode":""},"SC1035":{"code":"SC1035","title":"You need a space here","fix":"Bourne shells are very whitespace sensitive. Adding or removing spaces can drastically alter the mea","rationale":"Bourne shells are very whitespace sensitive. Adding or removing spaces can drastically alter the meaning of a script. In these cases, ShellCheck has noticed that you're missing a space at the position","problematicCode":"if ![-z foo ]; then true; fi # if command `[-z' w/ args `foo', `]' fails..","correctCode":"if ! [ -z foo ]; then true; fi # if command `[' w/ args `-z', `foo', `]' fails.."},"SC1036":{"code":"SC1036","title":"`(` is invalid here. Did you forget to escape it?","fix":"ShellCheck expected an ordinary shell word but found an opening parenthesis instead. Determine what ","rationale":"ShellCheck expected an ordinary shell word but found an opening parenthesis instead. Determine what you intended the parenthesis to do and rewrite accordingly. Common issues include: * Wanting them to","problematicCode":"echo (foo) bar","correctCode":"echo \"(foo) bar\"  # Literal parentheses\necho \"$(foo) bar\" # Command expansion\necho \"foo bar\"    # Tried to use parentheses for grouping or function invocation"},"SC1037":{"code":"SC1037","title":"Braces are required for positionals over 9, e.g. `${10}`.","fix":"For legacy reasons, `$10` is interpreted as the variable `$1` followed by the literal string `0`. Cu","rationale":"For legacy reasons, `$10` is interpreted as the variable `$1` followed by the literal string `0`. Curly braces are needed to tell the shell that both digits are part of the parameter expansion.","problematicCode":"echo \"Ninth parameter: $9\"\necho \"Tenth parameter: $10\"","correctCode":"echo \"Ninth parameter: $9\"\necho \"Tenth parameter: ${10}\""},"SC1038":{"code":"SC1038","title":"Shells are space sensitive. Use `< <(cmd)`, not `<<(cmd)`.","fix":"You are using `<<(` which is an invalid construct. You probably meant to redirect `<` from process s","rationale":"You are using `<<(` which is an invalid construct. You probably meant to redirect `<` from process substitution `<(..)` instead. To do this, a space is needed between the `<` and `<(..)`, i.e. `< <(cm","problematicCode":"while IFS= read -r line\ndo\n  printf \"%q\\n\" \"$line\"\ndone <<(curl -s http://example.com)","correctCode":"while IFS= read -r line\ndo\n  printf \"%q\\n\" \"$line\"\ndone <  <(curl -s http://example.com)"},"SC1039":{"code":"SC1039","title":"Remove indentation before end token (or use `<<-` and indent with tabs).","fix":"The here document delimiter will not be recognized if it is indented. You can fix it in one of two w","rationale":"The here document delimiter will not be recognized if it is indented. You can fix it in one of two ways: 1. Simply remove the indentation, even though this may break formatting. 2. Use `<<-` instead o","problematicCode":"for f in *.png\ndo\n  cat << HTML\n     <img src=\"$f\" /><br/>\n  HTML\ndone > index.html","correctCode":"for f in *.png\ndo\n  cat << HTML\n     <img src=\"$f\" /><br/>\nHTML\ndone > index.html"},"SC1040":{"code":"SC1040","title":"When using `<<-`, you can only indent with tabs.","fix":"`<<-`, by design, only strips tabs. Not spaces. Your editor may be automatically replacing tabs with","rationale":"`<<-`, by design, only strips tabs. Not spaces. Your editor may be automatically replacing tabs with spaces, either when you type them or when you save the file or both. If you're unable to make it st","problematicCode":"cat <<- foo\n        Hello world\n    foo","correctCode":"^Icat <<- foo\n^I^IHello world\n^Ifoo"},"SC1041":{"code":"SC1041","title":"Found `eof` further down, but not on a separate line.","fix":"Your here document isn't properly terminated. There is a line containing the terminator you've chose","rationale":"Your here document isn't properly terminated. There is a line containing the terminator you've chosen, but it's not by itself on a separate line. In the example code, the script uses `<<-eof`, which i","problematicCode":"cat <<-eof\nHello World\n-eof","correctCode":"cat <<- eof\nHello World\neof"},"SC1043":{"code":"SC1043","title":"Found EOF further down, but with wrong casing.","fix":"ShellCheck found a here document (`<<`) where the end token is missing. However, the end token appea","rationale":"ShellCheck found a here document (`<<`) where the end token is missing. However, the end token appears with different case further down. If this was meant to be the end of the here document, make sure","problematicCode":"cat << EOF\nHello World\nEof","correctCode":"cat << EOF\nHello World\nEOF"},"SC1044":{"code":"SC1044","title":"Couldn't find end token `EOF` in the here document.","fix":"The `<<` here document (aka heredoc) was not properly terminated. The terminating token needs to be ","rationale":"The `<<` here document (aka heredoc) was not properly terminated. The terminating token needs to be on a separate line without indenting (or indented with tabs only when using `<<-`). Note that you ca","problematicCode":"cat << EOF\n  Hello World","correctCode":"cat << EOF\n  Hello World\nEOF"},"SC1045":{"code":"SC1045","title":"It's not `foo &; bar`, just `foo & bar`.","fix":"Both `&` and `;` terminate the command. You should only use one of them.","rationale":"Both `&` and `;` terminate the command. You should only use one of them.","problematicCode":"foo &; bar","correctCode":"foo & bar"},"SC1046":{"code":"SC1046","title":"Couldn't find `fi` for this `if`.","fix":"ShellCheck has found an `if` statement that does not appear to have a matching terminating `fi`. Thi","rationale":"ShellCheck has found an `if` statement that does not appear to have a matching terminating `fi`. This could be because it's missing entirely, or because the `if` statement was incorrectly terminated b","problematicCode":"if true\nthen\n  echo \"True\"\ndone","correctCode":"if true\nthen\n  echo \"True\"\nfi"},"SC1047":{"code":"SC1047","title":"Expected `fi` matching previously mentioned `if`.","fix":"Expected `fi` matching previously mentioned `if`.","rationale":"","problematicCode":"","correctCode":""},"SC1048":{"code":"SC1048","title":"Can't have empty then clauses (use `true` as a no-op).","fix":"Shells do not allow empty `then` clauses. They need at least one command (and comments are not comma","rationale":"Shells do not allow empty `then` clauses. They need at least one command (and comments are not commands). If you want a `then` clause that does nothing, use a dummy command like `true`.","problematicCode":"if [ -e foo ]\nthen\n  # TODO: handle this\nfi","correctCode":"if [ -e foo ]\nthen\n  # TODO: handle this\n  true\nfi\n# Or use the no-op colon operator \":\"\nif [ -e foo ]\nthen\n  # TODO: handle this\n  :\nfi"},"SC1049":{"code":"SC1049","title":"Did you forget the `then` for this `if`?","fix":"ShellCheck found a parsing error in the script, and determined that it's most likely due to a missin","rationale":"ShellCheck found a parsing error in the script, and determined that it's most likely due to a missing `then` keyword for the `if` or `elif` indicated. Make sure the `then` is there. Note that the `the","problematicCode":"if true\n  echo \"foo\"\nelif true\n  echo \"bar\"\nfi","correctCode":"if true\nthen\n  echo \"foo\"\nelif true\nthen\n  echo \"bar\"\nfi"},"SC1050":{"code":"SC1050","title":"Expected `then`.","fix":"ShellCheck has found an `if` statement that appears to be missing a `then`. Make sure the `then` exi","rationale":"ShellCheck has found an `if` statement that appears to be missing a `then`. Make sure the `then` exists, and that it is the first word of the line (or immediately preceded by a semicolon).","problematicCode":"if true\n  echo \"True\"\nfi","correctCode":"if true\nthen\n  echo \"True\"\nfi"},"SC1051":{"code":"SC1051","title":"Semicolons directly after `then` are not allowed. Just remove it.","fix":"`then` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow th","rationale":"`then` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow them directly with a line break or another command.","problematicCode":"if true; then; echo \"Hi\"; fi","correctCode":"if true; then echo \"Hi\"; fi"},"SC1052":{"code":"SC1052","title":"Semicolons directly after `then` are not allowed. Just remove it.","fix":"`then` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow th","rationale":"`then` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow them directly with a line break or another command.","problematicCode":"if true; then; echo \"Hi\"; fi","correctCode":"if true; then echo \"Hi\"; fi"},"SC1053":{"code":"SC1053","title":"Semicolons directly after `else` are not allowed. Just remove it.","fix":"`else` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow th","rationale":"`else` keywords should not be followed by semicolons. It's not valid shell syntax. You can follow them directly with a line break or another command.","problematicCode":"if mycommand; then echo \"True\"; else; echo \"False\"; fi","correctCode":"if mycommand; then echo \"True\"; else echo \"False\"; fi"},"SC1054":{"code":"SC1054","title":"You need a space after the `{`.","fix":"`{` is only recognized as the start of a command group when it's a separate token. If it's not a sep","rationale":"`{` is only recognized as the start of a command group when it's a separate token. If it's not a separate token, like in the problematic example, it will be considered a literal character, as if writi","problematicCode":"foo() {echo \"hello world\"; }","correctCode":"foo() { echo \"hello world\"; }"},"SC1055":{"code":"SC1055","title":"You need at least one command here. Use `true;` as a no-op.","fix":"ShellCheck found an empty code block. This could be an empty function as shown, a loop with an empty","rationale":"ShellCheck found an empty code block. This could be an empty function as shown, a loop with an empty body, or similar. Sh/bash does not allow empty code blocks. Insert at least one command. If you don","problematicCode":"submitbug() {\n  # TODO: Implement me\n}","correctCode":"submitbug() {\n  # TODO: Implement me\n  true\n}"},"SC1056":{"code":"SC1056","title":"Expected a `}`. If you have one, try a `;` or `\\n` in front of it.","fix":"`}` is only recognized as the end of a command group when it's a separate token. If it's not a separ","rationale":"`}` is only recognized as the end of a command group when it's a separate token. If it's not a separate token, like in the problematic example, it will be considered a literal character, as if writing","problematicCode":"#!/bin/bash\nbar() { echo \"hello world\" }","correctCode":"#!/bin/bash\nbar() { echo \"hello world\";}"},"SC1057":{"code":"SC1057","title":"Did you forget the `do` for this loop?","fix":"ShellCheck found a loop that appears to be missing its `do` statement. Make sure the loop syntax is ","rationale":"ShellCheck found a loop that appears to be missing its `do` statement. Make sure the loop syntax is correct.","problematicCode":"while read -r line\n  echo $line\ndone","correctCode":"while read -r line\ndo\n  echo $line\ndone"},"SC1058":{"code":"SC1058","title":"Expected `do`.","fix":"ShellCheck has found a loop that appears to be missing a `do` statement. In the problematic code, it","rationale":"ShellCheck has found a loop that appears to be missing a `do` statement. In the problematic code, it was simply forgotten. Verify that the `do` exists, and that it's in the correct place.","problematicCode":"for file in *\n  echo \"$file\"\ndone","correctCode":"for file in *\ndo\n  echo \"$file\"\ndone"},"SC1059":{"code":"SC1059","title":"Semicolon is not allowed directly after `do`. You can just delete it.","fix":"Semicolon `;` is not allowed directly after a `do` keyword. Follow it directly with either a command","rationale":"Semicolon `;` is not allowed directly after a `do` keyword. Follow it directly with either a command or a linefeed as shown in the example.","problematicCode":"while true; do; true; done\nwhile true;\ndo;\n  true;\ndone;","correctCode":"while true; do true; done\nwhile true;\ndo\n  true;\ndone;"},"SC1060":{"code":"SC1060","title":"Can't have empty do clauses (use `true` as a no-op)","fix":"An empty `do ... done` block is not valid. Use `true` or `:` if you need no command at all.","rationale":"An empty `do ... done` block is not valid. Use `true` or `:` if you need no command at all.","problematicCode":"for i in 1 2 3; do\ndone","correctCode":"for i in 1 2 3; do\n  true\ndone"},"SC1061":{"code":"SC1061","title":"Couldn't find `done` for this `do`.","fix":"ShellCheck found a `do` without a corresponding `done`. Double check that the `done` exists, and tha","rationale":"ShellCheck found a `do` without a corresponding `done`. Double check that the `done` exists, and that it correctly matches the indicated `do`. A companion warning [[SC1062]] is emitted where ShellChec","problematicCode":"yes() {\n  while echo \"y\"\n  do\n    true\n}","correctCode":"yes() {\n  while echo \"y\"\n  do\n    true\n  done\n}"},"SC1062":{"code":"SC1062","title":"Expected `done` matching previously mentioned `do`.","fix":"Expected `done` matching previously mentioned `do`.","rationale":"","problematicCode":"","correctCode":""},"SC1063":{"code":"SC1063","title":"You need a line feed or semicolon before the `do`.","fix":"ShellCheck found a `do` on the same line as a loop, but `do` only starts a loop block at the start o","rationale":"ShellCheck found a `do` on the same line as a loop, but `do` only starts a loop block at the start of a line/statement. Make the `do` the start of a new line/statement by inserting a linefeed or semic","problematicCode":"for file in * do\n  echo \"$file\"\ndone","correctCode":"for file in *; do\n  echo \"$file\"\ndone\n# or\nfor file in *\ndo\n  echo \"$file\"\ndone"},"SC1064":{"code":"SC1064","title":"Expected a `{` to open the function definition.","fix":"ShellCheck found what appears to be the start of a function definition, but without a function body.","rationale":"ShellCheck found what appears to be the start of a function definition, but without a function body. One common cause is that you are trying to call a function by appending parentheses, e.g. `foo()` l","problematicCode":"foo() {\n  echo \"hello world\"\n}\nfoo()","correctCode":"foo() {\n  echo \"hello world\"\n}\nfoo"},"SC1065":{"code":"SC1065","title":"Trying to declare parameters? Don't. Use `()` and refer to params as `$1`, `$2`, …","fix":"Shell script functions behave just like scripts and other commands: - They always take a 0 to N para","rationale":"Shell script functions behave just like scripts and other commands: - They always take a 0 to N parameters, referred to with `$1`, `$2` etc. They can not declare parameters by name. - They are execute","problematicCode":"foo(input) {\n  echo \"$input\"\n}\nfoo(\"hello world\");","correctCode":"foo() {\n  echo \"$1\"\n}\nfoo \"hello world\""},"SC1066":{"code":"SC1066","title":"Don't use `$` on the left side of assignments.","fix":"Unlike Perl or PHP, `$` is not used when assigning to a variable.","rationale":"Unlike Perl or PHP, `$` is not used when assigning to a variable.","problematicCode":"$greeting=\"Hello World\"","correctCode":"greeting=\"Hello World\""},"SC1067":{"code":"SC1067","title":"For indirection, use arrays, `declare \"var$n=value\"`, or (for sh) `read`/`eval`","fix":"`var$n=value` is not a valid way of assigning to a dynamically created variable name in any shell. P","rationale":"`var$n=value` is not a valid way of assigning to a dynamically created variable name in any shell. Please use one of the other methods to assign to names via expanded strings. [Wooledge BashFaq #6](ht","problematicCode":"n=1\nvar$n=\"hello\"","correctCode":"n=1\nvar[n]=\"hello\"\necho \"${var[n]}\""},"SC1068":{"code":"SC1068","title":"Don't put spaces around the `=` in assignments.","fix":"Shells are space sensitive. `foo=42` means to assign `42` to the variable `foo`. `foo = 42` means to","rationale":"Shells are space sensitive. `foo=42` means to assign `42` to the variable `foo`. `foo = 42` means to run a command named `foo`, and pass `=` as `$1` and `42` as `$2`.","problematicCode":"foo = 42","correctCode":"foo=42"},"SC1069":{"code":"SC1069","title":"You need a space before the `[`.","fix":"ShellCheck found a keyword immediately followed by a `[`. There needs to be a space between them.","rationale":"ShellCheck found a keyword immediately followed by a `[`. There needs to be a space between them.","problematicCode":"if[ -e file ]\nthen echo \"exists\"; fi","correctCode":"if [ -e file ]\nthen echo \"exists\"; fi"},"SC1070":{"code":"SC1070","title":"Parsing stopped here. Mismatched keywords or invalid parentheses?","fix":"ShellCheck found a syntax error at the indicated location. Barring a bug in ShellCheck itself, your ","rationale":"ShellCheck found a syntax error at the indicated location. Barring a bug in ShellCheck itself, your shell will also crash with a syntax error at the same location, so you cannot ignore this check.","problematicCode":"","correctCode":""},"SC1071":{"code":"SC1071","title":"ShellCheck only supports sh/bash/dash/ksh scripts. Sorry!","fix":"You have specified the shebang of an unsupported language or shell dialect. ShellCheck only supports","rationale":"You have specified the shebang of an unsupported language or shell dialect. ShellCheck only supports a limited number of Bourne-based Unix shells: bash, ksh, dash and POSIX sh. It does not support scr","problematicCode":"#!/usr/bin/python\nprint \"Hello\"","correctCode":""},"SC1072":{"code":"SC1072","title":"Unexpected ..","fix":"# shellcheck disable | # shellcheck disable=all","rationale":"","problematicCode":"# shellcheck disable\necho stuff that shellcheck up to at least v0.10.0 will not even see because of the incorrect directive above","correctCode":"# shellcheck disable=all\necho stuff that shellcheck will correctly ignore entirely"},"SC1073":{"code":"SC1073","title":"Couldn't parse this (thing). Fix to allow more checks.","fix":"Couldn't parse this (thing). Fix to allow more checks.","rationale":"","problematicCode":"","correctCode":""},"SC1074":{"code":"SC1074","title":"Did you forget the `;;` after the previous case item?","fix":"Syntax `case` needs `;;` after the previous case item. If not, syntax error will cause.","rationale":"Syntax `case` needs `;;` after the previous case item. If not, syntax error will cause.","problematicCode":"while getoptions f option\ndo\n  case \"${options}\"\n  in\n  f) FTR=\"${ARG}\"\n    \\?) exit\n  esac\ndone","correctCode":"while getoptions f option\ndo\n  case \"${options}\"\n  in\n  f) FTR=\"${ARG}\";;\n    \\?) exit;;\n  esac\ndone"},"SC1075":{"code":"SC1075","title":"Use `elif` instead of `else if`.","fix":"Many languages allow alternate branches with `else if`, but `sh` is not one of them. Use `elif` inst","rationale":"Many languages allow alternate branches with `else if`, but `sh` is not one of them. Use `elif` instead.","problematicCode":"if [ \"$#\" -eq 0 ]\nthen\n  echo \"Usage: ...\"\nelse if [ \"$#\" -lt 2 ]\nthen\n  echo \"Missing operand\"\nfi","correctCode":"if [ \"$#\" -eq 0 ]\nthen\n  echo \"Usage: ...\"\nelif [ \"$#\" -lt 2 ]\nthen\n  echo \"Missing operand\"\nfi"},"SC1076":{"code":"SC1076","title":"Trying to do math? Use e.g. `[ $((i/2+7)) -ge 18 ]`.","fix":"ShellCheck found a loose `+*/%` in a test statement. This usually happens when trying to do arithmet","rationale":"ShellCheck found a loose `+*/%` in a test statement. This usually happens when trying to do arithmetic in a condition, but without using the arithmetic expansion construct `$((expression))`. In C, `if","problematicCode":"[ i / 2 + 7 -ge 18 ]","correctCode":"[ $((i / 2 + 7)) -ge 18 ]"},"SC1077":{"code":"SC1077","title":"For command expansion, the tick should slant left (`` ` `` vs `´`).","fix":"In some fonts it's hard to tell ticks apart, but Bash strongly distinguishes between backticks (grav","rationale":"In some fonts it's hard to tell ticks apart, but Bash strongly distinguishes between backticks (grave accent `` ` ``), forward ticks (acute accent `´`) and regular ticks (apostrophe `'`). Backticks st","problematicCode":"echo \"Your username is ´whoami´\"","correctCode":"echo \"Your username is $(whoami)\" # Preferred\necho \"Your username is `whoami`\"  # Deprecated, will give [SC2006]"},"SC1078":{"code":"SC1078","title":"Did you forget to close this double-quoted string?","fix":"The first line is missing a quote. ShellCheck warns when it detects multi-line double quoted, single","rationale":"The first line is missing a quote. ShellCheck warns when it detects multi-line double quoted, single quoted or backticked strings when the character that follows it looks out of place (and gives a com","problematicCode":"greeting=\"hello\ntarget=\"world\"","correctCode":"greeting=\"hello\"\ntarget=\"world\""},"SC1079":{"code":"SC1079","title":"This is actually an end quote, but due to next char it looks suspect.","fix":"This is actually an end quote, but due to next char it looks suspect.","rationale":"","problematicCode":"","correctCode":""},"SC1080":{"code":"SC1080","title":"You need `\\` before line feeds to break lines in `[ ]`.","fix":"Bash/ksh `[[ ]]]` can include line breaks anywhere, but `[ ]` requires that you escape them. If you ","rationale":"Bash/ksh `[[ ]]]` can include line breaks anywhere, but `[ ]` requires that you escape them. If you are writing a multi-line `[ .. ]` statement, make sure to include these escapes. If the `[ ]` is sup","problematicCode":"[ \"$filename\" =\n  \"$otherfile\" ]","correctCode":"[ \"$filename\" = \\\n  \"$otherfile\" ]"},"SC1081":{"code":"SC1081","title":"Scripts are case-sensitive. Use `if`, not `If`.","fix":"Shells are case sensitive and do not accept `If` or `IF` in place of lowercase `if`.","rationale":"Shells are case sensitive and do not accept `If` or `IF` in place of lowercase `if`.","problematicCode":"If true\nThen\n  echo \"hello\"\nFi","correctCode":"if true\nthen\n  echo \"hello\"\nfi"},"SC1082":{"code":"SC1082","title":"This file has a UTF-8 BOM. Remove it with: `LC_CTYPE=C sed '1s/^...//' < yourscript`.","fix":"Some editors may save a file with a [Byte Order Mark](https://en.wikipedia.org/wiki/Byte_order_mark)","rationale":"Some editors may save a file with a [Byte Order Mark](https://en.wikipedia.org/wiki/Byte_order_mark) to mark the file as UTF-8. Shells do not understand this and will give errors on the first line: ``","problematicCode":"$ cat -v file\nM-oM-;M-?#!/bin/bash\necho \"hello world\"","correctCode":"$ bash myscript\nmyscript: line 1: #!/bin/sh: No such file or directory\n$ dash myscript\nmyscript: 1: myscript: #!/bin/sh: not found"},"SC1083":{"code":"SC1083","title":"This `{`/`}` is literal. Check if `;` is missing or quote the expression.","fix":"Curly brackets are normally used as syntax in parameter expansion, command grouping and brace expans","rationale":"Curly brackets are normally used as syntax in parameter expansion, command grouping and brace expansion. However, if they don't appear alone at the start of an expression or as part of a parameter or ","problematicCode":"rmf() { rm -f \"$@\" }","correctCode":"rmf() { rm -f \"$@\"; }"},"SC1084":{"code":"SC1084","title":"Use `#!`, not `!#`, for the shebang.","fix":"The shebang has been accidentally swapped. The `#` should come first: `#!`, not `!#`.","rationale":"The shebang has been accidentally swapped. The `#` should come first: `#!`, not `!#`.","problematicCode":"!#/bin/sh\necho \"Hello World\"","correctCode":"#!/bin/sh\necho \"Hello World\""},"SC1085":{"code":"SC1085","title":"Did you forget to move the ;; after extending this case item?","fix":"There should be no statements between `;;` and the next case item.","rationale":"There should be no statements between `;;` and the next case item.","problematicCode":"case $options in\n  foobar) echo foo ;; echo bar;;\n  *) echo unknown option ;;\nesac","correctCode":"case $options in\n  foobar) echo foo ; echo bar;;\n  *) echo unknown option ;;\nesac"},"SC1086":{"code":"SC1086","title":"Don't use `$` on the iterator name in for loops.","fix":"The variable is named `var`, and can be expanded to its value with `$var`. The `for` loop expects th","rationale":"The variable is named `var`, and can be expanded to its value with `$var`. The `for` loop expects the variable's name, not its value (and the name can not be specified indirectly).","problematicCode":"for $var in *\ndo\n  echo \"$var\"\ndone","correctCode":"for var in *\ndo\n  echo \"$var\"\ndone"},"SC1087":{"code":"SC1087","title":"Use braces when expanding arrays, e.g. `${array[idx]}` (or `${var}[..` to quiet).","fix":"Some languages use the syntax `$array[index]` to access an index of an arrays, but a shell will inte","rationale":"Some languages use the syntax `$array[index]` to access an index of an arrays, but a shell will interpret this as `$array` followed by the unrelated literal string (or glob) `[index]`. Curly braces ar","problematicCode":"echo \"$array[@]\"","correctCode":"echo \"${array[@]}\""},"SC1088":{"code":"SC1088","title":"Parsing stopped here. Invalid use of parentheses?","fix":"Parentheses are shell syntax and must be used correctly. For commands that expect literal parenthese","rationale":"Parentheses are shell syntax and must be used correctly. For commands that expect literal parentheses, such as `grep` or `find`, the parentheses need to be quoted or escaped so the shell does not inte","problematicCode":"grep ^(.*)\\1$ file","correctCode":"grep '^(.*)\\1$' file"},"SC1089":{"code":"SC1089","title":"Parsing stopped here. Is this keyword correctly matched up?","fix":"This error is typically seen when there are too many `fi`, `done` or `esac`s, or when there's a `do`","rationale":"This error is typically seen when there are too many `fi`, `done` or `esac`s, or when there's a `do` or `then` without a corresponding `while`, `for` or `if`. This is often due to deleting a loop or c","problematicCode":"if true\nthen\n  echo hello\nfi\nfi","correctCode":"if true\nthen\n  echo hello\nfi"},"SC1090":{"code":"SC1090","title":"Can't follow non-constant source. Use a directive to specify location","fix":"ShellCheck is not able to include sourced files from paths that are determined at runtime. The file ","rationale":"ShellCheck is not able to include sourced files from paths that are determined at runtime. The file will not be read, potentially resulting in warnings about unassigned variables and similar. Use a [[","problematicCode":". \"${util_path}\"","correctCode":"# shellcheck source=src/util.sh\n. \"${util_path}\""},"SC1091":{"code":"SC1091","title":"Not following: (error message here)","fix":"ShellCheck, for whichever reason, is not able to access the source file. This could be because: - yo","rationale":"ShellCheck, for whichever reason, is not able to access the source file. This could be because: - you did not include it on the command line, - did not use `shellcheck -x` (or specified `external-sour","problematicCode":"source somefile","correctCode":"# shellcheck source=somefile\nsource somefile"},"SC1092":{"code":"SC1092","title":"Stopping at 100 `source` frames :O","fix":"ShellCheck found a chain of 100+ files sourcing each other. It assumed there must be some internal b","rationale":"ShellCheck found a chain of 100+ files sourcing each other. It assumed there must be some internal bug, so it stopped.","problematicCode":"","correctCode":""},"SC1094":{"code":"SC1094","title":"Parsing of sourced file failed. Ignoring it.","fix":"ShellCheck encountered a parsing error in a sourced file, `mylib` in the example. Fix parsing error,","rationale":"ShellCheck encountered a parsing error in a sourced file, `mylib` in the example. Fix parsing error, or just disable it with a directive.","problematicCode":"source mylib","correctCode":"# shellcheck disable=SC1094\nsource mylib"},"SC1095":{"code":"SC1095","title":"You need a space or linefeed between the function name and body.","fix":"When using `function` keyword function definitions without `()`, a space is required between the fun","rationale":"When using `function` keyword function definitions without `()`, a space is required between the function name and the opening `{`.","problematicCode":"function foo{\n  echo \"hello world\"\n}","correctCode":"foo() {\n  echo \"hello world\"\n}"},"SC1097":{"code":"SC1097","title":"Unexpected `==`. For assignment, use `=`. For comparison, use `[`/`[[`.","fix":"ShellCheck has noticed that you're using `==` in an unexpected way. The two most common reasons for ","rationale":"ShellCheck has noticed that you're using `==` in an unexpected way. The two most common reasons for this is: * You wanted to assign a value but accidentally used `==` instead of `=`. * You wanted to c","problematicCode":"var==value","correctCode":"var=value"},"SC1098":{"code":"SC1098","title":"Quote/escape special characters when using `eval`, e.g. `eval \"a=(b)\"`.","fix":"Shells differ widely in how they handle unescaped parentheses in `eval` expressions. * `eval foo=bar","rationale":"Shells differ widely in how they handle unescaped parentheses in `eval` expressions. * `eval foo=bar` is allowed by dash, bash and ksh. * `eval foo=(bar)` is allowed by bash and ksh, but not dash. * `","problematicCode":"eval $var=(a b)","correctCode":"eval \"$var=(a b)\""},"SC1099":{"code":"SC1099","title":"You need a space before the `#`.","fix":"ShellCheck has noticed that you have a keyword immediately followed by a `#`. In order for the `#` t","rationale":"ShellCheck has noticed that you have a keyword immediately followed by a `#`. In order for the `#` to start a comment, it needs to come after a word boundary such as a space.","problematicCode":"while sleep 1\ndo# show time\n  date\ndone","correctCode":"while sleep 1\ndo # show time\n  date\ndone"},"SC1100":{"code":"SC1100","title":"This is a Unicode dash. Delete and retype as ASCII minus.","fix":"A character that looks similar to `-` has made its way into your code. This is usually due to copy-p","rationale":"A character that looks similar to `-` has made its way into your code. This is usually due to copy-pasting from blogs and other websites that formatted code as text, replacing the ASCII hyphen-minus w","problematicCode":"[[ 3 –gt 2 ]] # Uses unicode en-dash character","correctCode":"[[ 3 -gt 2 ]] # Uses regular ASCII hyphen-minus character"},"SC1101":{"code":"SC1101","title":"Delete trailing spaces after `\\` to break line (or use quotes for literal space).","fix":"To break a line you can use `\\` before the line break. However, if there are spaces after the backsl","rationale":"To break a line you can use `\\` before the line break. However, if there are spaces after the backslash, the escape will apply to them instead of the line break, and the command will not continue on t","problematicCode":"# There are spaces after the backslash:\necho hello \\  \n     world","correctCode":"# No spaces after the backslash:\necho hello \\\n     world"},"SC1102":{"code":"SC1102","title":"Shells disambiguate `$((` differently or not at all. For `$(command substitution)`, add space after `$(` . For `$((arithmetics))`, fix parsing errors.","fix":"You appear to be using `$((` with two (or more) parentheses in a row, where the first `$(` should op","rationale":"You appear to be using `$((` with two (or more) parentheses in a row, where the first `$(` should open a subshell. This is an ill-defined structure that is parsed differently between different shells ","problematicCode":"echo \"$((cmd \"$@\") 2>&1)\"","correctCode":"echo \"$( (cmd \"$@\") 2>&1)\""},"SC1103":{"code":"SC1103","title":"This shell type is unknown. Use e.g. `sh` or `bash`.","fix":"Shellcheck only supports a specific range of shell dialects, there are many more applications provid","rationale":"Shellcheck only supports a specific range of shell dialects, there are many more applications providing shell like experiences and some of them look and feel like POSIX shell or bash but does not supp","problematicCode":"# shellcheck shell=zsh\nexport PAGER=less","correctCode":"# shellcheck shell=sh\nexport PAGER=less"},"SC1104":{"code":"SC1104","title":"Use `#!`, not just `!`, for the shebang.","fix":"You appear to be specifying an interpreter in a shebang, but it's missing the hash part. The shebang","rationale":"You appear to be specifying an interpreter in a shebang, but it's missing the hash part. The shebang must always start with `#!`. Even the name \"shebang\" itself comes from \"hash\" (`#`) + \"bang\" (`!`).","problematicCode":"!/bin/sh\necho \"Hello\"","correctCode":"#!/bin/sh\necho \"Hello\""},"SC1105":{"code":"SC1105","title":"Shells disambiguate `((` differently or not at all. If the first `(` should start a subshell, add a space after it.","fix":"Shells disambiguate `((` differently or not at all. If the first `(` should start a subshell, add a space after it.","rationale":"","problematicCode":"","correctCode":""},"SC1106":{"code":"SC1106","title":"In arithmetic contexts, use `<` instead of `-lt`","fix":"The comparators `-lt`, `-ge`, `-eq` and friends are flags for the `test` command aka `[`. You are in","rationale":"The comparators `-lt`, `-ge`, `-eq` and friends are flags for the `test` command aka `[`. You are instead using it in an arithmetic context, such as `(( .. ))` or `$(( .. ))`, where you should be usin","problematicCode":"if (( 2 -lt 3 ))\nthen\n  echo \"True\"\nfi","correctCode":"if (( 2 < 3 ))\nthen\n  echo \"True\"\nfi"},"SC1107":{"code":"SC1107","title":"This directive is unknown. It will be ignored.","fix":"ShellCheck doesn't recognize the [[directive]] you're trying to use in a `# shellcheck` comment. See","rationale":"ShellCheck doesn't recognize the [[directive]] you're trying to use in a `# shellcheck` comment. See the [[Directive]]s page for supported directives. It could be misspelled, or you could be using an ","problematicCode":"# shellcheck foobar=baz\necho \"Hello World\"","correctCode":"# Ignore an unrecognized directive in 0.4.5 or later:\n# shellcheck disable=SC1107\n{\n  # shellcheck unrecognized=directive\n  echo \"Hello World\"\n}"},"SC1108":{"code":"SC1108","title":"You need a space before and after the `=` .","fix":"You appear to be missing the space on the left side of the operator. Shell in general, and `[` in pa","rationale":"You appear to be missing the space on the left side of the operator. Shell in general, and `[` in particular, is space sensitive. Operators and operands must be separate tokens. Please ensure that the","problematicCode":"[ \"$var\"= 2 ]","correctCode":"[ \"$var\" = 2 ]"},"SC1109":{"code":"SC1109","title":"This is an unquoted HTML entity. Replace with corresponding character.","fix":"There is an unquoted HTML entity, such as `&amp;`, `&gt;` or `&lt;` (instead of `&`, `>` and `<`) in","rationale":"There is an unquoted HTML entity, such as `&amp;`, `&gt;` or `&lt;` (instead of `&`, `>` and `<`) in your code. This usually happens when copy-pasting from a web site that has mismanaged its code form","problematicCode":"foo &amp;&amp; bar","correctCode":"foo && bar"},"SC1110":{"code":"SC1110","title":"This is a Unicode quote. Delete and retype it (or quote to make literal).","fix":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with sla","rationale":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with slanted Unicode quotes. Try deleting and retyping them, and/or disable “smart quotes” in your editor or","problematicCode":"echo ‘hello world’","correctCode":"echo 'hello world'"},"SC1111":{"code":"SC1111","title":"This is a Unicode quote. Delete and retype it (or ignore/singlequote for literal).","fix":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with sla","rationale":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with slanted Unicode quotes. The shell does not recognize these quotes and will not respect them. In this ca","problematicCode":"echo \"hello world”","correctCode":"echo \"hello world\""},"SC1112":{"code":"SC1112","title":"This is a Unicode quote. Delete and retype it (or ignore/doublequote for literal).","fix":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with sla","rationale":"Some software, like OS X, Word and WordPress, may automatically replace your regular quotes with slanted Unicode quotes. The shell does not recognize these quotes and will not respect them. In this ca","problematicCode":"echo 'hello world’","correctCode":"echo 'hello world'"},"SC1113":{"code":"SC1113","title":"Use `#!`, not just `#`, for the shebang.","fix":"You appear to be specifying a shebang, but missing the bang (i.e. `!`). The shebang should always be","rationale":"You appear to be specifying a shebang, but missing the bang (i.e. `!`). The shebang should always be on the form `#!/path/shell`.","problematicCode":"# /bin/bash\necho \"Hello World\"","correctCode":"#! /bin/bash\necho \"Hello World\""},"SC1114":{"code":"SC1114","title":"Remove leading spaces before the shebang.","fix":"The script has leading spaces before the shebang (`#!`). This is not allowed. The `#!` should be the","rationale":"The script has leading spaces before the shebang (`#!`). This is not allowed. The `#!` should be the first two bytes in the file, as they're used as a file signature by the OS to determine whether a f","problematicCode":"#!/bin/sh\necho \"Hello world\"","correctCode":"#!/bin/sh\necho \"Hello World\""},"SC1115":{"code":"SC1115","title":"Remove spaces between `#` and `!` in the shebang.","fix":"The script has spaces between the `#` and `!` in the shebang. This is not valid. Remove the spaces s","rationale":"The script has spaces between the `#` and `!` in the shebang. This is not valid. Remove the spaces so the OS can correctly recognize the file as a script.","problematicCode":"# !/bin/sh\necho \"Hello World\"","correctCode":"#!/bin/sh\necho \"Hello World\""},"SC1116":{"code":"SC1116","title":"Missing `$` on a `$((..))` expression? (or use `( (` for arrays).","fix":"You appear to be missing the `$` on an assignment from an arithmetic expression `var=$((..))` . With","rationale":"You appear to be missing the `$` on an assignment from an arithmetic expression `var=$((..))` . Without the `$`, this is an array expression which is either nested (ksh) or invalid (bash).","problematicCode":"var=((foo+1))","correctCode":"var=$((foo+1))"},"SC1117":{"code":"SC1117","title":"Backslash is literal in `\"\\n\"`. Prefer explicit escaping: `\"\\\\n\"`.","fix":"In a double quoted string, you have escaped a character that has no special behavior when escaped. I","rationale":"In a double quoted string, you have escaped a character that has no special behavior when escaped. Instead, it's invoking the fallback behavior of being interpreted literally. Instead of relying on th","problematicCode":"printf \"%s\\n\" \"Hello\"","correctCode":"printf \"%s\\\\n\" \"Hello\""},"SC1118":{"code":"SC1118","title":"Delete whitespace after the here-doc end token.","fix":"The end token of your here document has trailing whitespace. This is invisible to the naked eye, but","rationale":"The end token of your here document has trailing whitespace. This is invisible to the naked eye, but shells do not accept it. Remove the trailing whitespace.","problematicCode":"cat << \"eof\"\nHello\neof▭","correctCode":"cat << \"eof\"\nHello\neof"},"SC1119":{"code":"SC1119","title":"Add a linefeed between end token and terminating `)`.","fix":"When embedding a here document in `$(..)` or `(..)`, there needs to be a linefeed (newline) between ","rationale":"When embedding a here document in `$(..)` or `(..)`, there needs to be a linefeed (newline) between the here doc token and the closing `)`. Please insert one. Failing to do so may cause warnings like ","problematicCode":"var=$(fmt -s \"$COLUMNS\" << \"eof\"\nThis is a bunch of text\neof)","correctCode":"var=$(fmt -s \"$COLUMNS\" << \"eof\"\nThis is a bunch of text\neof\n)"},"SC1120":{"code":"SC1120","title":"No comments allowed after here-doc token. Comment the next line instead.","fix":"The terminator token for a here document must be on an entirely separate line. No comments are allow","rationale":"The terminator token for a here document must be on an entirely separate line. No comments are allowed on this line. Place the comment somewhere else, such as on the following line.","problematicCode":"cat << eof  # --- Start greeting --\nHello\neof         # --- End greeting ---","correctCode":"cat << eof  # --- Start greeting --\nHello\neof\n            # --- End greeting ---"},"SC1121":{"code":"SC1121","title":"Add `;`/`&` terminators (and other syntax) on the line with the `<<`, not here.","fix":"You are using `&`, `;`, `&>` or similar after a here document. This is not allowed. This should inst","rationale":"You are using `&`, `;`, `&>` or similar after a here document. This is not allowed. This should instead be part of the line that initiated the here document, i.e. the one with the `<<`. If it helps, l","problematicCode":"sudo bash -s << \"END\"\n  cmd1\n  cmd2\nEND &","correctCode":"sudo bash -s << \"END\" &\n  cmd1\n  cmd2\nEND"},"SC1122":{"code":"SC1122","title":"Nothing allowed after end token. To continue a command, put it on the line with the `<<`.","fix":"You have a here document, and appear to have added text after the terminating token. This is not all","rationale":"You have a here document, and appear to have added text after the terminating token. This is not allowed. If it was meant to continue the command, put it on the line with the `<<`. If it helps, look a","problematicCode":"cat << EOF\nHello\nEOF | nl","correctCode":"cat << EOF | nl\nHello\nEOF"},"SC1123":{"code":"SC1123","title":"ShellCheck directives are only valid in front of complete compound commands, like `if`, not e.g. individual `elif` branches.","fix":"You appear to have put a directive before a non-command keyword, such as `elif`, `else`, `do`, `;;` ","rationale":"You appear to have put a directive before a non-command keyword, such as `elif`, `else`, `do`, `;;` or similar. Unlike many other linters, ShellCheck comment directives apply to the next shell command","problematicCode":"if [ \"$prod\" = \"true\" ]\nthen\n  echo \"Prod mode\"\n# shellcheck disable=2154\nelif [ \"$debug\" = \"true\" ]\nthen\n  echo \"Debug mode\"\nfi","correctCode":"# Applies to entire `if...fi` command\n# shellcheck disable=2154\nif [ \"$prod\" = \"true\" ]\nthen\n  echo \"Prod mode\"\nelif [ \"$debug\" = \"true\" ]\nthen\n  echo \"Debug mode\"\nfi"},"SC1124":{"code":"SC1124","title":"ShellCheck directives are only valid in front of complete commands like `case` statements, not individual case branches.","fix":"You appear to have put a directive before a branch in a case statement. ShellCheck directives can no","rationale":"You appear to have put a directive before a branch in a case statement. ShellCheck directives can not be scoped to individual branches of `case` statements, only to the entire `case`, or to individual","problematicCode":"case $? in\n  0) echo \"Success\" ;;\n  # shellcheck disable=2154\n  *) echo \"$cmd $flag returned failure\" ;;\nesac","correctCode":"# Applies to everything in the `case` statement\n# shellcheck disable=2154\ncase $? in\n  0) echo \"Success\" ;;\n  *) echo \"$cmd $flag returned failure\" ;;\nesac"},"SC1125":{"code":"SC1125","title":"Invalid `key=value` pair in directive","fix":"A comment at the end of a directive must be preceded by a `#` to avoid it being interpreted as an in","rationale":"A comment at the end of a directive must be preceded by a `#` to avoid it being interpreted as an instruction. The [directive page](Directive#documenting-directive-use) contains more guidance about co","problematicCode":"# shellcheck disable=SC2153 (variable not a misspelling)","correctCode":"# shellcheck disable=SC2153 # variable not a misspelling"},"SC1126":{"code":"SC1126","title":"Place shellcheck directives before commands, not after.","fix":"ShellCheck expects directives to come before the relevant command. They are not allowed after.","rationale":"ShellCheck expects directives to come before the relevant command. They are not allowed after.","problematicCode":"var=1 # shellcheck disable=SC2034","correctCode":"# shellcheck disable=SC2034\nvar=1"},"SC1127":{"code":"SC1127","title":"Was this intended as a comment? Use `#` in sh.","fix":"ShellCheck found what appears to be a C-style comment, a line starting with `//` or `/*`. In Bourne ","rationale":"ShellCheck found what appears to be a C-style comment, a line starting with `//` or `/*`. In Bourne based shell scripts, the comment character is `#`","problematicCode":"// This is a comment.\n/* This too. */","correctCode":"# This is a comment.\n# This too."},"SC1128":{"code":"SC1128","title":"The shebang must be on the first line. Delete blanks and move comments.","fix":"A shebang only has an effect when it appears as the first line in a script. Specifically, the first ","rationale":"A shebang only has an effect when it appears as the first line in a script. Specifically, the first two bytes of the file must be `#!`. Adding comments, copyright notices or simply an accidental blank","problematicCode":"# Copyright 2018 Foobar, All rights reserved\n#!/bin/bash","correctCode":"#!/bin/bash\n# Copyright 2018 Foobar, All rights reserved"},"SC1129":{"code":"SC1129","title":"You need a space before the `!`.","fix":"ShellCheck found a keyword immediately followed by a `!`. There needs to be a space between them.","rationale":"ShellCheck found a keyword immediately followed by a `!`. There needs to be a space between them.","problematicCode":"while! [ -f file ]\ndo sleep 1; done","correctCode":"while ! [ -f file ]\ndo sleep 1; done"},"SC1130":{"code":"SC1130","title":"You need a space before the :.","fix":"ShellCheck found a keyword immediately followed by a `:`. `:` is a synonym for `true`, the command t","rationale":"ShellCheck found a keyword immediately followed by a `:`. `:` is a synonym for `true`, the command that \"does nothing, successfully\", and as a command name it needs a space. `do:` is as invalid as `do","problematicCode":"until make\ndo:; done","correctCode":"until make\ndo :; done"},"SC1131":{"code":"SC1131","title":"Use `elif` to start another branch.","fix":"ShellCheck noticed that you appear to be using `elseif` or `elsif` as a keyword. This is not valid. ","rationale":"ShellCheck noticed that you appear to be using `elseif` or `elsif` as a keyword. This is not valid. `sh` instead uses `elif` as its alternative branch keyword.","problematicCode":"if false\nthen\n  echo \"hi\"\nelseif true\nthen\n  echo \"ho\"\nfi","correctCode":"if false\nthen\n  echo \"hi\"\nelif true\nthen\n  echo \"ho\"\nfi"},"SC1132":{"code":"SC1132","title":"This `&` terminates the command. Escape it or add space after `&` to silence.","fix":"An unescaped and unquoted `&` terminates the command, but here it's used in the middle of what would","rationale":"An unescaped and unquoted `&` terminates the command, but here it's used in the middle of what would otherwise be a shell word. This most commonly happens when copying a URL with query string paramete","problematicCode":"curl https://www.google.com/search?q=cats&tbm=isch","correctCode":"curl \"https://www.google.com/search?q=cats&tbm=isch\""},"SC1133":{"code":"SC1133","title":"Unexpected start of line. If breaking lines, `|`/`||`/`&&` should be at the end of the previous one.","fix":"ShellCheck has found a line that unexpectedly started with `|`, `||` or `&&`. This usually happens w","rationale":"ShellCheck has found a line that unexpectedly started with `|`, `||` or `&&`. This usually happens when a line is broken incorrectly. When breaking around a `|`, `||` or `&&`, there are two options: *","problematicCode":"dmesg\n  | grep \"error\"","correctCode":"dmesg |\n  grep \"error\""},"SC1134":{"code":"SC1134","title":"Error parsing `shellcheckrc`:","fix":"Error parsing `shellcheckrc`:","rationale":"","problematicCode":"","correctCode":""},"SC1135":{"code":"SC1135","title":"Prefer escape over ending quote to make `$` literal. Instead of `\"It costs $\"5`, use `\"It costs \\$5\"`","fix":"The script appears to be closing a double quoted string for the sole purpose of making a dollar sign","rationale":"The script appears to be closing a double quoted string for the sole purpose of making a dollar sign `$` literal. While this happens to work, the better solution is instead to escape it with a backsla","problematicCode":"echo \"The apples are $\"\"1 each\"\neval \"var=$\"name","correctCode":"echo \"The apples are \\$1 each\"\neval \"var=\\$name\"\n# or better yet: var=\"${!name}\""},"SC1136":{"code":"SC1136","title":"Unexpected characters after terminating `]`. Missing semicolon/linefeed?","fix":"ShellCheck found unexpected characters after the `]` or `]]` in a `test` expression. In the example,","rationale":"ShellCheck found unexpected characters after the `]` or `]]` in a `test` expression. In the example, a colon was accidentally used instead of a semicolon. Similarly, a missing space before a comment (","problematicCode":"if [ -e \"foo.txt\" ]: then\n  echo \"Exists\"\nfi","correctCode":"if [ -e \"foo.txt\" ]; then\n  echo \"Exists\"\nfi"},"SC1137":{"code":"SC1137","title":"Missing second `(` to start arithmetic for ((;;)) loop","fix":"ShellCheck found an arithmetic `for ((;;))` expression where either the `((` or the `))` did not com","rationale":"ShellCheck found an arithmetic `for ((;;))` expression where either the `((` or the `))` did not come as a pair. Make sure to use `(( ))` and not `( )`.","problematicCode":"for (i=0; i<10; i++))\ndo\n  echo $i\ndone","correctCode":"for ((i=0; i<10; i++))\ndo\n  echo $i\ndone"},"SC1138":{"code":"SC1138","title":"Remove spaces between (( in arithmetic for loop.","fix":"ShellCheck finds arithmetic for ((;;)) expressions where (( or )) are intervening with spaces","rationale":"ShellCheck finds arithmetic for ((;;)) expressions where (( or )) are intervening with spaces","problematicCode":"for( (i=0; i<10; i++) ); do echo $i; done","correctCode":"for((i=0; i<10; i++)); do echo $i; done"},"SC1139":{"code":"SC1139","title":"Use `||` instead of `-o` between test commands.","fix":"You have a `[ .. ]` or `[[ .. ]]` test expression followed by `-o`/`-a` (or by Python-style `or`/`an","rationale":"You have a `[ .. ]` or `[[ .. ]]` test expression followed by `-o`/`-a` (or by Python-style `or`/`and`). `-o` and `-a` work *inside* `[ .. ]`, but they do not work *between* them. The Python operators","problematicCode":"if [ \"$1\" = \"-v\" ] -o [ -n \"$VERBOSE\" ]\nthen\n  echo \"Verbose log\"\nfi","correctCode":"if [ \"$1\" = \"-v\" ] || [ -n \"$VERBOSE\" ]\nthen\n  echo \"Verbose log\"\nfi"},"SC1140":{"code":"SC1140","title":"Unexpected parameters after condition. Missing `&&`/`||`, or bad expression?","fix":"ShellCheck found characters (other than redirections) after the `]` or `]]` in a test expression. Th","rationale":"ShellCheck found characters (other than redirections) after the `]` or `]]` in a test expression. This is not valid. This sometimes happens when there was an additional expression or command, but join","problematicCode":"[ \"$1\" ] input=\"$1\"","correctCode":"[ \"$1\" ] && input=\"$1\""},"SC1141":{"code":"SC1141","title":"Unexpected tokens after compound command. Bad redirection or missing `;`/`&&`/`||`/`|`?","fix":"ShellCheck found unexpected trailing characters after a compound command. The only things allowed af","rationale":"ShellCheck found unexpected trailing characters after a compound command. The only things allowed after compound commands are redirections, shell keywords, and the various command separators (`;`, `&`","problematicCode":"while echo \"$2\"; do true; done \\\n  head -n \"$1\"\nwhile sleep 1; do date; done > my file","correctCode":"while echo \"$2\"; do true; done \\\n  | head -n \"$1\"\nwhile sleep 1; do date; done > \"my file\""},"SC1142":{"code":"SC1142","title":"Use `done < <(cmd)` to redirect from process substitution (currently missing one `<`).","fix":"ShellCheck found a `done` keyword followed by a process substitution, e.g. `done <(cmd)`. The intent","rationale":"ShellCheck found a `done` keyword followed by a process substitution, e.g. `done <(cmd)`. The intention was most likely to redirect from this process substitution, in which case you will need one extr","problematicCode":"sum=0\nwhile IFS=\"\" read -r n\ndo\n  (( sum += n ))\ndone <(file)","correctCode":"sum=0\nwhile IFS=\"\" read -r n\ndo\n  (( sum += n ))\ndone < <(file)"},"SC1143":{"code":"SC1143","title":"This backslash is part of a comment and does not continue the line.","fix":"ShellCheck found a line continuation followed by a commented line that appears to try to do the same","rationale":"ShellCheck found a line continuation followed by a commented line that appears to try to do the same. Backslash line continuations are not respected in comments, and the line instead simply terminates","problematicCode":"sed \\\n  -e \"s/HOST/$HOSTNAME/g\"   \\\n# -e \"s/USER/$USER/g\"       \\\n  -e \"s/ARCH/$(uname -m)/g\" \\\n  \"$buildfile\"","correctCode":"sed \\\n  -e \"s/HOST/$HOSTNAME/g\"   \\\n  -e \"s/ARCH/$(uname -m)/g\" \\\n  \"$buildfile\"\n# This comment is moved out:\n# -e \"s/USER/$USER/g\"       \\"},"SC1144":{"code":"SC1144","title":"`external-sources` can only be enabled in .shellcheckrc, not in individual files.","fix":"Due to its origins as an online tool, ShellCheck will by default run in a sandbox where it only read","rationale":"Due to its origins as an online tool, ShellCheck will by default run in a sandbox where it only reads the files explicitly named on the command line. The `external-sources` [[directive]] allows disabl","problematicCode":"# shellcheck external-sources=true\nsource /dev/zero","correctCode":""},"SC1145":{"code":"SC1145","title":"Unknown `external-sources` value. Expected `true`/`false`.","fix":"The `external-sources` [[directive]] expects a value of `true` or `false`.","rationale":"The `external-sources` [[directive]] expects a value of `true` or `false`.","problematicCode":"external-sources=maybe","correctCode":"external-sources=true"},"SC2000":{"code":"SC2000","title":"See if you can use `${#variable}` instead","fix":"See if you can use `${#variable}` instead","rationale":"","problematicCode":"","correctCode":""},"SC2001":{"code":"SC2001","title":"See if you can use `${variable//search/replace}` instead.","fix":"Let's assume somewhere earlier in your code, you have put data into a variable (Ex: $string). Now yo","rationale":"Let's assume somewhere earlier in your code, you have put data into a variable (Ex: $string). Now you want to search and replace inside the contents of $string and echo the contents out. You could pas","problematicCode":"string=\"bwol\" ; echo \"$string\" | sed -e \"s/wo/ow/\"","correctCode":"string=\"bwol\" ; echo \"${string//wo/ow}\""},"SC2002":{"code":"SC2002","title":"Useless cat. Consider `cmd < file | ..` or `cmd file | ..` instead.","fix":"`cat` is a tool for con\"cat\"enating files. Reading a single file as input to a program is considered","rationale":"`cat` is a tool for con\"cat\"enating files. Reading a single file as input to a program is considered a [Useless Use Of Cat (UUOC)](http://en.wikipedia.org/wiki/Cat_(Unix)#Useless_use_of_cat). It's mor","problematicCode":"cat file | tr ' ' _ | nl\ncat file | while IFS= read -r i; do echo \"${i%?}\"; done","correctCode":"< file tr ' ' _ | nl  \nwhile IFS= read -r i; do echo \"${i%?}\"; done < file"},"SC2003":{"code":"SC2003","title":"expr is antiquated. Consider rewriting this using `$((..))`, `${}` or `[[  ]]`.","fix":"[To quote POSIX:](https://pubs.opengroup.org/onlinepubs/009695399/utilities/expr.html#tag_04_50_17) ","rationale":"[To quote POSIX:](https://pubs.opengroup.org/onlinepubs/009695399/utilities/expr.html#tag_04_50_17) > The expr utility has a rather difficult syntax [...] In many cases, the arithmetic and string feat","problematicCode":"i=$(expr 1 + 2)\nl=$(expr length \"$var\")","correctCode":"i=$((1+2))\nl=${#var}"},"SC2004":{"code":"SC2004","title":"`$`/`${}` is unnecessary on arithmetic variables.","fix":"The `$` or `${..}` on regular variables in arithmetic contexts is unnecessary, and can even lead to ","rationale":"The `$` or `${..}` on regular variables in arithmetic contexts is unnecessary, and can even lead to subtle bugs. This is because the contents of `$((..))` is first expanded into a string, and then eva","problematicCode":"echo $(($n + ${arr[i]}))","correctCode":"echo $((n + arr[i]))"},"SC2005":{"code":"SC2005","title":"Useless `echo`? Instead of `echo $(cmd)`, just use `cmd`","fix":"ShellCheck found the unnecessary construct `echo \"$(somecommand here)\"`. This is generally due to a ","rationale":"ShellCheck found the unnecessary construct `echo \"$(somecommand here)\"`. This is generally due to a misunderstanding about what `echo` does. It has no role in \"showing on screen\" or similar, but simpl","problematicCode":"echo \"$(whoami)\"","correctCode":"whoami"},"SC2006":{"code":"SC2006","title":"Use `$(...)` notation instead of legacy backticked `` `...` ``.","fix":"Backtick command substitution `` `...` `` is legacy syntax with several issues. 1. It has a series o","rationale":"Backtick command substitution `` `...` `` is legacy syntax with several issues. 1. It has a series of undefined behaviors related to quoting in POSIX. 1. It imposes a custom escaping mode with surpris","problematicCode":"echo \"You are running on `uname`\"","correctCode":"echo \"You are running on $(uname)\""},"SC2007":{"code":"SC2007","title":"Use `$((..))` instead of deprecated `$[..]`.","fix":"The `$[..]` syntax was deprecated in Bash 2.0 and replaced with the standard `$((..))` syntax from K","rationale":"The `$[..]` syntax was deprecated in Bash 2.0 and replaced with the standard `$((..))` syntax from Korn shell","problematicCode":"n=1\nn=$[n+1]","correctCode":"n=1\nn=$((n+1))"},"SC2008":{"code":"SC2008","title":"`echo` doesn't read from stdin, are you sure you should be piping to it?","fix":"You are piping command output to `echo`, but `echo` ignores all piped input. In particular, `echo` i","rationale":"You are piping command output to `echo`, but `echo` ignores all piped input. In particular, `echo` is not responsible for putting output on screen. Commands already output data, and with no further ac","problematicCode":"find . | echo","correctCode":"find ."},"SC2009":{"code":"SC2009","title":"Consider using `pgrep` instead of grepping `ps` output.","fix":"If you are just after a pid from a running program, then pgrep is a much safer alternative. Especial","rationale":"If you are just after a pid from a running program, then pgrep is a much safer alternative. Especially if you are also looking for a pid belonging to a certain user or group. All of the parameters are","problematicCode":"ps ax | grep -v grep | grep \"$service\" > /dev/null","correctCode":"pgrep -f \"$service\" > /dev/null"},"SC2010":{"code":"SC2010","title":"Don't use `ls | grep`. Use a glob or a for loop with a condition to allow non-alphanumeric filenames.","fix":"[Parsing ls](https://mywiki.wooledge.org/ParsingLs) is generally a bad idea because the output is fr","rationale":"[Parsing ls](https://mywiki.wooledge.org/ParsingLs) is generally a bad idea because the output is fragile and human readable. To better handle non-alphanumeric filenames, use a glob. If you need more ","problematicCode":"ls /directory | grep mystring","correctCode":"# BASH\nshopt -s extglob\nrm -- !(*.c)\n# POSIX\nfor f in ./*\ndo\n  case $f in\n    *.c) true;;\n    *) rm \"$f\";;\n  esac\ndone"},"SC2011":{"code":"SC2011","title":"Use `find -print0` or `find -exec` to better handle non-alphanumeric filenames.","fix":"Using `-print0` separates each output with a NUL character, rather than a newline, which is safer to","rationale":"Using `-print0` separates each output with a NUL character, rather than a newline, which is safer to pipe into `xargs`. Alternatively using `-exec` avoids the problem of piping and parsing filenames i","problematicCode":"ls | xargs -n1 wc -w","correctCode":"find . -maxdepth 1 -print0 | xargs -0 -n1 wc -w"},"SC2012":{"code":"SC2012","title":"Use `find` instead of `ls` to better handle non-alphanumeric filenames.","fix":"`ls` is only intended for human consumption: it has a loose, non-standard format and may \"clean up\" ","rationale":"`ls` is only intended for human consumption: it has a loose, non-standard format and may \"clean up\" filenames to make output easier to read. Here's an example: ```console $ ls -l total 0 -rw-r----- 1 ","problematicCode":"ls -l | grep \" $USER \" | grep '\\.txt$'","correctCode":"find ./*.txt -user \"$USER\"  # Using the names of the files"},"SC2013":{"code":"SC2013","title":"To read lines rather than words, pipe/redirect to a `while read` loop.","fix":"For loops by default (subject to `$IFS`) read word by word. Additionally, glob expansion will occur.","rationale":"For loops by default (subject to `$IFS`) read word by word. Additionally, glob expansion will occur. Given this text file: ```sh foo * bar ``` The for loop will print: ```sh Line: foo Line: aardwark.j","problematicCode":"for line in $(cat file | grep -v '^ *#')\ndo\n  echo \"Line: $line\"\ndone","correctCode":"grep -v '^ *#' file | while IFS= read -r line\ndo\n  echo \"Line: $line\"\ndone"},"SC2014":{"code":"SC2014","title":"This will expand once before find runs, not per file found.","fix":"Bash evaluates any command substitutions before the command they feature in is executed. In this cas","rationale":"Bash evaluates any command substitutions before the command they feature in is executed. In this case, the command is `find`. This means that `$(dirname {})` will run **before** `find` runs, and not *","problematicCode":"find . -name '*.tar' -exec tar xf {} -C \"$(dirname {})\" \\;","correctCode":"find . -name '*.tar' -exec sh -c 'tar xf \"$1\" -C \"$(dirname \"$1\")\"' _ {} \\;"},"SC2015":{"code":"SC2015","title":"Note that `A && B || C` is not if-then-else. C may run when A is true.","fix":"It's common to use `A && B` to run `B` when `A` is true, and `A || C` to run `C` when `A` is false. ","rationale":"It's common to use `A && B` to run `B` when `A` is true, and `A || C` to run `C` when `A` is false. However, combining them into `A && B || C` is not the same as `if A then B else C`. In this case, if","problematicCode":"[[ $dryrun ]] && echo \"Would delete file\" || rm file","correctCode":"if [[ $dryrun ]]\nthen\n  echo \"Would delete file\"\nelse\n  rm file\nfi"},"SC2016":{"code":"SC2016","title":"Expressions don't expand in single quotes, use double quotes for that.","fix":"ShellCheck found an expansion like `$var`, `$(cmd)`, or `` `cmd` `` in single quotes. Single quotes ","rationale":"ShellCheck found an expansion like `$var`, `$(cmd)`, or `` `cmd` `` in single quotes. Single quotes express all such expansions. If you want the expression to expand, use double quotes instead. If swi","problematicCode":"name=World\necho 'Hello $name'   # Outputs Hello $name","correctCode":"name=World\necho \"Hello $name\"   # Outputs Hello World"},"SC2017":{"code":"SC2017","title":"Increase precision by replacing `a/b*c` with `a*c/b`.","fix":"If integer division is performed before multiplication, the intermediate result will be truncated ca","rationale":"If integer division is performed before multiplication, the intermediate result will be truncated causing a loss of precision. In this case, if  `count=1` and `total=2`, then the problematic code resu","problematicCode":"percent=$((count/total*100))","correctCode":"percent=$((count*100/total))"},"SC2018":{"code":"SC2018","title":"Use `[:lower:]` to support accents and foreign alphabets.","fix":"`A-Z` and `a-z` are commonly intended to mean \"all uppercase\" and \"all lowercase letters\" respective","rationale":"`A-Z` and `a-z` are commonly intended to mean \"all uppercase\" and \"all lowercase letters\" respectively. This ignores accented characters in English, and foreign characters in other languages: $ tr 'a-","problematicCode":"PLATFORM=\"$(uname -s | tr 'A-Z' 'a-z')\"","correctCode":"PLATFORM=\"$(uname -s | tr '[:upper:]' '[:lower:]')\""},"SC2019":{"code":"SC2019","title":"Use `[:upper:]` to support accents and foreign alphabets.","fix":"Use `[:upper:]` to support accents and foreign alphabets.","rationale":"","problematicCode":"","correctCode":""},"SC2020":{"code":"SC2020","title":"`tr` replaces sets of chars, not words (mentioned due to duplicates).","fix":"`tr` is for `tr`ansliteration, turning some characters into other characters. It doesn't match strin","rationale":"`tr` is for `tr`ansliteration, turning some characters into other characters. It doesn't match strings or words, only individual characters. In this case, it transliterates h->g, e->o, l->d, o->y, res","problematicCode":"echo 'hello world' | tr 'hello' 'goodbye'","correctCode":"echo 'hello world' | sed -e 's/hello/goodbye/g'"},"SC2021":{"code":"SC2021","title":"Don't use `[]` around ranges in `tr`, it replaces literal square brackets.","fix":"Ancient System V `tr` required brackets around operands, but modern implementations including POSIX,","rationale":"Ancient System V `tr` required brackets around operands, but modern implementations including POSIX, GNU, OS X and *BSD instead treat them as literals. Unless you want to operate on literal square bra","problematicCode":"tr -cd '[a-z]'","correctCode":"tr -cd 'a-z'"},"SC2022":{"code":"SC2022","title":"Note that unlike globs, `o*` here matches `ooo` but not `oscar`.","fix":"As a glob, `foo*` means \"Any string starting with foo\", e.g. `food` and `foosball`. As a regular exp","rationale":"As a glob, `foo*` means \"Any string starting with foo\", e.g. `food` and `foosball`. As a regular expression, \"foo*\" means \"f followed by 1 or more o's, anywhere\", e.g. \"mofo\" or \"keyfob\". This constru","problematicCode":"grep 'foo*'","correctCode":"grep '^foo'"},"SC2023":{"code":"SC2023","title":"The shell may override `time` as seen in man time(1). Use `command time ..` for that one.","fix":"`time` is a built-in command. If you would like to use `time` from `$PATH`, you need to use `command","rationale":"`time` is a built-in command. If you would like to use `time` from `$PATH`, you need to use `command` to execute it as a regular command.","problematicCode":"time -some some","correctCode":"command time -some some"},"SC2024":{"code":"SC2024","title":"`sudo` doesn't affect redirects. Use `..| sudo tee file`","fix":"Redirections are performed by the current shell before `sudo` is started. This means that it will us","rationale":"Redirections are performed by the current shell before `sudo` is started. This means that it will use the current shell's user and permissions to open and read from or write to the file. * To *read* f","problematicCode":"# Write to a file\nsudo echo 3 > /proc/sys/vm/drop_caches\n# Append to a file\nsudo echo 'export FOO=bar' >> /etc/profile\n# Read from a file\nsudo wc -l < /etc/shadow","correctCode":"# Write to a file\necho 3 | sudo tee /proc/sys/vm/drop_caches > /dev/null\n# Append to a file\necho 'export FOO=bar' | sudo tee -a /etc/profile > /dev/null\n# Read from a file\nsudo cat /etc/shadow | wc -l"},"SC2025":{"code":"SC2025","title":"Make sure all escape sequences are enclosed in `\\[..\\]` to prevent line wrapping issues.","fix":"Bash is unable to determine exactly which parts of your prompt are text and which are terminal codes","rationale":"Bash is unable to determine exactly which parts of your prompt are text and which are terminal codes. You have to help it by wrapping invisible control codes in `\\[..\\]` (and ensuring that visible cha","problematicCode":"PS1='\\e[36m\\$ \\e(B\\e[m'","correctCode":"PS1='\\[\\e[36m\\]\\$ \\[\\e(B\\e[m\\]'"},"SC2026":{"code":"SC2026","title":"This word is outside of quotes. Did you intend to `'nest '\"'single quotes'\"'` instead?","fix":"In the first case, the user has four single quotes on a line, wishfully hoping that the shell will m","rationale":"In the first case, the user has four single quotes on a line, wishfully hoping that the shell will match them up as outer quotes around a string with literal single quotes: ```sh #                   v","problematicCode":"alias server_uptime='ssh $host 'uptime -p''","correctCode":"alias server_uptime='ssh $host '\"'uptime -p'\""},"SC2027":{"code":"SC2027","title":"The surrounding quotes actually unquote this. Remove or escape them.","fix":"Always quoting variables and command expansions is good practice, but blindly putting quotes left an","rationale":"Always quoting variables and command expansions is good practice, but blindly putting quotes left and right of them is not. In this case, ShellCheck has noticed that the quotes around the expansion ar","problematicCode":"echo \"You enter \"$HOSTNAME\". You can smell the wumpus.\" >> /etc/issue","correctCode":"echo \"You enter $HOSTNAME. You can smell the wumpus.\" >> /etc/issue"},"SC2028":{"code":"SC2028","title":"`echo` won't expand escape sequences. Consider `printf`.","fix":"Backslash escapes like `\\t` and `\\n` are not expanded by echo, and become literal backslash-t, backs","rationale":"Backslash escapes like `\\t` and `\\n` are not expanded by echo, and become literal backslash-t, backslash-n. `printf` does expand these sequences, and should be used instead. Other, non-portable method","problematicCode":"echo \"Name:\\t$value\"","correctCode":"printf 'Name:\\t%s\\n' \"$value\""},"SC2029":{"code":"SC2029","title":"Note that, unescaped, this expands on the client side.","fix":"Bash expands all arguments that are not escaped/singlequoted. This means that the problematic code i","rationale":"Bash expands all arguments that are not escaped/singlequoted. This means that the problematic code is identical to ```sh ssh host \"echo clienthostname\" ``` and will print out the client's hostname, no","problematicCode":"ssh host \"echo $HOSTNAME\"","correctCode":"ssh host \"echo \\$HOSTNAME\""},"SC2030":{"code":"SC2030","title":"Modification of var is local (to subshell caused by pipeline).","fix":"Modification of var is local (to subshell caused by pipeline).","rationale":"","problematicCode":"","correctCode":""},"SC2031":{"code":"SC2031","title":"var was modified in a subshell. That change might be lost.","fix":"Variables set in subshells are not available outside the subshell. This is a wide topic, and better ","rationale":"Variables set in subshells are not available outside the subshell. This is a wide topic, and better described on the [Wooledge Bash Wiki](http://mywiki.wooledge.org/BashFAQ/024). Here are some constru","problematicCode":"n=0\nprintf \"%s\\n\" {1..10} | while read i; do (( n+=i )); done\necho $n","correctCode":"# Bash specific: process substitution. Also try shopts like lastpipe.\nn=0\nwhile read i; do (( n+=i )); done < <(printf \"%s\\n\" {1..10})\necho $n"},"SC2032":{"code":"SC2032","title":"This function can't be invoked via su on line 42.","fix":"This function can't be invoked via su on line 42.","rationale":"","problematicCode":"","correctCode":""},"SC2033":{"code":"SC2033","title":"Shell functions can't be passed to external commands. Use separate script or sh -c.","fix":"Shell functions are only known to the shell. External commands like `find`, `xargs`, `su` and `sudo`","rationale":"Shell functions are only known to the shell. External commands like `find`, `xargs`, `su` and `sudo` do not recognize shell functions. Instead, the function contents can be executed in a shell, either","problematicCode":"foo() { bar --baz \"$@\"; frob --baz \"$@\"; };\nfind . -exec foo {} +","correctCode":"find . -exec sh -c 'bar --baz \"$@\"; frob --baz \"$@\";' -- {} +"},"SC2034":{"code":"SC2034","title":"foo appears unused. Verify it or export it.","fix":"Use variable: echo \"$varname\" | Export: export varname | Use dummy: varname=_ or _varname","rationale":"Variables not used for anything are often associated with bugs, so ShellCheck warns about them. Also note that something like `local let foo=42` does not make a `let` statement local -- it instead dec","problematicCode":"foo=42\necho \"$FOO\"","correctCode":"foo=42\necho \"$foo\""},"SC2035":{"code":"SC2035","title":"Use `./*glob*` or `-- *glob*` so names with dashes won't become options.","fix":"Since files and arguments are strings passed the same way, programs can't properly determine which i","rationale":"Since files and arguments are strings passed the same way, programs can't properly determine which is which, and rely on dashes to determine what's what. A file named `-f` (`touch -- -f`) will not be ","problematicCode":"","correctCode":""},"SC2036":{"code":"SC2036","title":"If you wanted to assign the output of the pipeline, use `a=$(b | c)` .","fix":"The intention in this code was that `sum` would in some way get the value of the command `find | wc ","rationale":"The intention in this code was that `sum` would in some way get the value of the command `find | wc -l`. However, `|` has precedence over the `=`, so the command is a two stage pipeline consisting of ","problematicCode":"sum=find | wc -l","correctCode":"sum=$(find | wc -l)"},"SC2037":{"code":"SC2037","title":"To assign the output of a command, use `var=$(cmd)` .","fix":"To assign the output of a command to a variable, use `$(command substitution)`. Just typing a comman","rationale":"To assign the output of a command to a variable, use `$(command substitution)`. Just typing a command after the `=` sign does not work.","problematicCode":"var=grep -c pattern file","correctCode":"var=$(grep -c pattern file)"},"SC2038":{"code":"SC2038","title":"Use `-print0`/`-0` or `find -exec +` to allow for non-alphanumeric filenames.","fix":"By default, `xargs` interprets spaces and quotes in an unsafe and unexpected way. Whenever it's used","rationale":"By default, `xargs` interprets spaces and quotes in an unsafe and unexpected way. Whenever it's used, it should be used with `-0` or `--null` to split on `\\0` bytes, and `find` should be made to outpu","problematicCode":"find . -type f | xargs md5sum","correctCode":"find . -type f -print0 | xargs -0 md5sum\nfind . -type f -exec md5sum {} +"},"SC2039":{"code":"SC2039","title":"In POSIX sh, *something* is undefined.","fix":"$'c-style-escape'","rationale":"","problematicCode":"","correctCode":""},"SC2040":{"code":"SC2040","title":"`#!/bin/sh` was specified, so ____ is not supported, even when sh is actually bash.","fix":"`#!/bin/sh` was specified, so ____ is not supported, even when sh is actually bash.","rationale":"","problematicCode":"","correctCode":""},"SC2041":{"code":"SC2041","title":"This is a literal string. To run as a command, use `$(..)` instead of `'..'` .","fix":"The intent was to run the code in the single quotes. This would have worked with slanted backticks, ","rationale":"The intent was to run the code in the single quotes. This would have worked with slanted backticks, `` `..` ``, but here the very similar looking single quotes `'..'` were used, resulting in a string ","problematicCode":"for i in 'seq 1 10'\ndo\n  echo \"$i\"\ndone","correctCode":"for i in $(seq 1 10)\ndo\n  echo \"$i\"\ndone"},"SC2042":{"code":"SC2042","title":"Use spaces, not commas, to separate loop elements.","fix":"ShellCheck found a `for` loop where the items appeared to be delimited by commas. These will be trea","rationale":"ShellCheck found a `for` loop where the items appeared to be delimited by commas. These will be treated as literal commas. Use spaces instead.","problematicCode":"for f in foo,bar,baz\ndo\n  echo \"$f\"\ndone","correctCode":"for f in foo bar baz\ndo\n  echo \"$f\"\ndone"},"SC2043":{"code":"SC2043","title":"This loop will only ever run once for a constant value. Did you perhaps mean to loop over `dir/*`, `$var` or `$(cmd)`?","fix":"ShellCheck has detected that your for loop iterates over a single, constant value. This is most like","rationale":"ShellCheck has detected that your for loop iterates over a single, constant value. This is most likely a bug in your code, caused by you not expanding the value in the way you want. You should make su","problematicCode":"for var in value\ndo\n  echo \"$var\"\ndone","correctCode":"for var in /my/dir/* ; do echo \"$var\"; done"},"SC2044":{"code":"SC2044","title":"For loops over find output are fragile. Use `find -exec` or a `while read` loop.","fix":"`for var in $(find ...)`  loops rely on word splitting and will evaluate globs, which will wreck hav","rationale":"`for var in $(find ...)`  loops rely on word splitting and will evaluate globs, which will wreck havoc with filenames containing whitespace or glob characters. `find -exec` `for i in glob` and `find`+","problematicCode":"for file in $(find mydir -mtime -7 -name '*.mp3')\ndo\n  (( count++ ))\n  echo \"Playing file no. $count\"\n  play \"$file\"\ndone\necho \"Played $count files\"","correctCode":"while IFS= read -r -d '' file\ndo\n  (( count++ ))\n  echo \"Playing file no. $count\"\n  play \"$file\"\ndone <   <(find mydir -mtime -7 -name '*.mp3' -print0)\necho \"Played $count files\""},"SC2045":{"code":"SC2045","title":"Iterating over ls output is fragile. Use globs.","fix":"When looping over a set of files, it's always better to use globs when possible. Using command expan","rationale":"When looping over a set of files, it's always better to use globs when possible. Using command expansion causes word splitting and glob expansion, which will cause problems for certain filenames (typi","problematicCode":"for f in $(ls *.wav)\ndo\n  echo \"$f\"\ndone","correctCode":"for f in *.wav\ndo\n  [[ -e \"$f\" ]] || break  # handle the case of no *.wav files\n  echo \"$f\"\ndone"},"SC2046":{"code":"SC2046","title":"Quote this to prevent word splitting.","fix":"When command expansions are unquoted, word splitting and globbing will occur. This often manifests i","rationale":"When command expansions are unquoted, word splitting and globbing will occur. This often manifests itself by breaking when filenames contain spaces. Trying to fix it by adding quotes or escapes to the","problematicCode":"ls -l $(getfilename)","correctCode":"# getfilename outputs 1 file\nls -l \"$(getfilename)\"\n# getfilename outputs multiple files, linefeed separated\ngetfilename | while IFS='' read -r line\ndo\n    ls -l \"$line\"\ndone"},"SC2048":{"code":"SC2048","title":"Use `\"$@\"` (with quotes) to prevent whitespace problems.","fix":"`$*` and `${array[*]}`, unquoted, is subject to word splitting and globbing. Let's say you have thre","rationale":"`$*` and `${array[*]}`, unquoted, is subject to word splitting and globbing. Let's say you have three arguments or array elements: `baz`, `foo bar` and `*` `\"$@\"` and `\"${array[@]}\" `will expand into ","problematicCode":"cp $* ~/dir\ncp ${array[*]} ~/dir","correctCode":"cp \"$@\" ~/dir\ncp \"${array[@]}\" ~/dir"},"SC2049":{"code":"SC2049","title":"`=~` is for regex, but this looks like a glob. Use `=` instead.","fix":"You are using `=~` to match against a regex -- specifically a Extended Regular Expression (ERE) -- b","rationale":"You are using `=~` to match against a regex -- specifically a Extended Regular Expression (ERE) -- but the right-hand side looks more like a glob: * It may have a leading `*`, like in `*.txt` * In a g","problematicCode":"[[ $file =~ *.txt ]]","correctCode":"[[ $file = *.txt ]]"},"SC2050":{"code":"SC2050","title":"This expression is constant. Did you forget the `$` on a variable?","fix":"ShellCheck has found a `[ .. ]` or `[[ .. ]]` comparison that only involves literal strings. The int","rationale":"ShellCheck has found a `[ .. ]` or `[[ .. ]]` comparison that only involves literal strings. The intention was probably to check a variable or command output instead. This is usually due to missing `$","problematicCode":"if [ myvar = \"test\" ]\nthen\n  echo \"Test mode\"\nfi","correctCode":"if [ \"$myvar\" = \"test\" ]\nthen\n  echo \"Test mode\"\nfi"},"SC2051":{"code":"SC2051","title":"Bash doesn't support variables in brace range expansions.","fix":"In Bash, brace expansion happens before variable expansion. This means that brace expansion will not","rationale":"In Bash, brace expansion happens before variable expansion. This means that brace expansion will not account for variables. For integers, use an arithmetic for loop instead. For zero-padded numbers or","problematicCode":"for i in {1..$n}\ndo\n  echo \"$i\"\ndone","correctCode":"for ((i=1; i <= n; i++))\ndo\n  echo \"$i\"\ndone"},"SC2053":{"code":"SC2053","title":"Quote the rhs of `=` in `[[ ]]` to prevent glob matching.","fix":"When the right-hand side of `=`, `==` or `!=` is unquoted in `[[ .. ]]`, it will be treated like a g","rationale":"When the right-hand side of `=`, `==` or `!=` is unquoted in `[[ .. ]]`, it will be treated like a glob. This has some unexpected consequences like `[[ $var = $var ]]` being false (for `var='[a]'`), o","problematicCode":"[[ $a = $b ]]","correctCode":"[[ $a = \"$b\" ]]"},"SC2054":{"code":"SC2054","title":"Use spaces, not commas, to separate array elements.","fix":"You appear to have used commas to separate array elements in an array assignment. Other languages re","rationale":"You appear to have used commas to separate array elements in an array assignment. Other languages require this, but bash instead treats the commas as literal strings. In the problematic code, the firs","problematicCode":"flags=(\"-l\", \"-d\", \"--sort=size\")\nls \"${flags[@]}\"","correctCode":"flags=(\"-l\" \"-d\" \"--sort=size\")\nls \"${flags[@]}\""},"SC2055":{"code":"SC2055","title":"You probably wanted `&&` here, otherwise it's always true.","fix":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `[[ $1 !","rationale":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `[[ $1 != foo || $1 != bar ]]` is always true (when `foo != bar`): * If `$1 = foo` then `$1 != bar` is true,","problematicCode":"if [[ $1 != foo || $1 != bar ]]\nthen\n  echo \"$1 is not foo or bar\"\nfi","correctCode":"if [[ $1 != foo && $1 != bar ]]\nthen\n  echo \"$1 is not foo or bar\"\nfi"},"SC2056":{"code":"SC2056","title":"You probably wanted `&&` here","fix":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `(( $1 !","rationale":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `(( $1 != 0 || $1 != 3 ))` is always true: * If `$1 = 0` then `$1 != 3` is true, so the statement is true. *","problematicCode":"if  (( $1 != 0 || $1 != 3 ))\nthen\n  echo \"$1 is not 0 or 3\"\nfi","correctCode":"if  (( $1 != 0 && $1 != 3 ))\nthen\n  echo \"$1 is not 0 or 3\"\nfi"},"SC2057":{"code":"SC2057","title":"Unknown binary operator.","fix":"You are using an unknown binary operator in a `test` expression. Choose one that exists. In bash, us","rationale":"You are using an unknown binary operator in a `test` expression. Choose one that exists. In bash, use `help test` to see a list of supported operators: FILE1 -nt FILE2  True if file1 is newer than fil","problematicCode":"[ \"$var\" -leq 42 ]","correctCode":"[ \"$var\" -le 42 ]"},"SC2058":{"code":"SC2058","title":"Unknown unary operator.","fix":"You are using an unknown unary operator in a `test` expression. Perhaps it's a typo? In bash, you ca","rationale":"You are using an unknown unary operator in a `test` expression. Perhaps it's a typo? In bash, you can use `help test` to see a list of supported operators: -a FILE        True if file exists. -b FILE ","problematicCode":"[ -E 42 ]","correctCode":"[ -e 42 ]"},"SC2059":{"code":"SC2059","title":"Don't use variables in the `printf` format string. Use `printf \"..%s..\" \"$foo\"`.","fix":"`printf` interprets escape sequences and format specifiers in the format string. If variables are in","rationale":"`printf` interprets escape sequences and format specifiers in the format string. If variables are included, any escape sequences or format specifiers in the data will be interpreted too, when you most","problematicCode":"printf \"Hello, $NAME\\n\"","correctCode":"printf \"Hello, %s\\n\" \"$NAME\""},"SC2060":{"code":"SC2060","title":"Quote parameters to `tr` to prevent glob expansion.","fix":"From the shell's point of view, unquoted `[:digit:]` is a glob equivalent to `[digit:]` that matches","rationale":"From the shell's point of view, unquoted `[:digit:]` is a glob equivalent to `[digit:]` that matches any single character filename from the group, such as `d` or `t`, in the current directory. If some","problematicCode":"tr -cd [:digit:]","correctCode":"tr -cd '[:digit:]'"},"SC2061":{"code":"SC2061","title":"Quote the parameter to `-name` so the shell won't interpret it.","fix":"Several find options take patterns to match against, including `-ilname`, `-iname`, `-ipath`, `-ireg","rationale":"Several find options take patterns to match against, including `-ilname`, `-iname`, `-ipath`, `-iregex`, `-iwholename`, `-lname`, `-name`, `-path`, `-regex` and `-wholename`. These compete with the sh","problematicCode":"find . -name *.txt","correctCode":"find . -name '*.txt'"},"SC2062":{"code":"SC2062","title":"Quote the grep pattern so the shell won't interpret it.","fix":"The regex passed to grep frequently contains characters that collide with globs. The code above is s","rationale":"The regex passed to grep frequently contains characters that collide with globs. The code above is supposed to match \"f followed by 1 or more o's\", but if the directory contains a file called \"foo.txt","problematicCode":"","correctCode":""},"SC2063":{"code":"SC2063","title":"Grep uses regex, but this looks like a glob.","fix":"In globs, `*` matches any number of any character. In regex, `*` matches any number of the preceding","rationale":"In globs, `*` matches any number of any character. In regex, `*` matches any number of the preceding character. `grep` uses regex, not globs, so this means that `grep '*foo'` is nonsensical because th","problematicCode":"grep '*foo*'","correctCode":"grep 'foo'   # or more explicitly,  grep '.*foo.*'"},"SC2064":{"code":"SC2064","title":"Use single quotes, otherwise this expands now rather than when signalled.","fix":"With double quotes, all parameter and command expansions will expand when the trap is defined rather","rationale":"With double quotes, all parameter and command expansions will expand when the trap is defined rather than when it's executed. In the example with the Problematic code, the message will contain the dat","problematicCode":"trap \"echo \\\"Finished on $(date)\\\"\" EXIT\ntrap \"rm -fr '$testdir1' '$testdir2'\" $TRAP_SIGNALS\ntrap \"rm $tmp\" $TRAP_SIGNALS\ntrap \"${remove_aar_temp}\" SIGTERM SIGQUIT EXIT\ntrap \"$(shopt -p extglob)\" RETU","correctCode":"trap 'echo \"Finished on $(date)\"' EXIT"},"SC2065":{"code":"SC2065","title":"This is interpreted as a shell file redirection, not a comparison.","fix":"A word that looks like a redirection in simple shell commands causes it to be interpreted as a redir","rationale":"A word that looks like a redirection in simple shell commands causes it to be interpreted as a redirection. ShellCheck would guess that you don't want it in tests.","problematicCode":"[ 1 >2 ] || [ 3>'aaa bb' ] # Simple example of problematic code","correctCode":"[ 1 -gt 2 ] || [ 3 \\> 'aaa bb' ] # arithmetical, lexicographical"},"SC2066":{"code":"SC2066","title":"Since you double-quoted this, it will not word split, and the loop will only run once.","fix":"You get this warning because you have a loop that will only ever run exactly one iteration. Since yo","rationale":"You get this warning because you have a loop that will only ever run exactly one iteration. Since you have a loop, you clearly expect it to run more than once. You just have to decide how it should be","problematicCode":"for s in \"$(mycommand)\"; do echo \"$s\"; done","correctCode":"hello world\nMy *.png"},"SC2067":{"code":"SC2067","title":"Missing `;` or `+` terminating `-exec`. You can't use `|`/`||`/`&&`, and `;` has to be a separate, quoted argument.","fix":"`find -exec` is still subject to all normal shell rules, so all shell features like `|`, `||`, `&` a","rationale":"`find -exec` is still subject to all normal shell rules, so all shell features like `|`, `||`, `&` and `&&` will apply to the `find` command itself, and not to the command you are trying to construct ","problematicCode":"find . -type f -exec shellcheck {} | wc -l \\;\nfind . -exec echo {} ;","correctCode":"find . -type f -exec sh -c 'shellcheck \"$1\" | wc -l' -- {} \\;\nfind . -exec echo {} \\;"},"SC2068":{"code":"SC2068","title":"Double quote array expansions to avoid re-splitting elements.","fix":"Double quotes around `$@` (and similarly, `${array[@]}`) prevents globbing and word splitting of ind","rationale":"Double quotes around `$@` (and similarly, `${array[@]}`) prevents globbing and word splitting of individual elements, while still expanding to multiple separate arguments. Let's say you have four argu","problematicCode":"cp $@ ~/dir","correctCode":"cp \"$@\" ~/dir"},"SC2069":{"code":"SC2069","title":"To redirect stdout+stderr, `2>&1` must be last (or use `{ cmd > file; } 2>&1` to clarify).","fix":"When it comes to redirection, order matters. The problematic code means \"Point stderr to where stdou","rationale":"When it comes to redirection, order matters. The problematic code means \"Point stderr to where stdout is currently pointing (the terminal). Then point stdout to /dev/null\". The correct code means \"Poi","problematicCode":"firefox 2>&1 > /dev/null","correctCode":"firefox > /dev/null 2>&1"},"SC2070":{"code":"SC2070","title":"`-n` doesn't work with unquoted arguments. Quote or use `[[ ]]`.","fix":"When `$var` is unquoted, a blank value will cause it to wordsplit and disappear. If `$var` is empty,","rationale":"When `$var` is unquoted, a blank value will cause it to wordsplit and disappear. If `$var` is empty, these two statements are identical: ```sh [ -n $var ] [ -n ] ``` `[ string ]` is shorthand for test","problematicCode":"if [ -n $var ]\nthen\n  echo \"var has a value\"\nelse\n  echo \"var is empty\"\nfi","correctCode":"if [ -n \"$var\" ]\nthen\n  echo \"var has a value\"\nelse\n  echo \"var is empty\"\nfi"},"SC2071":{"code":"SC2071","title":"`>` is for string comparisons. Use `-gt` instead.","fix":"`<` and `>`, in both `[[` and `[` (when escaped) will do a lexicographical comparison, not a numeric","rationale":"`<` and `>`, in both `[[` and `[` (when escaped) will do a lexicographical comparison, not a numerical comparison. This means that `[[ 5 > 10 ]]` is true because 5 comes after 10 alphabetically. Meanw","problematicCode":"if [[ $var > 10 ]]\nthen\n  echo \"Incorrectly triggers when var=5\"\nfi","correctCode":"if [[ $var -gt 10 ]]\nthen\n  echo \"Correct numerical comparison\"\nfi"},"SC2072":{"code":"SC2072","title":"Decimals are not supported. Either use integers only, or use `bc` or `awk` to compare.","fix":"Bash and Posix sh does not support decimals in numbers. Decimals should either be avoided, or compar","rationale":"Bash and Posix sh does not support decimals in numbers. Decimals should either be avoided, or compared using a tool that does support them.","problematicCode":"[[ 2 -lt 3.14 ]]","correctCode":"[[ 200 -lt 314 ]]                   # Use fixed point math\n[[ $(echo \"2 < 3.14\" | bc) == 1 ]]  # Use bc"},"SC2073":{"code":"SC2073","title":"Escape `\\<` to prevent it redirecting (or switch to `[[ .. ]]`).","fix":"You are using the operator `<` or `>` in a `[` test expression. In this context, it will be consider","rationale":"You are using the operator `<` or `>` in a `[` test expression. In this context, it will be considered a file redirection operator instead, so `[ \"aardvark\" < \"zebra\" ]` is equivalent to `[ \"aardvark\"","problematicCode":"if [ \"aardvark\" < \"zebra\" ]\nthen\n  echo \"Alphabetical!\"\nfi","correctCode":"if [ \"aardvark\" \\< \"zebra\" ]\nthen\n  echo \"Alphabetical!\"\nfi"},"SC2074":{"code":"SC2074","title":"Can't use `=~` in `[ ]`. Use `[[..]]` instead.","fix":"`=~` only works in `[[ .. ]]` tests. It does not work with `test` or `[` in any shell. If you're tar","rationale":"`=~` only works in `[[ .. ]]` tests. It does not work with `test` or `[` in any shell. If you're targeting POSIX `sh`, rewrite in terms of `case` or `grep` instead.","problematicCode":"[ \"$input\" =~ DOC[0-9]*\\.txt ] && echo \"match\"","correctCode":"[[ \"$input\" =~ DOC[0-9]*\\.txt ]] && echo \"match\""},"SC2075":{"code":"SC2075","title":"Escaping `\\<` is required in `[..]`, but invalid in `[[..]]`","fix":"Grammatically speaking, `[` is considered a normal command name, so `<` and `>` are interpreted as r","rationale":"Grammatically speaking, `[` is considered a normal command name, so `<` and `>` are interpreted as redirections. When using the lexicographical string operators `<` and `>` in `[ .. ]`, they must be e","problematicCode":"[[ aardvark \\< zebra ]]","correctCode":"[[ aardvark < zebra ]]"},"SC2076":{"code":"SC2076","title":"Don't quote rhs of `=~`, it'll match literally rather than as a regex.","fix":"Quotes on the right hand side of `=~` can be used to match literally, so that `[[ $1 =~ ^\"$2\".* ]]` ","rationale":"Quotes on the right hand side of `=~` can be used to match literally, so that `[[ $1 =~ ^\"$2\".* ]]` works even if `$2` contains regex metacharacters. This mirrors the behavior of globs, `[[ $1 = \"$2\"*","problematicCode":"[[ $foo =~ \"^fo+ bar$\" ]]","correctCode":"[[ $foo =~ ^fo+\\ bar$ ]]"},"SC2077":{"code":"SC2077","title":"You need spaces around the comparison operator.","fix":"`[[ 0 = 1 ]]` means \"check if 0 and 1 are equal\". `[[ str ]]` is short form for `[[ -n str ]]`, and ","rationale":"`[[ 0 = 1 ]]` means \"check if 0 and 1 are equal\". `[[ str ]]` is short form for `[[ -n str ]]`, and means \"check if `str` is non-empty\". It doesn't matter if `str` happens to contain `0=1`. Always use","problematicCode":"[[ 0=1 ]]","correctCode":"[[ 0 = 1 ]]"},"SC2078":{"code":"SC2078","title":"This expression is constant. Did you forget a `$` somewhere?","fix":"ShellCheck has found a `[ .. ]` or `[[ .. ]]` statement that just contains a literal string. Such a ","rationale":"ShellCheck has found a `[ .. ]` or `[[ .. ]]` statement that just contains a literal string. Such a check does not do anything useful, and will always be true (or always false, for empty strings). Thi","problematicCode":"if [ \"myvar\" ]\nthen\n  echo \"myvar is set\"\nfi","correctCode":"if [ \"$myvar\" ]\nthen\n  echo \"myvar is set\"\nfi"},"SC2079":{"code":"SC2079","title":"`(( ))` doesn't support decimals. Use `bc` or `awk`.","fix":"`(( ))` doesn't support decimals. Use `bc` or `awk`.","rationale":"","problematicCode":"","correctCode":""},"SC2080":{"code":"SC2080","title":"Numbers with leading 0 are considered octal.","fix":"ShellCheck found an integer literal with a leading zero, but containing the digits 8 or 9. This is i","rationale":"ShellCheck found an integer literal with a leading zero, but containing the digits 8 or 9. This is invalid, as the integer will be interpreted as an octal value (e.g. 0777 == 0x1FF == 511). To have th","problematicCode":"echo $(( 16 - 08 ))","correctCode":"echo $(( 16 - 8 ))"},"SC2081":{"code":"SC2081","title":"`[ .. ]` can't match globs. Use `[[ .. ]]` or grep.","fix":"`[ .. ]` aka `test` can not match against globs. In bash/ksh, you can instead use `[[ .. ]]` which s","rationale":"`[ .. ]` aka `test` can not match against globs. In bash/ksh, you can instead use `[[ .. ]]` which supports this behavior. In sh, you can rewrite to use `grep`. ```sh if echo $var | grep -q '^[0-9]*$'","problematicCode":"if [ $var == *[^0-9]* ]\nthen\n  echo \"$var is not numeric\"\nfi","correctCode":"if [[ $var == *[^0-9]* ]]\nthen\n  echo \"$var is not numeric\"\nfi"},"SC2082":{"code":"SC2082","title":"To expand via indirection, use `name=\"foo$n\"; echo \"${!name}\"`.","fix":"You can expand a variable `var_1` with `${var_1}`, but you can not generate the string `var_1` with ","rationale":"You can expand a variable `var_1` with `${var_1}`, but you can not generate the string `var_1` with an embedded expansion, like `${var_$n}`. Instead, if at all possible, you should use an array. Bash ","problematicCode":"var_1=\"hello world\"\nn=1\necho \"${var_$n}\"","correctCode":"# Use arrays instead of dynamic names\ndeclare -a var\nvar[1]=\"hello world\"\nn=1\necho \"${var[n]}\""},"SC2083":{"code":"SC2083","title":"Don't add spaces after the slash in `./file`.","fix":"Contrary to popular belief, there is no command or syntax `./` that runs a file. `./myfile` is simpl","rationale":"Contrary to popular belief, there is no command or syntax `./` that runs a file. `./myfile` is simply the shortest path equivalent to `myfile` that specifies a directory and therefore causes a shell t","problematicCode":"gcc -o myfile file.c\n./ myfile","correctCode":"gcc -o myfile file.c\n./myfile"},"SC2084":{"code":"SC2084","title":"Remove `$` or use `_=$((expr))` to avoid executing output.","fix":"`$((..))` expands to a number. If it's the only word on the line, the shell will try to execute this","rationale":"`$((..))` expands to a number. If it's the only word on the line, the shell will try to execute this number as a command name: ```sh $ i=4 $ $(( i++ )) 4: command not found $ echo $i 5 ``` To avoid tr","problematicCode":"i=4\n$(( i++ ))","correctCode":"i=4\n(( i++ ))"},"SC2086":{"code":"SC2086","title":"Double quote to prevent globbing and word splitting.","fix":"IFS='' | \"$debug\" | $debug","rationale":"The first code looks like \"print the first argument\". It's actually \"Split the first argument by IFS (spaces, tabs and line feeds). Expand each of them as if it was a glob. Join all the resulting stri","problematicCode":"echo $1\nfor i in $*; do :; done # this one and the next one also apply to expanding arrays.\nfor i in $@; do :; done","correctCode":"echo \"$1\"\nfor i in \"$@\"; do :; done # or, 'for i; do'"},"SC2087":{"code":"SC2087","title":"Quote `EOF` to make here document expansions happen on the server side rather than on the client.","fix":"When the end token of a here document is unquoted, parameter expansion and command substitution will","rationale":"When the end token of a here document is unquoted, parameter expansion and command substitution will happen on in contents of the here doc. This means that before sending the commands to the server, t","problematicCode":"ssh host.example.com << EOF\n  echo \"Logged in on $HOSTNAME\"\nEOF","correctCode":"ssh host.example.com << \"EOF\"\n  echo \"Logged in on $HOSTNAME\"\nEOF"},"SC2088":{"code":"SC2088","title":"Tilde does not expand in quotes. Use `$HOME`.","fix":"Tilde does not expand to the user's home directory when it's single or double quoted. Use double quo","rationale":"Tilde does not expand to the user's home directory when it's single or double quoted. Use double quotes and `$HOME` instead. Alternatively, the `~/` can be left unquoted, as in `rm ~/\"Desktop/$filenam","problematicCode":"rm \"~/Desktop/$filename\"","correctCode":"rm \"$HOME/Desktop/$filename\""},"SC2089":{"code":"SC2089","title":"Quotes/backslashes will be treated literally. Use an array.","fix":"Bash does not interpret data as code. Consider almost any other languages, such as Python: ```python","rationale":"Bash does not interpret data as code. Consider almost any other languages, such as Python: ```python #!/usr/bin/env python3 print 1+1   # prints 2 a=\"1+1\" print a     # prints 1+1, not 2 ``` Here, `1+","problematicCode":"args='-lh \"My File.txt\"'\nls $args","correctCode":"args=(-lh \"My File.txt\")\nls \"${args[@]}\""},"SC2090":{"code":"SC2090","title":"Quotes/backslashes in this variable will not be respected.","fix":"Quotes/backslashes in this variable will not be respected.","rationale":"","problematicCode":"","correctCode":""},"SC2091":{"code":"SC2091","title":"Remove surrounding `$()` to avoid executing output (or use `eval` if intentional).","fix":"ShellCheck has detected that you have a command that just consists of a command substitution. This o","rationale":"ShellCheck has detected that you have a command that just consists of a command substitution. This often happens when you want to run a command (possibly from a variable name), without realizing that ","problematicCode":"if $(which epstopdf)\nthen\n  echo \"Found epstopdf\"\nfi","correctCode":"if which epstopdf\nthen\n  echo \"Found epstopdf\"\nfi"},"SC2092":{"code":"SC2092","title":"Remove backticks to avoid executing output.","fix":"Remove backticks to avoid executing output.","rationale":"","problematicCode":"","correctCode":""},"SC2093":{"code":"SC2093","title":"Remove `exec ` if script should continue after this command.","fix":"The script contains an `exec` command followed by other commands in the same block. This is likely a","rationale":"The script contains an `exec` command followed by other commands in the same block. This is likely an error because the script will not resume after an `exec` command. Instead, \"exec\" refers to the Un","problematicCode":"echo \"Starting compilation\"\nexec ./compile\necho \"Starting deployment\"\nexec ./deploy","correctCode":"echo \"Starting compilation\"\n./compile\necho \"Starting deployment\"\n./deploy"},"SC2094":{"code":"SC2094","title":"Make sure not to read and write the same file in the same pipeline.","fix":"Each step in a pipeline runs in parallel. In this case, `grep foo file.txt` will immediately try to ","rationale":"Each step in a pipeline runs in parallel. In this case, `grep foo file.txt` will immediately try to read `file.txt` while `sed .. > file.txt` will immediately try to truncate it. This is a race condit","problematicCode":"grep foo file.txt | sed -e 's/foo/bar/g' > file.txt","correctCode":"grep foo file.txt  | sed -e 's/foo/bar/g' > tmpfile && mv tmpfile file.txt"},"SC2095":{"code":"SC2095","title":"Use `ssh -n` to prevent ssh from swallowing stdin.","fix":"Commands that process stdin will compete with the `read` statement for input. This is especially tri","rationale":"Commands that process stdin will compete with the `read` statement for input. This is especially tricky for commands you wouldn't expect reads from stdin, like `ssh .. uptime`, `ffmpeg` and `mplayer`.","problematicCode":"while read -r host\ndo\n  ssh \"$host\" \"uptime\"\ndone < hosts.txt","correctCode":"while read -r host\ndo\n  ssh -n \"$host\" \"uptime\"\ndone < hosts.txt"},"SC2096":{"code":"SC2096","title":"On most OS, shebangs can only specify a single parameter.","fix":"Most operating systems, including POSIX, Linux and FreeBSD, allow only a single parameter in the she","rationale":"Most operating systems, including POSIX, Linux and FreeBSD, allow only a single parameter in the shebang. The example is equivalent to calling `env 'bash -x'` instead of `env 'bash' '-x'`, and it will","problematicCode":"#!/usr/bin/env bash -x","correctCode":"#!/usr/bin/env bash\nset -x"},"SC2097":{"code":"SC2097","title":"This assignment is only seen by the forked process.","fix":"In `name=World cmd \"$name\"`, `name=World` is passed in as part of the environment to `cmd` (i.e., in","rationale":"In `name=World cmd \"$name\"`, `name=World` is passed in as part of the environment to `cmd` (i.e., in the `envp` parameter to [execve(2)](http://linux.die.net/man/2/execve)).  This means that `cmd` and","problematicCode":"name=World cmd -m \"Hello $name\"","correctCode":"name=World\ncmd -m \"Hello $name\""},"SC2098":{"code":"SC2098","title":"This expansion will not see the mentioned assignment.","fix":"This expansion will not see the mentioned assignment.","rationale":"","problematicCode":"","correctCode":""},"SC2099":{"code":"SC2099","title":"Use `$((..))` for arithmetics, e.g. `i=$((i + 2))`","fix":"Unlike most languages, variable assignments in shell scripts are space sensitive and (almost) always","rationale":"Unlike most languages, variable assignments in shell scripts are space sensitive and (almost) always assign strings. To evaluate a mathematical expressions, use `$((..))` as in the correct code: i=$((","problematicCode":"i=3\ni=i + 2","correctCode":"i=3\ni=$((i + 2))"},"SC2100":{"code":"SC2100","title":"Use `$((..))` for arithmetics, e.g. `i=$((i + 2))`","fix":"Unlike most languages, variable assignments (almost) always assigns strings and not expressions. In ","rationale":"Unlike most languages, variable assignments (almost) always assigns strings and not expressions. In the example code, `i` will become the string `i+2` instead of the intended `5`. To instead evaluate ","problematicCode":"i=3\ni=i+2","correctCode":"i=3\ni=$((i + 2))"},"SC2101":{"code":"SC2101","title":"Named class needs outer `[]`, e.g. `[[:digit:]]`.","fix":"Predefined character groups are supposed to be used inside character ranges. `[:digit:]` matches one","rationale":"Predefined character groups are supposed to be used inside character ranges. `[:digit:]` matches one of \"digt:\" just like `[abc]` matches one of \"abc\". `[[:digit:]]` matches a digit.","problematicCode":"gzip file[:digit:]*.txt","correctCode":"gzip file[[:digit:]]*.txt"},"SC2102":{"code":"SC2102","title":"Ranges can only match single chars (mentioned due to duplicates).","fix":"ShellCheck found a glob range expression (such as `[a-z]`) that contains multiple of the same charac","rationale":"ShellCheck found a glob range expression (such as `[a-z]`) that contains multiple of the same character. Range expressions can only be used to match a single character in a given set, so `[ab]` and `[","problematicCode":"echo [100-999].txt","correctCode":"echo [1-9][0-9][0-9].txt"},"SC2103":{"code":"SC2103","title":"Use a `( subshell )` to avoid having to `cd` back.","fix":"When doing `cd dir; somestuff; cd ..`, `cd dir` can fail when permissions are lacking, if the dir wa","rationale":"When doing `cd dir; somestuff; cd ..`, `cd dir` can fail when permissions are lacking, if the dir was deleted, or if `dir` is actually a file. In this case, `somestuff` will run in the wrong directory","problematicCode":"for dir in */\ndo\n  cd \"$dir\"\n  convert index.png index.jpg\n  cd ..\ndone","correctCode":"for dir in */\ndo\n  (\n  cd \"$dir\" || exit\n  convert index.png index.jpg\n  )\ndone"},"SC2104":{"code":"SC2104","title":"In functions, use `return` instead of `break`.","fix":"`break` or `continue` are used to abort or continue a loop, and are not the right way to exit a func","rationale":"`break` or `continue` are used to abort or continue a loop, and are not the right way to exit a function. Use `return` instead.","problematicCode":"foo() {\n  if [[ -z $1 ]]\n  then\n    break\n  fi\n  echo \"Hello $1\"\n}","correctCode":"foo() {\n  if [[ -z $1 ]]\n  then\n    return 1\n  fi\n  echo \"Hello $1\"\n}"},"SC2105":{"code":"SC2105","title":"`break` is only valid in loops","fix":"`break` or `continue` was found outside a loop. These statements are valid only in loops. In particu","rationale":"`break` or `continue` was found outside a loop. These statements are valid only in loops. In particular, `break` is not required in `case` statements as there is no implicit fall-through. To return fr","problematicCode":"case \"$1\" in\n    -v)\n       verbose=1\n       break\n       ;;\n    -d)\n       debug=1\nesac","correctCode":"case \"$1\" in\n    -v)\n       verbose=1\n       ;;\n    -d)\n       debug=1\nesac"},"SC2106":{"code":"SC2106","title":"This only exits the subshell caused by the pipeline.","fix":"The most common cause of this issue is probably using a single `|` when `||` was intended.  The reas","rationale":"The most common cause of this issue is probably using a single `|` when `||` was intended.  The reason this message appears, though, is that a construction like this, intended to surface a failure ins","problematicCode":"for i in a b c; do\n  echo hi | grep -q bye | break\ndone","correctCode":"for i in a b c; do\n  echo hi | grep -q bye || break\ndone"},"SC2107":{"code":"SC2107","title":"Instead of `[ a && b ]`, use `[ a ] && [ b ]`.","fix":"`&&` can not be used in a `[ .. ]` test expression. Instead, make two `[ .. ]` expressions and put t","rationale":"`&&` can not be used in a `[ .. ]` test expression. Instead, make two `[ .. ]` expressions and put the `&&` between them.","problematicCode":"[ \"$1\" = \"-v\" && -z \"$2\" ]","correctCode":"[ \"$1\" = \"-v\" ] && [ -z \"$2\" ]"},"SC2108":{"code":"SC2108","title":"In `[[..]]`, use `&&` instead of `-a`.","fix":"`-a` for logical AND is not supported in a `[[ .. ]]` expression. Use `&&` instead.","rationale":"`-a` for logical AND is not supported in a `[[ .. ]]` expression. Use `&&` instead.","problematicCode":"[[ \"$1\" = \"-v\" -a -z \"$2\" ]]","correctCode":"[[ \"$1\" = \"-v\" && -z \"$2\" ]]"},"SC2109":{"code":"SC2109","title":"Instead of `[ a || b ]`, use `[ a ] || [ b ]`.","fix":"`||` cannot be used in a `[ .. ]` test expression. Instead, make two `[ .. ]` expressions and put th","rationale":"`||` cannot be used in a `[ .. ]` test expression. Instead, make two `[ .. ]` expressions and put the `||` between them.","problematicCode":"[ \"$1\" = \"-v\" || \"$1\" = \"-help\" ]","correctCode":"[ \"$1\" = \"-v\" ] || [ \"$1\" = \"-help\" ]"},"SC2110":{"code":"SC2110","title":"In `[[..]]`, use `||` instead of `-o`.","fix":"`-o` for logical OR is not supported in a `[[ .. ]]` expression. Use `||` instead.","rationale":"`-o` for logical OR is not supported in a `[[ .. ]]` expression. Use `||` instead.","problematicCode":"[[ \"$1\" = \"-v\" -o \"$1\" = \"-help\" ]]","correctCode":"[[ \"$1\" = \"-v\" || \"$1\" = \"-help\" ]]"},"SC2111":{"code":"SC2111","title":"ksh does not allow `function` keyword and `()` at the same time.","fix":"Ksh allows two ways of defining functions: POSIX sh style `foo() { ..; }` and Ksh specific `function","rationale":"Ksh allows two ways of defining functions: POSIX sh style `foo() { ..; }` and Ksh specific `function foo { ..; }`. ShellCheck found a function definition that uses both at the same time, `function foo","problematicCode":"#!/bin/ksh\nfunction foo() {\n  echo \"Hello World\"\n}","correctCode":"# POSIX sh function\nfoo() {\n  echo \"Hello World\"\n}"},"SC2112":{"code":"SC2112","title":"`function` keyword is non-standard. Delete it.","fix":"`function` is a non-standard keyword that can be used to declare functions in Bash and Ksh. In POSIX","rationale":"`function` is a non-standard keyword that can be used to declare functions in Bash and Ksh. In POSIX `sh` and `dash`, a function is instead declared without the `function` keyword as in the correct ex","problematicCode":"#!/bin/sh\nfunction hello() {\n  echo \"Hello World\"\n}","correctCode":"#!/bin/sh\nhello() {\n  echo \"Hello World\"\n}"},"SC2113":{"code":"SC2113","title":"`function` keyword is non-standard. Use `foo()` instead of `function foo`.","fix":"`function` is a non-standard keyword that can be used to declare functions in Bash and Ksh. In POSIX","rationale":"`function` is a non-standard keyword that can be used to declare functions in Bash and Ksh. In POSIX `sh` and `dash`, a function is defined without a `function` keyword. Instead, the function name is ","problematicCode":"#!/bin/sh\nfunction hello {\n    echo \"Hello World\"\n}","correctCode":"#!/bin/sh\nhello() {\n    echo \"Hello World\"\n}"},"SC2114":{"code":"SC2114","title":"Warning: deletes a system directory.","fix":"The example line of code was an actual bug in the [Bumblebee NVIDIA driver](https://github.com/MrMEE","rationale":"The example line of code was an actual bug in the [Bumblebee NVIDIA driver](https://github.com/MrMEEE/bumblebee-Old-and-abbandoned/commit/a047be85247755cdbe0acce6f1dafc8beb84f2ac). Due to an accidenta","problematicCode":"rm -rf /usr /lib/nvidia-current/xorg/xorg","correctCode":"rm -rf /usr/lib/nvidia-current/xorg/xorg"},"SC2115":{"code":"SC2115","title":"Use `\"${var:?}\"` to ensure this never expands to `/*` .","fix":"If `STEAMROOT` is empty, this will [end up deleting everything](https://github.com/ValveSoftware/ste","rationale":"If `STEAMROOT` is empty, this will [end up deleting everything](https://github.com/ValveSoftware/steam-for-linux/issues/3671) in the system's root directory. Using `:?` will cause the command to fail ","problematicCode":"rm -rf \"$STEAMROOT/\"*","correctCode":"rm -rf \"${STEAMROOT:?}/\"*"},"SC2116":{"code":"SC2116","title":"Useless echo? Instead of `cmd $(echo foo)`, just use `cmd foo`.","fix":"You appear to be using `echo` to write a value to stdout, and then using `$(..)` or `` `..` `` to ca","rationale":"You appear to be using `echo` to write a value to stdout, and then using `$(..)` or `` `..` `` to capture the value again. This is as pointless as mailing yourself a postcard: you already have what yo","problematicCode":"greeting=$(echo \"Hello, $name\")\n# or\ntar czf \"$(echo \"$(date +%F).tar.gz\")\" *","correctCode":"greeting=\"Hello, $name\"\n# or\ntar czf \"$(date +%F).tar.gz\" *"},"SC2117":{"code":"SC2117","title":"To run commands as another user, use `su -c` or `sudo`.","fix":"It's commonly believed that `su` makes a session run as another user. In reality, it starts an entir","rationale":"It's commonly believed that `su` makes a session run as another user. In reality, it starts an entirely new shell, independent of the one currently running your script. `su; whoami` will start a root ","problematicCode":"whoami\nsu\nwhoami","correctCode":"whoami\nsudo whoami"},"SC2118":{"code":"SC2118","title":"Ksh does not support `|&`. Use `2>&1 |`","fix":"You are using the Bash specific shorthand `|&`, but your script is running with Ksh. Rewrite it to i","rationale":"You are using the Bash specific shorthand `|&`, but your script is running with Ksh. Rewrite it to its full, POSIX-compatible form as shown in the example.","problematicCode":"#!/usr/bin/ksh\nmake |& tee ~/log","correctCode":"#!/usr/bin/ksh\nmake 2>&1 | tee ~/log"},"SC2119":{"code":"SC2119","title":"Use `foo \"$@\"` if function's `$1` should mean script's `$1`.","fix":"Use `foo \"$@\"` if function's `$1` should mean script's `$1`.","rationale":"","problematicCode":"","correctCode":""},"SC2120":{"code":"SC2120","title":"foo references arguments, but none are ever passed.","fix":"In a function, `$1` and up refers to the function's parameters, not the script's parameters. If you ","rationale":"In a function, `$1` and up refers to the function's parameters, not the script's parameters. If you want to process your script's parameters in a function, you have to explicitly pass them. You can do","problematicCode":"sayhello() {\n  echo \"Hello $1\"\n}\nsayhello","correctCode":"sayhello() {\n  echo \"Hello $1\"\n}\nsayhello \"$@\""},"SC2121":{"code":"SC2121","title":"To assign a variable, use just `var=value`, not `set ..`.","fix":"`set` is not used to set or assign variables in Bourne shells. It's used to set shell options and po","rationale":"`set` is not used to set or assign variables in Bourne shells. It's used to set shell options and positional parameters. To assign variables, use `var=value` with no `set` or other qualifiers.","problematicCode":"set var=42\nset var 42","correctCode":"var=42"},"SC2122":{"code":"SC2122","title":"`>=` is not a valid operator. Use `! a < b` instead.","fix":"The operators `<=` and `>=` are not supported by Bourne shells. Instead of \"less than or equal\", rew","rationale":"The operators `<=` and `>=` are not supported by Bourne shells. Instead of \"less than or equal\", rewrite as \"not greater than\".","problematicCode":"[[ a >= b ]]","correctCode":"[[ ! a < b ]]"},"SC2123":{"code":"SC2123","title":"`PATH` is the shell search path. Use another name.","fix":"`PATH` is where the shell looks for the commands it executes. By inadvertently overwriting it, the s","rationale":"`PATH` is where the shell looks for the commands it executes. By inadvertently overwriting it, the shell will be unable to find commands (like `cat` in this case). You get this warning when ShellCheck","problematicCode":"PATH=/my/dir\ncat \"$PATH/myfile\"","correctCode":"path=/my/dir\ncat \"$path/myfile\""},"SC2124":{"code":"SC2124","title":"Assigning an array to a string! Assign as array, or use `*` instead of `@` to concatenate.","fix":"Arrays and `$@` can contain multiple elements. Simple variables contain only one. When assigning mul","rationale":"Arrays and `$@` can contain multiple elements. Simple variables contain only one. When assigning multiple elements to one element, the default behavior depends on the shell (bash concatenates with spa","problematicCode":"# Want to store multiple elements in var\nvar=$@\nfor i in $var; do ..; done","correctCode":"# Bash: use an array variable\nvar=( \"$@\" )\nfor i in \"${var[@]}\"; do ..; done\n# POSIX sh: without array support, one possible workaround\n# is to store elements concatenated with a delimiter (here linef"},"SC2125":{"code":"SC2125","title":"Brace expansions and globs are literal in assignments. Quote it or use an array.","fix":"`echo *.png {1..9}` expands to all png files and numbers from 1 to 9, but `var=*.png` or `var={1..9}","rationale":"`echo *.png {1..9}` expands to all png files and numbers from 1 to 9, but `var=*.png` or `var={1..9}` will just assign the literal strings `'*.png'` and `'{1..9}'`. To make the variable contain all pn","problematicCode":"foo={1..9}\necho \"$foo\"","correctCode":"foo=( {1..9} )\necho \"${foo[@]}\""},"SC2126":{"code":"SC2126","title":"Consider using `grep -c` instead of `grep | wc`","fix":"This is purely a stylistic issue. `grep` can count lines without piping to `wc`. Often this number i","rationale":"This is purely a stylistic issue. `grep` can count lines without piping to `wc`. Often this number is only used to see whether there are matches (i.e. `== 0`). In these cases it's clearer and more eff","problematicCode":"grep foo | wc -l","correctCode":"grep -c foo"},"SC2127":{"code":"SC2127","title":"To use `${ ..; }`, specify `#!/usr/bin/env ksh`.","fix":"You are using a shell syntax feature not supported by the script's shell. Either rewrite the constru","rationale":"You are using a shell syntax feature not supported by the script's shell. Either rewrite the construct, or switch to a different shell interpreter.","problematicCode":"#!/usr/bin/env bash\nvar=${ mycmd; };","correctCode":"#!/usr/bin/env ksh\nvar=${ mycmd; };"},"SC2128":{"code":"SC2128","title":"Expanding an array without an index only gives the element in the index 0.","fix":"When referencing arrays, `$myarray` is equivalent to `${myarray[0]}` -- which is usually the first o","rationale":"When referencing arrays, `$myarray` is equivalent to `${myarray[0]}` -- which is usually the first of multiple elements. This is also true for associative arrays. Therefore, if 0 (zero) is not a valid","problematicCode":"myarray=(foo bar)\nfor f in $myarray\ndo\n  cat \"$f\"\ndone","correctCode":"myarray=(foo bar)\nfor f in \"${myarray[@]}\"\ndo\n  cat \"$f\"\ndone"},"SC2129":{"code":"SC2129","title":"Consider using `{ cmd1; cmd2; } >> file` instead of individual redirects.","fix":"Rather than adding `>> something` after every single line, you can simply group the relevant command","rationale":"Rather than adding `>> something` after every single line, you can simply group the relevant commands and redirect the group. So the file has to be opened and closed only once and it means a performan","problematicCode":"echo foo >> file\ndate >> file\ncat stuff  >> file","correctCode":"{\n  echo foo\n  date\n  cat stuff\n} >> file"},"SC2130":{"code":"SC2130","title":"`-eq` is for integer comparisons. Use `=` instead.","fix":"Shells have two sets of comparison operators: for integers (`-eq`, `-gt`, ...) and strings (`=`, `>`","rationale":"Shells have two sets of comparison operators: for integers (`-eq`, `-gt`, ...) and strings (`=`, `>`, ...). ShellCheck has noticed that you're using an integer comparison with string data. If you are ","problematicCode":"[[ $foo -eq \"Y\" ]]","correctCode":"[[ $foo = \"Y\" ]]"},"SC2139":{"code":"SC2139","title":"This expands when defined, not when used. Consider escaping.","fix":"With double quotes, this particular alias will be defined as `echo /home/me`, so it will always prin","rationale":"With double quotes, this particular alias will be defined as `echo /home/me`, so it will always print the same path. This is rarely intended. By using single quotes or escaping any expansions, we defi","problematicCode":"alias whereami=\"echo $PWD\"","correctCode":"alias whereami='echo $PWD'"},"SC2140":{"code":"SC2140","title":"Word is of the form `\"A\"B\"C\"` (B indicated). Did you mean `\"ABC\"` or `\"A\\\"B\\\"C\"`?","fix":"This warning triggers when an unquoted literal string is found suspiciously sandwiched between two d","rationale":"This warning triggers when an unquoted literal string is found suspiciously sandwiched between two double quoted strings. This usually indicates one of: - quotes that were supposed to be nested, and t","problematicCode":"echo \"<img src=\"foo.png\" />\" > file.html","correctCode":"echo \"<img src=\\\"foo.png\\\" />\" > file.html"},"SC2141":{"code":"SC2141","title":"Did you mean `IFS=$'\\t'` ?","fix":"`IFS=\"\\t\"` splits on backslash and the letter \"t\". `IFS=$'\\t'` splits on tab.","rationale":"`IFS=\"\\t\"` splits on backslash and the letter \"t\". `IFS=$'\\t'` splits on tab.","problematicCode":"IFS=\"\\t\"","correctCode":"IFS=$'\\t'"},"SC2142":{"code":"SC2142","title":"Aliases can't use positional parameters. Use a function.","fix":"Aliases just substitute the start of a command with something else. They therefore can't use positio","rationale":"Aliases just substitute the start of a command with something else. They therefore can't use positional parameters, such as `$1`. Rewrite your alias as a function.","problematicCode":"alias archive='mv \"$@\" /backup'","correctCode":"archive() { mv \"$@\" /backup; }"},"SC2143":{"code":"SC2143","title":"Use `grep -q` instead of comparing output with `[ -n .. ]`.","fix":"The problematic code has to iterate the entire directory and read all matching lines into memory bef","rationale":"The problematic code has to iterate the entire directory and read all matching lines into memory before making a decision. The correct code is cleaner and stops at the first matching line, avoiding bo","problematicCode":"if [ -n \"$(find . | grep 'IMG[0-9]')\" ]\nthen\n  echo \"Images found\"\nfi","correctCode":"if find . | grep -q 'IMG[0-9]'\nthen\n  echo \"Images found\"\nfi"},"SC2144":{"code":"SC2144","title":"`-e` doesn't work with globs. Use a `for` loop.","fix":"`[ -e file* ]` only works if there's 0 or 1 matches. If there are multiple, it becomes `[ -e file1 f","rationale":"`[ -e file* ]` only works if there's 0 or 1 matches. If there are multiple, it becomes `[ -e file1 file2 ]`, and the test fails. `[[ -e file* ]]` doesn't work at all. Instead, use a for loop to expand","problematicCode":"if [ -e dir/*.mp3 ]\nthen\n  echo \"There are mp3 files.\"\nfi","correctCode":"for file in dir/*.mp3\ndo\n  if [ -e \"$file\" ]\n  then\n    echo \"There are mp3 files\"\n    break\n  fi\ndone"},"SC2145":{"code":"SC2145","title":"Argument mixes string and array. Use `*` or separate argument.","fix":"The behavior when concatenating a string and array is rarely intended. The preceding string is prefi","rationale":"The behavior when concatenating a string and array is rarely intended. The preceding string is prefixed to the first array element, while the succeeding string is appended to the last one. The middle ","problematicCode":"printf \"Error: %s\\n\" \"Bad parameters: $@\"","correctCode":"printf \"Error: %s\\n\" \"Bad parameters: $*\""},"SC2146":{"code":"SC2146","title":"This action ignores everything before the `-o`. Use `\\( \\)` to group.","fix":"In `find`, two predicates with no operator between them is considered a logical, short-circuiting AN","rationale":"In `find`, two predicates with no operator between them is considered a logical, short-circuiting AND (as if using `-a`). E.g., `-name '*.mkv' -exec ..` is the same as `-name '*.mkv' -a -exec ..`. `-a","problematicCode":"find . -name '*.avi' -o -name '*.mkv' -exec cp {} /media \\;","correctCode":"find . \\( -name '*.avi' -o -name '*.mkv' \\) -exec cp {} /media \\;"},"SC2147":{"code":"SC2147","title":"Literal tilde in PATH works poorly across programs.","fix":"Having literal `~` in PATH is a bad idea. Bash handles it, but nothing else does. This means that ev","rationale":"Having literal `~` in PATH is a bad idea. Bash handles it, but nothing else does. This means that even if you're always using Bash, you should avoid it because any invoked program that relies on PATH ","problematicCode":"PATH=\"$PATH:~/bin\"","correctCode":"PATH=\"$PATH:$HOME/bin\""},"SC2148":{"code":"SC2148","title":"Tips depend on target shell and yours is unknown. Add a shebang.","fix":"Different shells support different features. To give effective advice, ShellCheck needs to know whic","rationale":"Different shells support different features. To give effective advice, ShellCheck needs to know which shell your script is going to run on. You will get a different numbers of warnings about different","problematicCode":"echo \"$RANDOM\"   # Does this work?","correctCode":"#!/bin/sh\necho \"$RANDOM\"  # Unsupported in sh. Produces warning."},"SC2149":{"code":"SC2149","title":"Remove `$`/`${}` for numeric index, or escape it for string.","fix":"For a numerically indexed array, the `$` is mostly pointless and can be removed like in [[SC2004]]. ","rationale":"For a numerically indexed array, the `$` is mostly pointless and can be removed like in [[SC2004]]. For associative arrays, the `$` should be escaped to avoid accidental dereferencing: ```bash declare","problematicCode":"# Regular array\nindex=42\necho $((array[$index]))","correctCode":"# Regular array\nindex=42\necho $((array[index]))"},"SC2150":{"code":"SC2150","title":"`-exec` does not automatically invoke a shell. Use `-exec sh -c ..` for that.","fix":"find `-exec` and `-execdir` uses `execve(2)` style semantics, meaning it expects an executable and z","rationale":"find `-exec` and `-execdir` uses `execve(2)` style semantics, meaning it expects an executable and zero or more arguments that should be passed to it. It does not use `system(3)` style semantics, mean","problematicCode":"find . -type f -exec 'cat {} | wc -l' \\;","correctCode":"find . -type f -exec sh -c 'cat {} | wc -l' \\;         # Insecure\nfind . -type f -exec sh -c 'cat \"$1\" | wc -l' _ {} \\;  # Secure"},"SC2151":{"code":"SC2151","title":"Only one integer 0-255 can be returned. Use stdout for other data.","fix":"In bash, `return` can only be used to signal success or failure (0 = success, 1-255 = failure). To r","rationale":"In bash, `return` can only be used to signal success or failure (0 = success, 1-255 = failure). To return textual or multiple values from a function, write them to stdout and capture them with command","problematicCode":"myfunc() {\n  return foo bar\n}","correctCode":"myfunc() {\n  echo foo\n  echo bar\n  return 0\n}"},"SC2152":{"code":"SC2152","title":"Can only return 0-255. Other data should be written to stdout.","fix":"In many languages, `return` is used to return from the function with a final result. In sh/bash, `re","rationale":"In many languages, `return` is used to return from the function with a final result. In sh/bash, `return` can only be used to signal success or failure (0 = success, 1-255 = failure), more akin to `th","problematicCode":"myfunc() {\n  return \"Hello $USER\"\n}","correctCode":"myfunc() {\n  echo \"Hello $USER\"\n  return 0\n}"},"SC2153":{"code":"SC2153","title":"Possible Misspelling: MYVARIABLE may not be assigned. Did you mean MY_VARIABLE?","fix":"ShellCheck has noticed that you reference a variable that is not assigned in the script, but which h","rationale":"ShellCheck has noticed that you reference a variable that is not assigned in the script, but which has a name similar to another known variable. You should verify that the variable name is spelled cor","problematicCode":"MY_VARIABLE=\"hello world\"\necho \"$MYVARIABLE\"","correctCode":"MY_VARIABLE=\"hello world\"\necho \"$MY_VARIABLE\""},"SC2154":{"code":"SC2154","title":"var is referenced but not assigned.","fix":"ShellCheck has noticed that you reference a variable that is not assigned. Double check that the var","rationale":"ShellCheck has noticed that you reference a variable that is not assigned. Double check that the variable is indeed assigned, and that the name is not misspelled. Note: This message only triggers for ","problematicCode":"var=name\nn=42\necho \"$var_$n.jpg\"   # overextended","correctCode":"var=name\nn=42\necho \"${var}_${n}.jpg\""},"SC2155":{"code":"SC2155","title":"Declare and assign separately to avoid masking return values.","fix":"In the original code, the return value of `mycmd` is ignored, and `export` will instead always retur","rationale":"In the original code, the return value of `mycmd` is ignored, and `export` will instead always return true. This may prevent conditionals, `set -e` and traps from working correctly. When first marked ","problematicCode":"export foo=\"$(mycmd)\"","correctCode":"foo=\"$(mycmd)\"\nexport foo"},"SC2156":{"code":"SC2156","title":"Injecting filenames is fragile and insecure. Use parameters.","fix":"In the problematic example, the filename is passed by injecting it into a shell string. Any shell me","rationale":"In the problematic example, the filename is passed by injecting it into a shell string. Any shell metacharacters in the filename will be interpreted as part of the script, and not as part of the filen","problematicCode":"find . -name '*.mp3' -exec sh -c 'i=\"{}\"; sox \"$i\" \"${i%.mp3}.wav\"' \\;","correctCode":"find . -name '*.mp3' -exec sh -c 'i=\"$1\"; sox \"$i\" \"${i%.mp3}.wav\"' shell {} \\;"},"SC2157":{"code":"SC2157","title":"Argument to implicit `-n` is always true due to literal strings.","fix":"Since `[ str ]` and `[ -n str ]` check that the string is non-empty, any literal characters in the s","rationale":"Since `[ str ]` and `[ -n str ]` check that the string is non-empty, any literal characters in the string -- including a space character like in the example -- will cause the test to always be true. E","problematicCode":"if [ \"$foo \" ]\nthen\n  echo \"this is always true because of the trailing space\"\nfi","correctCode":"if [ \"$foo\" ]\nthen\n  echo \"correctly checks value\"\nfi"},"SC2158":{"code":"SC2158","title":"`[ false ]` is true. Remove the brackets","fix":"`[ str ]` checks whether `str` is non-empty. It doesn't matter if `str` is `false`, it will still be","rationale":"`[ str ]` checks whether `str` is non-empty. It doesn't matter if `str` is `false`, it will still be evaluated for non-emptyness. Instead, use the command `false` which -- as the manual puts it -- doe","problematicCode":"if [ false ]\nthen\n  echo \"triggers anyways\"\nfi","correctCode":"if false\nthen\n  echo \"never triggers\"\nfi"},"SC2159":{"code":"SC2159","title":"`[ 0 ]` is true. Use `false` instead.","fix":"`[ str ]` checks whether `str` is non-empty. It doesn't matter if `str` is `0`, it will still be eva","rationale":"`[ str ]` checks whether `str` is non-empty. It doesn't matter if `str` is `0`, it will still be evaluated for non-emptyness. Instead, use the command `false` which -- as the manual puts it -- does no","problematicCode":"if [ 0 ]\nthen\n  echo \"always triggers\"\nfi","correctCode":"if false\nthen\n  echo \"never triggers\"\nfi"},"SC2160":{"code":"SC2160","title":"Instead of `[ true ]`, just use `true`.","fix":"This is a stylistic suggestion to use `true` instead of `[ true ]`. `[ true ]` seems to suggest that","rationale":"This is a stylistic suggestion to use `true` instead of `[ true ]`. `[ true ]` seems to suggest that the value \"true\" is somehow relevant to the statement. This is not the case, it doesn't matter. You","problematicCode":"if [ true ]\nthen\n  echo \"always triggers\"\nfi","correctCode":"if true\nthen\n  echo \"always triggers\"\nfi"},"SC2161":{"code":"SC2161","title":"Instead of `[ 1 ]`, use `true`.","fix":"This is a stylistic suggestion to use `true` instead of `[ 1 ]`. `[ 1 ]` seems to suggest that the v","rationale":"This is a stylistic suggestion to use `true` instead of `[ 1 ]`. `[ 1 ]` seems to suggest that the value \"1\" is somehow relevant to the statement. This is not the case: it doesn't matter. You can repl","problematicCode":"while [ 1 ]\ndo\n  echo \"infinite loop\"\ndone","correctCode":"while true\ndo\n  echo \"infinite loop\"\ndone"},"SC2162":{"code":"SC2162","title":"`read` without `-r` will mangle backslashes.","fix":"By default, `read` will interpret backslashes before spaces and line feeds (i.e. you can use backsla","rationale":"By default, `read` will interpret backslashes before spaces and line feeds (i.e. you can use backslashes in your string as an escape character). This is rarely expected or desired. Normally you just w","problematicCode":"echo \"Enter name:\"\nread name","correctCode":"echo \"Enter name:\"\nread -r name"},"SC2163":{"code":"SC2163","title":"This does not export `FOO`. Remove `$`/`${}` for that, or use `${var?}` to quiet.","fix":"`export` takes a variable name, but shellcheck has noticed that you give it an expanded variable ins","rationale":"`export` takes a variable name, but shellcheck has noticed that you give it an expanded variable instead. The problematic code does not export `MYVAR` but a variable called `foo` if any.","problematicCode":"MYVAR=foo\nexport $MYVAR","correctCode":"MYVAR=foo\nexport MYVAR"},"SC2164":{"code":"SC2164","title":"Use `cd ... || exit` in case `cd` fails.","fix":"`cd` can fail for a variety of reasons: misspelled paths, missing directories, missing permissions, ","rationale":"`cd` can fail for a variety of reasons: misspelled paths, missing directories, missing permissions, broken symlinks and more. If/when it does, the script will keep going and do all its operations in t","problematicCode":"cd generated_files\nrm -r *.c","correctCode":"cd generated_files || exit\nrm -r *.c"},"SC2165":{"code":"SC2165","title":"This nested loop overrides the index variable of its parent.","fix":"When nesting loops, especially arithmetic for loops, using the same loop variable can cause unexpect","rationale":"When nesting loops, especially arithmetic for loops, using the same loop variable can cause unexpected results. In the problematic code, `i` will contain the last filename from the inner loop, which w","problematicCode":"for((i=0; i<10; i++))\ndo\n  for i in *\n  do\n    echo \"$i\"\n  done\ndone","correctCode":"for((i=0; i<10; i++))\ndo\n  for j in *\n  do\n    echo \"$j\"\n  done\ndone"},"SC2166":{"code":"SC2166","title":"Prefer `[ p ] && [ q ]` as `[ p -a q ]` is not well-defined.","fix":"`-a` and `-o` to mean AND and OR in a `[ .. ]` test expression is not well defined, and can cause in","rationale":"`-a` and `-o` to mean AND and OR in a `[ .. ]` test expression is not well defined, and can cause incorrect results when arguments start with dashes or contain `!`. [From POSIX](http://pubs.opengroup.","problematicCode":"[ \"$1\" = \"test\" -a -z \"$2\" ]","correctCode":"[ \"$1\" = \"test\" ] && [ -z \"$2\" ]"},"SC2167":{"code":"SC2167","title":"This parent loop has its index variable overridden.","fix":"This parent loop has its index variable overridden.","rationale":"","problematicCode":"","correctCode":""},"SC2168":{"code":"SC2168","title":"`local` is only valid in functions.","fix":"In Bash, `local` can only be used in functions. In other contexts, it's an error.","rationale":"In Bash, `local` can only be used in functions. In other contexts, it's an error.","problematicCode":"local foo=bar\necho \"$foo\"","correctCode":"foo=bar\necho \"$foo\""},"SC2169":{"code":"SC2169","title":"In dash, *something* is not supported.","fix":"In dash, *something* is not supported.","rationale":"","problematicCode":"","correctCode":""},"SC2170":{"code":"SC2170","title":"Invalid number for `-eq`. Use `=` to compare as string (or use `$var` to expand as a variable).","fix":"You are comparing a string value with a numerical operator, such as `-eq`, `-ne`, `-lt` or `-gt`. Th","rationale":"You are comparing a string value with a numerical operator, such as `-eq`, `-ne`, `-lt` or `-gt`. These only work for numbers. If you want to compare the value as a string, switch to the equivalent st","problematicCode":"read -r n\nif [ n -lt 0 ]\nthen\n   echo \"bad input\"\nfi\nif [ \"$USER\" -eq root ]\nthen\n  echo \"You are root\"\nfi","correctCode":"read -r n\nif [ \"$n\" -lt 0 ]        # Numerical comparison\nthen\n   echo \"bad input\"\nfi\nif [ \"$USER\" = root ]    # String comparison\nthen\n  echo \"You are root\"\nfi"},"SC2171":{"code":"SC2171","title":"Found trailing `]` outside test. Add missing `[` or quote if intentional.","fix":"ShellCheck found a non-test command that ends with `]` or `]]`. If this was intended to be a test ex","rationale":"ShellCheck found a non-test command that ends with `]` or `]]`. If this was intended to be a test expression like in the first example, add the missing `[` or `[[`. If the `]` was intended to be liter","problematicCode":"if foo -eq bar ]; then true; fi","correctCode":"if [ foo -eq bar ]; then true; fi"},"SC2172":{"code":"SC2172","title":"Trapping signals by number is not well-defined. Prefer signal names.","fix":"Signal numbers can vary between platforms. Prefer signal names, which are fixed. Signal numbers 1, 2","rationale":"Signal numbers can vary between platforms. Prefer signal names, which are fixed. Signal numbers 1, 2, 3, 6, 9, 14 and 15 are specified as parts of the optional POSIX XSI and ShellCheck will not warn a","problematicCode":"trap myfunc 28","correctCode":"trap myfunc WINCH"},"SC2173":{"code":"SC2173","title":"SIGKILL/SIGSTOP can not be trapped.","fix":"SIGKILL and SIGSTOP can not be caught/ignored (according to POSIX and as implemented on platforms in","rationale":"SIGKILL and SIGSTOP can not be caught/ignored (according to POSIX and as implemented on platforms including Linux and FreeBSD). Trying to trap this signal has undefined results.","problematicCode":"trap 'echo \"unkillable\"' KILL","correctCode":""},"SC2174":{"code":"SC2174","title":"When used with `-p`, `-m` only applies to the deepest directory.","fix":"When using `-m 0755`, the mode of the directory created will be set to 0755. When using `-p`, parent","rationale":"When using `-m 0755`, the mode of the directory created will be set to 0755. When using `-p`, parent directories which do not exist will be created, but the mode specified by `-m` will only be used on","problematicCode":"mkdir -p -m 0755 foo/bar/baz","correctCode":"mkdir -p foo/bar/baz\nchmod 0755 foo/bar/baz foo/bar foo"},"SC2175":{"code":"SC2175","title":"Quote this invalid brace expansion since it should be passed literally to eval","fix":"Using `eval somecommand {1..$n}` depends both on bash silently failing to interpret the brace expans","rationale":"Using `eval somecommand {1..$n}` depends both on bash silently failing to interpret the brace expansion, and on it passing failing brace expansions literally. Rather than depending on these questionab","problematicCode":"eval echo {1..$n}","correctCode":"eval \"echo {1..$n}\""},"SC2176":{"code":"SC2176","title":"`time` is undefined for pipelines. time single stage or `bash -c` instead.","fix":"This behavior is explicitly left undefined [in POSIX](http://pubs.opengroup.org/onlinepubs/969991979","rationale":"This behavior is explicitly left undefined [in POSIX](http://pubs.opengroup.org/onlinepubs/9699919799/utilities/time.html).","problematicCode":"time foo | bar","correctCode":"foo | { time bar; }"},"SC2177":{"code":"SC2177","title":"`time` is undefined for compound commands, use `time sh -c` instead.","fix":"`time` is only defined for Simple Commands [by POSIX](http://pubs.opengroup.org/onlinepubs/969991979","rationale":"`time` is only defined for Simple Commands [by POSIX](http://pubs.opengroup.org/onlinepubs/9699919799/utilities/time.html). Timing loops, command groups and similar is not.","problematicCode":"time for i in *.bmp; do convert \"$i\" \"$i.png\"; done","correctCode":"time sh -c 'for i in *.bmp; do convert \"$i\" \"$i.png\"; done'"},"SC2178":{"code":"SC2178","title":"Variable was used as an array but is now assigned a string.","fix":"ShellCheck noticed that you have used a variable as an array, but then assign it a string. `array=fo","rationale":"ShellCheck noticed that you have used a variable as an array, but then assign it a string. `array=foo` is equivalent to `array[0]=foo`, and leaves the rest of the elements unaffected. In the incorrect","problematicCode":"flags[0]=\"-r\"\nflags[1]=\"--delete-after\"\nif [ \"$dryrun\" ]\nthen\n   flags=\"--dry-run\"\nfi","correctCode":"flags[0]=\"-r\"\nflags[1]=\"--delete-after\"\nif [ \"$dryrun\" ]\nthen\n   flags=( \"--dry-run\" )\nfi"},"SC2179":{"code":"SC2179","title":"Use `array+=(\"item\")` to append items to an array.","fix":"It looks like you are trying to append a string to an array with `var+=string`. This instead appends","rationale":"It looks like you are trying to append a string to an array with `var+=string`. This instead appends to the first element of the array (equivalent to `var[0]+=three`). In the problematic code, the arr","problematicCode":"var=(one two)\nvar+=three","correctCode":"var=(one two)\nvar+=( three )"},"SC2180":{"code":"SC2180","title":"Bash does not support multidimensional arrays. Use 1D or associative arrays.","fix":"Bash does not support multidimensional arrays. Rewrite it to use 1D arrays. Associative arrays map a","rationale":"Bash does not support multidimensional arrays. Rewrite it to use 1D arrays. Associative arrays map arbitrary strings to values, and are therefore useful since you can construct keys like `\"1,2,3\"` or ","problematicCode":"foo[1][2]=bar\necho \"${foo[1][2]}\"","correctCode":"declare -A foo\nfoo[1,2]=bar\necho \"${foo[1,2]}\""},"SC2181":{"code":"SC2181","title":"Check exit code directly with e.g. `if mycmd;`, not indirectly with `$?`.","fix":"Running a command and then checking its exit status `$?` against 0 is redundant. Instead of just che","rationale":"Running a command and then checking its exit status `$?` against 0 is redundant. Instead of just checking the exit code of a command, it checks the exit code of a command (e.g. `[`) that checks the ex","problematicCode":"make mytarget\nif [ $? -ne 0 ]\nthen\n  echo \"Build failed\"\nfi","correctCode":"if ! make mytarget\nthen\n  echo \"Build failed\"\nfi"},"SC2182":{"code":"SC2182","title":"This printf format string has no variables. Other arguments are ignored.","fix":"ShellCheck has noticed that you're using a `printf` with multiple arguments, but where the first arg","rationale":"ShellCheck has noticed that you're using a `printf` with multiple arguments, but where the first argument has no `%s` or equivalent variable placeholders. `echo` accepts zero or more strings to write,","problematicCode":"place=\"world\"\nprintf hello $place","correctCode":"place=\"world\"\nprintf \"hello %s\\n\" \"$place\""},"SC2183":{"code":"SC2183","title":"This format string has 2 variables, but is passed 1 argument.","fix":"ShellCheck has noticed that you're using a `printf` format string with more `%s` variables than argu","rationale":"ShellCheck has noticed that you're using a `printf` format string with more `%s` variables than arguments to fill them. In the problematic example case, the last `%s` will just become an empty string ","problematicCode":"printf \"Hello %s, welcome to %s.\\n\" \"$USER\"","correctCode":"printf \"Hello %s, welcome to %s.\\n\" \"$USER\" \"$HOSTNAME\""},"SC2184":{"code":"SC2184","title":"Quote arguments to unset so they're not glob expanded.","fix":"Arguments to `unset` are subject to regular glob expansion. This is especially relevant when unsetti","rationale":"Arguments to `unset` are subject to regular glob expansion. This is especially relevant when unsetting indices in arrays, where `[..]` is considered a glob character group. In the problematic code, ha","problematicCode":"unset foo[index]","correctCode":"unset 'foo[index]'"},"SC2185":{"code":"SC2185","title":"Some finds don't have a default path. Specify `.` explicitly.","fix":"When not provided a search path, GNU and Busybox `find` will use a default path of `.`, the current ","rationale":"When not provided a search path, GNU and Busybox `find` will use a default path of `.`, the current directory. On POSIX, macOS/OSX, FreeBSD, OpenBSD and NetBSD, it will instead result in an error. Exp","problematicCode":"find -type f","correctCode":"find . -type f"},"SC2186":{"code":"SC2186","title":"tempfile is deprecated. Use mktemp instead.","fix":"`tempfile` is a Debian specific utility for creating temporary files. Its man page notes: >tempfile ","rationale":"`tempfile` is a Debian specific utility for creating temporary files. Its man page notes: >tempfile is deprecated; you should use mktemp(1) instead. Neither `tempfile` nor `mktemp` are POSIX, but `tem","problematicCode":"tmp=$(tempfile)","correctCode":"tmp=$(mktemp)"},"SC2187":{"code":"SC2187","title":"Ash scripts will be checked as Dash. Add `# shellcheck shell=dash` to silence.","fix":"ShellCheck has no first class support for `ash`, but it does support its Debian fork `dash` and defa","rationale":"ShellCheck has no first class support for `ash`, but it does support its Debian fork `dash` and defaults to this whenever `ash` is specified. Unfortunately, while the two are similar, they are not com","problematicCode":"#!/bin/ash\necho \"Hello World\"","correctCode":"#!/bin/ash\n# shellcheck shell=dash\necho \"Hello World\""},"SC2188":{"code":"SC2188","title":"This redirection doesn't have a command. Move to its command (or use `true` as no-op).","fix":"ShellCheck found a redirection that doesn't actually redirect from/to anything. This could indicate ","rationale":"ShellCheck found a redirection that doesn't actually redirect from/to anything. This could indicate a bug, such as in the problematic code where an additional linefeed causes `report.txt` to be trunca","problematicCode":"{ \n  echo \"Report for $(date +%F)\"\n  uptime\n  df -h\n}\n  > report.txt","correctCode":"{ \n  echo \"Report for $(date +%F)\"\n  uptime\n  df -h\n} > report.txt"},"SC2189":{"code":"SC2189","title":"You can't have `|` between this redirection and the command it should apply to.","fix":"ShellCheck has found a stage in a pipeline that consists of a redirection but no command. This doesn","rationale":"ShellCheck has found a stage in a pipeline that consists of a redirection but no command. This doesn't make sense because a redirection without a command will not read or write any data. This is most ","problematicCode":"< file.txt | grep foo","correctCode":"< file.txt grep foo   # or more canonically: grep foo < file.txt"},"SC2190":{"code":"SC2190","title":"Elements in associative arrays need index, e.g. `array=( [index]=value )` .","fix":"You appear to be initializing or appending an array element to an associative array without giving i","rationale":"You appear to be initializing or appending an array element to an associative array without giving it an index. In an indexed array, elements will be auto-indexed by incremented characters. In associa","problematicCode":"declare -A foo\nfoo=( myvalue )","correctCode":"declare -A foo\nfoo=( [key]=myvalue )"},"SC2191":{"code":"SC2191","title":"The `=` here is literal. To assign by index, use `( [index]=value )` with no spaces. To keep as literal, quote it.","fix":"The shell doesn't care about the `=` sign in your array assignment because it's not part of a recogn","rationale":"The shell doesn't care about the `=` sign in your array assignment because it's not part of a recognized index assignment. Instead, it's considered a literal character and becomes part of an array ele","problematicCode":"array=( [index] = value )","correctCode":"array=( [index]=value )"},"SC2192":{"code":"SC2192","title":"This array element has no value. Remove spaces after `=` or use `\"\"` for empty string.","fix":"You have an array element on the form `[index]=`. The shell will interpret this as an independent el","rationale":"You have an array element on the form `[index]=`. The shell will interpret this as an independent element with index `index` and value `<empty string>`. This may happen as part of the expression `[ind","problematicCode":"array=([1]=one [2]= two)","correctCode":"array=([1]=one [2]=two)"},"SC2193":{"code":"SC2193","title":"The arguments to this comparison can never be equal. Make sure your syntax is correct.","fix":"ShellCheck has determined that the two values you're comparing can never be equal. Most of the time,","rationale":"ShellCheck has determined that the two values you're comparing can never be equal. Most of the time, this happens because of a syntax issue that introduced unintended literal characters into one of th","problematicCode":"[ $var+1 == 5 ]              # Unevaluated math\n[ \"{$var}\" == \"value\" ]      # Swapped around $ and {\n[ \"$(cmd1) | cmd2\" == \"42\" ] # Ended with ) too soon\n[[ \"$var \" == *.png ]]       # Trailing space","correctCode":"[ $((var+1)) == 5 ]          # Evaluated math\n[ \"${var}\" == \"value\" ]      # Correct variable expansion\n[ \"$(cmd1 | cmd2)\" == \"42\" ] # Correct command substitution\n[[ \"$var\" == *.png ]]        # No tr"},"SC2194":{"code":"SC2194","title":"This word is constant. Did you forget the `$` on a variable?","fix":"You are using a `case` statement to compare a literal word. You most likely wanted to treat this wor","rationale":"You are using a `case` statement to compare a literal word. You most likely wanted to treat this word as a `$variable` or `$(command)` instead.","problematicCode":"case foo in\n  bar) echo \"Match\"\nesac","correctCode":"case $foo in\n  bar) echo \"Match\"\nesac"},"SC2195":{"code":"SC2195","title":"This pattern will never match the case statement's word. Double check them.","fix":"ShellCheck has detected that one of the patterns in a `case` statement will never match. Often, this","rationale":"ShellCheck has detected that one of the patterns in a `case` statement will never match. Often, this is due to mistakes in the case statement word that results in unintended literal characters. In the","problematicCode":"case \"$var \" in      # Trailing space\n  value) echo \"Match\"\nesac","correctCode":"case \"${var}\" in     # No trailing space\n  value) echo \"Match\"\nesac"},"SC2196":{"code":"SC2196","title":"`egrep` is non-standard and deprecated. Use `grep -E` instead.","fix":"`egrep` is a non-standard command. Its functionality is provided in POSIX by `grep -E`. [POSIX grep]","rationale":"`egrep` is a non-standard command. Its functionality is provided in POSIX by `grep -E`. [POSIX grep](http://pubs.opengroup.org/onlinepubs/9699919799/utilities/grep.html) says: >This grep has been enha","problematicCode":"egrep 'foo|bar' file","correctCode":"grep -E 'foo|bar' file"},"SC2197":{"code":"SC2197","title":"`fgrep` is non-standard and deprecated. Use `grep -F` instead.","fix":"`fgrep` is a non-standard command. Its functionality is provided in POSIX by `grep -F`. [POSIX grep]","rationale":"`fgrep` is a non-standard command. Its functionality is provided in POSIX by `grep -F`. [POSIX grep](http://pubs.opengroup.org/onlinepubs/9699919799/utilities/grep.html) says: >This grep has been enha","problematicCode":"fgrep '*.*' file","correctCode":"grep -F '*.*' file"},"SC2198":{"code":"SC2198","title":"Arrays don't work as operands in `[ ]`. Use a loop (or concatenate with `*` instead of `@`).","fix":"Array expansions become a series of words in `[ .. ]`. Operators expect single words only. The probl","rationale":"Array expansions become a series of words in `[ .. ]`. Operators expect single words only. The problematic code is equivalent to `[ \"$ext\" = jpg bmp png ]`, which is invalid syntax. A typical error me","problematicCode":"ext=png\nallowedExt=(jpg bmp png)\n[ \"$ext\" = \"${allowedExt[@]}\" ] && echo \"Extension is valid\"","correctCode":"ext=png\nallowedExt=(jpg bmp png)\nfor value in \"${allowedExt[@]}\"\ndo\n  [ \"$ext\" = \"$value\" ] && echo \"Extension is valid\"\ndone"},"SC2199":{"code":"SC2199","title":"Arrays implicitly concatenate in `[[ ]]`. Use a loop (or explicit `*` instead of `@`).","fix":"Array expansions in `[[ .. ]]` will implicitly concatenate into a single string, much like in assign","rationale":"Array expansions in `[[ .. ]]` will implicitly concatenate into a single string, much like in assignments. The problematic code is equivalent to `[ \"$ext\" = \"jpg bmp png\" ]`. Instead, use a `for` loop","problematicCode":"ext=png\nallowedExt=(jpg bmp png)\n[[ \"$ext\" = \"${allowedExt[@]}\" ]] && echo \"Extension is valid\"","correctCode":"ext=png\nallowedExt=(jpg bmp png)\nfor value in \"${allowedExt[@]}\"\ndo\n  [[ \"$ext\" = \"$value\" ]] && echo \"Extension is valid\"\ndone"},"SC2200":{"code":"SC2200","title":"Brace expansions don't work as operands in `[ ]`. Use a loop.","fix":"Brace expansions in `[ ]` will expand to a sequence of words. Operators work on single words. The pr","rationale":"Brace expansions in `[ ]` will expand to a sequence of words. Operators work on single words. The problematic code is equivalent to `[ \"$file\" = index.htm index.html index.php ]`, which is invalid syn","problematicCode":"[ \"$file\" = index.{htm,html,php} ] && echo \"This is the main file\"","correctCode":"for main in index.{htm,html,php}\ndo\n  [ \"$file\" = \"$main\" ] && echo \"This is the main file\"\ndone"},"SC2201":{"code":"SC2201","title":"Brace expansion doesn't happen in `[[ ]]`. Use a loop.","fix":"Brace expansions doesn't happen in `[[ ]]`. They will just be interpreted literally. Instead, use a ","rationale":"Brace expansions doesn't happen in `[[ ]]`. They will just be interpreted literally. Instead, use a `for` loop to iterate over values, and apply your condition to each.","problematicCode":"[[ \"$file\" = index.{htm,html,php} ]] && echo \"This is the main file\"","correctCode":"for main in index.{htm,html,php}\ndo\n  [[ \"$file\" = \"$main\" ]] && echo \"This is the main file\"\ndone"},"SC2202":{"code":"SC2202","title":"Globs don't work as operands in `[ ]`. Use a loop.","fix":"Globs in `[ ]` will expand to a sequence of words, one per matching filename. Meanwhile, operators w","rationale":"Globs in `[ ]` will expand to a sequence of words, one per matching filename. Meanwhile, operators work on single words. The problematic code is equivalent to `[ current.log -nt backup/file1.log backu","problematicCode":"[ current.log -nt backup/*.log ] && echo \"This is the latest file\"","correctCode":"newerThanAll=true\nfor log in backup/*.log\ndo\n  [ current.log -nt \"$log\" ] || newerThanAll=false\ndone\n[ \"$newerThanAll\" = \"true\" ] && echo \"This is the latest file\""},"SC2203":{"code":"SC2203","title":"Globs are ignored in `[[ ]]` except right of `=`/`!=`. Use a loop.","fix":"Globs in `[[ ]]` will not filename expand, and will be treated literally (or as patterns on the righ","rationale":"Globs in `[[ ]]` will not filename expand, and will be treated literally (or as patterns on the right-hand side of `=`, `==` and `!=`). The problematic code is equivalent to `[[ current.log -nt 'backu","problematicCode":"[[ current.log -nt backup/*.log ]] && echo \"This is the latest file\"","correctCode":"newerThanAll=true\nfor log in backup/*.log\ndo\n  [[ current.log -nt \"$log\" ]] || newerThanAll=false\ndone\n[[ \"$newerThanAll\" = \"true\" ]] && echo \"This is the latest file\""},"SC2204":{"code":"SC2204","title":"`(..)` is a subshell. Did you mean `[ .. ]`, a test expression?","fix":"Tests like `-d` to see if something is a directory or `-z` to see if it's non-empty are actually fla","rationale":"Tests like `-d` to see if something is a directory or `-z` to see if it's non-empty are actually flags to the `test` command, and only work as tests in that context. `[` is an alias for `test`, so you","problematicCode":"if ( -d mydir )\nthen\n  echo \"It's a directory\"\nfi","correctCode":"if [ -d mydir ]\nthen\n  echo \"It's a directory\"\nfi"},"SC2205":{"code":"SC2205","title":"`(..)` is a subshell. Did you mean `[ .. ]`, a test expression?","fix":"Tests like `-eq` to check numeric equality or `\\<` for string comparison only work are actually para","rationale":"Tests like `-eq` to check numeric equality or `\\<` for string comparison only work are actually parameters to the `test` command, and only work as tests in that context. `[` is an alias for `test`, so","problematicCode":"if ( 1 -lt 2 )\nthen\n  echo \"1 is less than 2\"\nfi","correctCode":"if [ 1 -lt 2 ]\nthen\n  echo \"1 is less than 2\"\nfi"},"SC2206":{"code":"SC2206","title":"Quote to prevent word splitting/globbing, or split robustly with mapfile or `read -a`.","fix":"You are expanding a variable unquoted in an array. This will invoke the shell's sloppy word splittin","rationale":"You are expanding a variable unquoted in an array. This will invoke the shell's sloppy word splitting and glob expansion. Instead, prefer explicitly splitting (or not splitting): * If the variable sho","problematicCode":"array=( $var )","correctCode":"array=( \"$var\" )"},"SC2207":{"code":"SC2207","title":"Prefer `mapfile` or `read -a` to split command output (or quote to avoid splitting).","fix":"You are doing unquoted command expansion in an array. This will invoke the shell's sloppy word split","rationale":"You are doing unquoted command expansion in an array. This will invoke the shell's sloppy word splitting and glob expansion. Instead, prefer explicitly splitting (or not splitting): * If you want to s","problematicCode":"array=( $(mycommand) )","correctCode":"# For bash 4.4+, must not be in posix mode, may use temporary files\nmapfile -t array < <(mycommand)\n# For bash 3.x+, must not be in posix mode, may use temporary files\narray=()\nwhile IFS='' read -r li"},"SC2208":{"code":"SC2208","title":"Use `[[ ]]` or quote arguments to `-v` to avoid glob expansion.","fix":"With `[`, arguments will undergo glob expansion. If a file `foo0` exists when the problematic code i","rationale":"With `[`, arguments will undergo glob expansion. If a file `foo0` exists when the problematic code is run, it will check for the variable `foo0` instead of the array entry `foo[0]`. If there additiona","problematicCode":"[ -v foo[0] ]","correctCode":"[ -v 'foo[0]' ]"},"SC2209":{"code":"SC2209","title":"Use `var=$(command)` to assign output (or quote to assign string).","fix":"Putting `var=` in front of a command will not assign its output. Use `var=$(my command here)` to exe","rationale":"Putting `var=` in front of a command will not assign its output. Use `var=$(my command here)` to execute the command and capture its output. If you do want to assign a literal string, use quotes to ma","problematicCode":"user=whoami         # Want to run whoami and assign output\nPAGER=cat git log   # Want to assign the string \"cat\"","correctCode":"user=$(whoami)\nPAGER=\"cat\" git log"},"SC2210":{"code":"SC2210","title":"This is a file redirection. Was it supposed to be a comparison or fd operation?","fix":"You are redirecting to or from a filename that is an integer. For example, `ls > file` where `file` ","rationale":"You are redirecting to or from a filename that is an integer. For example, `ls > file` where `file` happens to be `3`. This is not likely to be intentional. The most common causes are: 1. Trying to co","problematicCode":"if x > 5; then echo \"true\"; fi","correctCode":"if (( x > 5 )); then echo \"true\"; fi"},"SC2211":{"code":"SC2211","title":"This is a glob used as a command name. Was it supposed to be in `${..}`, array, or is it missing quoting?","fix":"You are using a glob as a command name. This is usually a mistake caused by one of the following: * ","rationale":"You are using a glob as a command name. This is usually a mistake caused by one of the following: * Trying to use `` `*foo*` `` or `$(*foo*)` to expand a glob. * Using `var=$(*.txt)` instead of `var=(","problematicCode":"for f in $(*.png); do echo \"$f\"; done   # Trying to loop over a glob\narray=$(*.txt)                          # Trying to assign an array\necho \"$(array[1])\"                      # Trying to expand an a","correctCode":"for f in *.png; do echo \"$f\"; done\narray=(*.txt)\necho \"${array[1]}\""},"SC2212":{"code":"SC2212","title":"Use `false` instead of empty `[`/`[[` conditionals.","fix":"`[ ]` is a somewhat obscure way of expressing falsehood, and the behavior is likely intended to allo","rationale":"`[ ]` is a somewhat obscure way of expressing falsehood, and the behavior is likely intended to allow the incorrectly quoted command `[ $var ]` to still work when the variable is unset. POSIX has a mo","problematicCode":"if [ ]\nthen\n  echo \"Temporarily disabled\"\nfi","correctCode":"if false\nthen\n  echo \"Temporarily disabled\"\nfi"},"SC2213":{"code":"SC2213","title":"getopts specified `-n`, but it's not handled by this `case`.","fix":"You have a `while getopts` loop where the corresponding `case` statement fails to handle one of the ","rationale":"You have a `while getopts` loop where the corresponding `case` statement fails to handle one of the flags. Either add a case to handle the flag, or remove it from the `getopts` option string.","problematicCode":"while getopts \"vrn\" n\ndo\n  case \"$n\" in\n    v) echo \"Verbose\" ;;\n    r) echo \"Recursive\" ;;\n    \\?) usage;;\n  esac\ndone","correctCode":"while getopts \"vrn\" n\ndo\n  case \"$n\" in\n    v) echo \"Verbose\" ;;\n    r) echo \"Recursive\" ;;\n    n) echo \"Dry-run\" ;;    # -n handled here\n    \\?) usage;;\n  esac\ndone"},"SC2214":{"code":"SC2214","title":"This case is not specified by getopts.","fix":"You have a `case` statement in a `while getopts` loop that matches a flag that hasn't been provided ","rationale":"You have a `case` statement in a `while getopts` loop that matches a flag that hasn't been provided in the `getopts` option string. Either add the flag to the options list, or delete the case statemen","problematicCode":"while getopts \"vr\" n\ndo\n  case \"$n\" in\n    v) echo \"Verbose\" ;;\n    r) echo \"Recursive\" ;;\n    n) echo \"Dry-run\" ;;\n    *) usage;;\n  esac\ndone","correctCode":"while getopts \"vrn\" n    # 'n' added here\ndo\n  case \"$n\" in\n    v) echo \"Verbose\" ;;\n    r) echo \"Recursive\" ;;\n    n) echo \"Dry-run\" ;;\n    *) usage;;\n  esac\ndone"},"SC2215":{"code":"SC2215","title":"This flag is used as a command name. Bad line break or missing `[ .. ]`?","fix":"You are using a name that starts with a dash as a command name. This is almost always a bug. There a","rationale":"You are using a name that starts with a dash as a command name. This is almost always a bug. There are two typical ways in which this happens: * Missing `[ .. ]` or `[[ .. ]]` around a test expression","problematicCode":"if -e .bashrc\nthen\n  echo \".bashrc already exists\"\nfi","correctCode":"if [ -e .bashrc ]\nthen\n  echo \".bashrc already exists\"\nfi"},"SC2216":{"code":"SC2216","title":"Piping to `rm`, a command that doesn't read stdin. Wrong command or missing `xargs`?","fix":"You are piping to one of several commands that don't read from stdin. This may happen when: * Confus","rationale":"You are piping to one of several commands that don't read from stdin. This may happen when: * Confusing one command for another, e.g. using `echo` where `cat` was intended. * Incorrectly refactoring, ","problematicCode":"ls | echo                      # Want to print result\ncat files | rm                 # Want to delete items from a file\nfind . -type f | cp dir        # Want to process 'find' output\nrm file | true   ","correctCode":"ls\ncat files | while IFS= read -r file; do rm -- \"$file\"; done\nfind . -type f -exec cp {} dir \\;\nrm file || true"},"SC2217":{"code":"SC2217","title":"Redirecting to `echo`, a command that doesn't read stdin. Bad quoting or missing `xargs`?","fix":"You are redirecting to one of several commands that don't read from stdin. This may happen when: * C","rationale":"You are redirecting to one of several commands that don't read from stdin. This may happen when: * Confusing one command for another, e.g. using `echo` where `cat` was intended. * Incorrectly quoting ","problematicCode":"echo << eof\n  Hello World\neof","correctCode":"cat << eof\n  Hello World\neof"},"SC2218":{"code":"SC2218","title":"This function is only defined later. Move the definition up.","fix":"You are calling a function that you are defining later in the file. The function definition must com","rationale":"You are calling a function that you are defining later in the file. The function definition must come first. Function definitions are much like variable assignments, and define a name at the point the","problematicCode":"#!/bin/sh\nmyfunction\nmyfunction() {\n  echo \"Hello World\"\n}","correctCode":"#!/bin/sh\nmyfunction() {\n  echo \"Hello World\"\n}\nmyfunction"},"SC2219":{"code":"SC2219","title":"Instead of `let expr`, prefer `(( expr ))` .","fix":"The `(( .. ))` arithmetic compound command evaluates expressions in the same way as `let`, except it","rationale":"The `(( .. ))` arithmetic compound command evaluates expressions in the same way as `let`, except it's not subject to glob expansion and therefore requires no additional quoting or escaping. This warn","problematicCode":"let a++","correctCode":"(( a++ )) || true"},"SC2220":{"code":"SC2220","title":"Invalid flags are not handled. Add a `*)` case.","fix":"The `case` statement handling `getopts` arguments does not have a default branch to handle unknown f","rationale":"The `case` statement handling `getopts` arguments does not have a default branch to handle unknown flags. When a flag is not recognized, such as if passing `-Z` to the example code, `getopts` will set","problematicCode":"#!/bin/sh\nwhile getopts \"vr\" f\ndo\n  case \"$f\" in\n    v) echo \"verbose\" ;;\n    r) echo \"recursive\" ;;\n  esac\ndone","correctCode":"#!/bin/sh\nwhile getopts \"vr\" f\ndo\n  case \"$f\" in\n    v) echo \"verbose\" ;;\n    r) echo \"recursive\" ;;\n    *) echo \"usage: $0 [-v] [-r]\" >&2\n       exit 1 ;;\n  esac\ndone"},"SC2221":{"code":"SC2221","title":"This pattern always overrides a later one.","fix":"You have specified multiple patterns in a `case` statement, where one will always override the other","rationale":"You have specified multiple patterns in a `case` statement, where one will always override the other. The pattern being overridden is indicated with a [[SC2222]] warning. In the example, `-?` actually","problematicCode":"case \"$1\" in\n  -?) echo \"Usage: $0 [-n]\";;\n  -n) echo \"Hello World\";;\n   *) exit 1;;\nesac","correctCode":"case \"$1\" in\n  -\\?) echo \"Usage: $0 [-n]\";;\n# '-?') echo \"Usage: $0 [-n]\";;  # Also valid\n  -n) echo \"Hello World\";;\n   *) exit 1;;\nesac"},"SC2222":{"code":"SC2222","title":"This pattern never matches because of a previous pattern.","fix":"This pattern never matches because of a previous pattern.","rationale":"","problematicCode":"","correctCode":""},"SC2223":{"code":"SC2223","title":"This default assignment may cause DoS due to globbing. Quote it.","fix":"This statement is an idiomatic way of assigning a default value to an environment variable. However,","rationale":"This statement is an idiomatic way of assigning a default value to an environment variable. However, even though it's passed to `:` which ignores arguments, it's better to quote it. If `COLUMNS='/*/*/","problematicCode":": ${COLUMNS:=80}","correctCode":": \"${COLUMNS:=80}\""},"SC2224":{"code":"SC2224","title":"This `mv` has no destination. Check the arguments.","fix":"ShellCheck found an `mv` command with a single parameter. This may be because the source and destina","rationale":"ShellCheck found an `mv` command with a single parameter. This may be because the source and destination was accidentally merged into a single argument, or because the line was broken in an invalid wa","problematicCode":"mv \"$file $dir\"","correctCode":"mv \"$file\" \"$dir\""},"SC2225":{"code":"SC2225","title":"This `cp` has no destination. Check the arguments.","fix":"ShellCheck found a `cp` command with a single parameter. This may be because the source and destinat","rationale":"ShellCheck found a `cp` command with a single parameter. This may be because the source and destination was accidentally merged into a single argument, or because the line was broken in an invalid way","problematicCode":"cp \"$file $dir\"","correctCode":"cp \"$file\" \"$dir\""},"SC2226":{"code":"SC2226","title":"This `ln` has no destination. Check the arguments, or specify `.` explicitly.","fix":"ShellCheck found a `ln` command with a single parameter. This may be because the source and destinat","rationale":"ShellCheck found a `ln` command with a single parameter. This may be because the source and destination was accidentally merged into a single argument, because the line was broken in an invalid way, o","problematicCode":"ln \"$file $dir\"","correctCode":"ln \"$file\" \"$dir\""},"SC2227":{"code":"SC2227","title":"Redirection applies to the find command itself. Rewrite to work per action (or move to end).","fix":"ShellCheck detected a `find` command with a redirection in the middle. This redirection may have bee","rationale":"ShellCheck detected a `find` command with a redirection in the middle. This redirection may have been intended to apply only to a specific action like `-exec` or `-print`, but it does in fact apply to","problematicCode":"find . -name '*.ppm' -exec pnmtopng {} > {}.png \\;","correctCode":"find . -name '*.ppm' -exec sh -c 'pnmtopng \"$1\" > \"$1.png\"' _ {} \\;"},"SC2229":{"code":"SC2229","title":"This does not read `foo`. Remove `$`/`${}` for that, or use `${var?}` to quiet.","fix":"`read` takes a variable name, but ShellCheck has noticed that you give it an expanded variable inste","rationale":"`read` takes a variable name, but ShellCheck has noticed that you give it an expanded variable instead. This will populate whatever the variable expands to instead of the variable itself. For example:","problematicCode":"read $foo","correctCode":"read foo"},"SC2230":{"code":"SC2230","title":"`which` is non-standard. Use builtin `command -v` instead.","fix":"`which` is a non-standard, external tool that locates an executable in PATH. `command -v` is a POSIX","rationale":"`which` is a non-standard, external tool that locates an executable in PATH. `command -v` is a POSIX standard builtin, which uses the same lookup mechanism that the shell itself would.","problematicCode":"which grep","correctCode":"# For the path of a single, unaliased, external command,\n# or to check whether this will just \"run\" in this shell:\ncommand -v grep\n# To check whether commands exist, without obtaining a reusable path:"},"SC2231":{"code":"SC2231","title":"Quote expansions in this `for` loop glob to prevent word splitting, e.g. `\"${dir}\"/*.txt`.","fix":"When iterating over globs containing expansions, you can still quote all expansions in the path to b","rationale":"When iterating over globs containing expansions, you can still quote all expansions in the path to better handle whitespace and special characters. Just make sure glob characters are outside quotes. `","problematicCode":"for file in ${dir}/*.txt\ndo\n  echo \"Found ${file}\"\ndone","correctCode":"for file in \"${dir}\"/*.txt\ndo\n  echo \"Found ${file}\"\ndone"},"SC2232":{"code":"SC2232","title":"Can't use `sudo` with builtins like `cd`. Did you want `sudo sh -c ..` instead?","fix":"Due to the Unix process model, `sudo` can only change the privileges of a new, external process. It ","rationale":"Due to the Unix process model, `sudo` can only change the privileges of a new, external process. It can not grant privileges to a currently running process. This means that shell builtins -- commands ","problematicCode":"sudo cd /root\npwd","correctCode":"sudo sh -c 'cd /root && pwd'"},"SC2233":{"code":"SC2233","title":"Remove superfluous `(..)` around condition to avoid subshell overhead.","fix":"The shell syntax is `if cmd`, `elif cmd`, `while cmd` and `until cmd` without any parentheses. Inste","rationale":"The shell syntax is `if cmd`, `elif cmd`, `while cmd` and `until cmd` without any parentheses. Instead, parentheses are an independent construct used to create subshells. ShellCheck has noticed that y","problematicCode":"if ([ \"$x\" -gt 0 ])\nthen true; fi","correctCode":"if [ \"$x\" -gt 0 ]\nthen true; fi"},"SC2234":{"code":"SC2234","title":"Remove superfluous `(..)` around test command to avoid subshell overhead.","fix":"You are wrapping a single test command in `(..)`, creating an unnecessary subshell. This serves no p","rationale":"You are wrapping a single test command in `(..)`, creating an unnecessary subshell. This serves no purpose, but is significantly slower: ```console $ i=0; time while ( [ \"$i\" -lt 10000 ] ); do i=$((i+","problematicCode":"([ \"$x\" -gt 0 ]) && foo","correctCode":"[ \"$x\" -gt 0 ] && foo"},"SC2235":{"code":"SC2235","title":"Use `{ ..; }` instead of `(..)` to avoid subshell overhead.","fix":"You appear to be using `(..)` to group test commands. This creates a subshell, making it unnecessari","rationale":"You appear to be using `(..)` to group test commands. This creates a subshell, making it unnecessarily slow. Avoid this by using `{ ..; }` to group. Be careful to note that unlike `(..)`, this require","problematicCode":"([ \"$x\" ] || [ \"$y\" ]) && [ \"$z\" ]","correctCode":"{ [ \"$x\" ] || [ \"$y\" ]; } && [ \"$z\" ]"},"SC2236":{"code":"SC2236","title":"Use `-n` instead of `! -z`.","fix":"You have negated `test -z` or `test -n`, resulting in a needless double-negative. You can just use t","rationale":"You have negated `test -z` or `test -n`, resulting in a needless double-negative. You can just use the other operator instead: ```sh # Identical tests to verify that a value is assigned [ ! -z foo ] #","problematicCode":"if [ ! -n \"$JAVA_HOME\" ]; then echo \"JAVA_HOME not specified\"; fi\nif [ ! -z \"$STY\" ];       then echo \"You are already running screen\"; fi","correctCode":"if [ -z \"$JAVA_HOME\" ]; then echo \"JAVA_HOME not specified\"; fi\nif [ -n \"$STY\" ];       then echo \"You are already running screen\"; fi"},"SC2237":{"code":"SC2237","title":"Use `[ -n .. ]` instead of `! [ -z .. ]`.","fix":"You have negated `test -z` or `test -n`, resulting in a needless double-negative. You can just use t","rationale":"You have negated `test -z` or `test -n`, resulting in a needless double-negative. You can just use the other operator instead: ```sh # Identical tests to verify that a value is assigned ! [ -z foo ] #","problematicCode":"if ! [ -n \"$JAVA_HOME\" ]; then echo \"JAVA_HOME not specified\"; fi\nif ! [ -z \"$STY\" ];       then echo \"You are already running screen\"; fi","correctCode":"if [ -z \"$JAVA_HOME\" ]; then echo \"JAVA_HOME not specified\"; fi\nif [ -n \"$STY\" ];       then echo \"You are already running screen\"; fi"},"SC2238":{"code":"SC2238","title":"Redirecting to/from command name instead of file. Did you want pipes/xargs (or quote to ignore)?","fix":"You are using file redirection, but the filename is an unquoted command name. Instead of running the","rationale":"You are using file redirection, but the filename is an unquoted command name. Instead of running the command and feeding data to it, this just writes to a file with the same name. To run the command a","problematicCode":"cat file > tr -d '\\r'\ncat file > rm","correctCode":"cat file | tr -d '\\r'         # tr reads stdin\ncat file | xargs -d '\\n' rm   # rm reads arguments"},"SC2239":{"code":"SC2239","title":"Ensure the shebang uses the absolute path to the interpreter.","fix":"The script's interpreter, as specified in the shebang, does not start with a `/`. The interpreter sh","rationale":"The script's interpreter, as specified in the shebang, does not start with a `/`. The interpreter should always be specified by absolute path to ensure that the script can be executed from any directo","problematicCode":"#!bin/sh\necho \"Hello World\"","correctCode":"#!/bin/sh\necho \"Hello World\""},"SC2240":{"code":"SC2240","title":"The dot command does not support arguments in sh/dash. Set them as variables.","fix":"In Bash and Ksh, you can use `. myscript arg1 arg2..` to set `$1` and `$2` in the sourced script. Th","rationale":"In Bash and Ksh, you can use `. myscript arg1 arg2..` to set `$1` and `$2` in the sourced script. This is not the case in Dash, where any additional arguments are ignored, or in POSIX sh where the beh","problematicCode":"#!/bin/sh\n. include/myscript example.com 80","correctCode":"#!/bin/sh\nhost=example.com port=80 . include/myscript"},"SC2241":{"code":"SC2241","title":"The exit status can only be one integer 0-255. Use stdout for other data.","fix":"In bash, `exit` can only be used to signal success or failure (0 = success, 1-255 = failure). To exi","rationale":"In bash, `exit` can only be used to signal success or failure (0 = success, 1-255 = failure). To exit with textual or multiple values from a function, write them to stdout and capture them with comman","problematicCode":"exit foo bar","correctCode":"echo foo\necho bar\nexit"},"SC2242":{"code":"SC2242","title":"Can only exit with status 0-255. Other data should be written to stdout/stderr.","fix":"`exit` can only be used to signal success or failure (0 = success, 1-255 = failure). It can not be u","rationale":"`exit` can only be used to signal success or failure (0 = success, 1-255 = failure). It can not be used to return string data, and it can not be used to print error messages. String data should be wri","problematicCode":"exit \"Bad filename\"","correctCode":"echo \"Bad filename\" >&2\nexit 1"},"SC2243":{"code":"SC2243","title":"Prefer explicit `-n` to check for output (or run command without `[`/`[[` to check for success)","fix":"`[ \"$(mycommand)\" ]` is equivalent to `[ -n \"$(mycommand)\" ]` and checks whether the command's outpu","rationale":"`[ \"$(mycommand)\" ]` is equivalent to `[ -n \"$(mycommand)\" ]` and checks whether the command's output on stdout was non-empty. Users more familiar with other languages are often surprised to learn tha","problematicCode":"if [ \"$(mycommand --myflags)\" ]\nthen\n  echo \"True\"\nfi","correctCode":"# Check that the command outputs something on stdout\nif [ -n \"$(mycommand --myflags)\" ]\nthen\n  echo \"The command had output on stdout\"\nfi\n# Check instead that the command succeeded (exit code = 0)\nif "},"SC2244":{"code":"SC2244","title":"Prefer explicit `-n` to check non-empty string (or use `=`/`-ne` to check boolean/integer).","fix":"`[ \"$var\" ]` is equivalent to `[ -n \"$var\" ]` and checks that a string is non-empty. Users more fami","rationale":"`[ \"$var\" ]` is equivalent to `[ -n \"$var\" ]` and checks that a string is non-empty. Users more familiar with other languages are often surprised to learn that `[ \"$var\" ]` is true when: * `var=false`","problematicCode":"if [ \"$1\" ]\nthen\n  echo \"True\"\nfi","correctCode":"# Check if $1 is empty or non-empty\nif [ -n \"$1\" ]\nthen\n  echo \"True, $1 is a non-empty value\"\nfi\n# Check instead if $1 is true or false, as in Java\n[ \"$1\" = \"true\" ]\n# Check instead if $1 is non-zero"},"SC2245":{"code":"SC2245","title":"-d only applies to the first expansion of this glob. Use a loop to check any/all.","fix":"Ksh has the curious behavior of ignoring anything after an unrecognized flag to `test`/`[`, which me","rationale":"Ksh has the curious behavior of ignoring anything after an unrecognized flag to `test`/`[`, which means that file checking operators against globs will effectively apply the operator to the first expa","problematicCode":"#!/bin/ksh\nif [ -f ksh* ]\nthen\n  echo \"The file exists\"\nfi","correctCode":"#!/bin/ksh\nfor f in ksh*\ndo\n  if [ -f \"$f\" ]\n  then\n    echo \"Found a matching file: $f\"\n  fi\ndone"},"SC2246":{"code":"SC2246","title":"This shebang specifies a directory. Ensure the interpreter is a file.","fix":"The shebang specifies which file to use as an interpreter, but probably due to some kind of typo, yo","rationale":"The shebang specifies which file to use as an interpreter, but probably due to some kind of typo, your script's interpreter ends in a `/`, indicating a directory. Ensure it points to a valid executabl","problematicCode":"#!/bin/sh/\necho \"Hello World\"","correctCode":"#!/bin/sh\necho \"Hello World\""},"SC2247":{"code":"SC2247","title":"Flip leading `$` and `\"` if this should be a quoted substitution.","fix":"ShellCheck has found a `$\"(` or `$\"{` . This is most likely due to flipping the dollar-sign and doub","rationale":"ShellCheck has found a `$\"(` or `$\"{` . This is most likely due to flipping the dollar-sign and double quote: echo $\"(cmd)\"  # Supposed to be \"$(cmd)\" echo $\"{var}\"  # Supposed to be \"${var}\" Instead ","problematicCode":"var=$\"(whoami)\"","correctCode":"var=\"$(whoami)\""},"SC2248":{"code":"SC2248","title":"Prefer double quoting even when variables don't contain special characters.","fix":"Shellcheck normally warns about unquoted variable use due to potential globbing or word splitting is","rationale":"Shellcheck normally warns about unquoted variable use due to potential globbing or word splitting issues. See [[SC2086]] for details. However if it is determined that a variable does not have have spa","problematicCode":"subdir='example'\ncd ${subdir}","correctCode":"subdir='example'\ncd \"${subdir}\""},"SC2249":{"code":"SC2249","title":"Consider adding a default `*)` case, even if it just exits with error.","fix":"ShellCheck found a `case` statement that may not be considering all possible cases. This may mean th","rationale":"ShellCheck found a `case` statement that may not be considering all possible cases. This may mean that only the [happy paths](https://en.wikipedia.org/wiki/Happy_path) are accounted for. Consider addi","problematicCode":"case \"$1\" in\n  start) start_service ;;\n  stop)  stop_service ;;\n  restart|reload|force-reload)\n    stop_service;\n    start_service;;\nesac","correctCode":"case \"$1\" in\n  start) start_service ;;\n  stop)  stop_service ;;\n  restart|reload|force-reload)\n    stop_service;\n    start_service;;\n  *)\n    echo >&2 \"Invalid choice: $1\"\n    exit 1\nesac"},"SC2250":{"code":"SC2250","title":"Prefer putting braces around variable references even when not strictly required.","fix":"If a variable gets called, and there is a string that gets appended to the variable that could get m","rationale":"If a variable gets called, and there is a string that gets appended to the variable that could get misinterpreted as possibly part of the name of the variable. Then it will not call the right variable","problematicCode":"partial_path='example'\ncurl \"http://example.com/$partial_path_version/explain.html\"","correctCode":"partial_path='example'\ncurl \"http://example.com/${partial_path}_version/explain.html\""},"SC2251":{"code":"SC2251","title":"This `!` is not on a condition and skips errexit. Add `|| exit 1` or make sure `$?` is checked.","fix":"ShellCheck has found a command inverted with `!` that may have no effect. In particular, it does not","rationale":"ShellCheck has found a command inverted with `!` that may have no effect. In particular, it does not appear as a condition in an `if` statement or `while` loop, or as the final command in a script or ","problematicCode":"set -e\n! false\nrest","correctCode":"set -e\n! false || exit 1"},"SC2252":{"code":"SC2252","title":"You probably wanted `&&` here, otherwise it's always true.","fix":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `[ \"$1\" ","rationale":"This is not a bash issue, but a simple, common logical mistake applicable to all languages. `[ \"$1\" != foo ] || [ \"$1\" != bar ]` is always true (when `foo != bar`): * If `$1 = foo` then `$1 != bar` is","problematicCode":"if [ \"$1\" != foo ] || [ \"$1\" != bar ]\nthen\n  echo \"$1 is not foo or bar\"\nfi","correctCode":"if [ \"$1\" != foo ] && [ \"$1\" != bar ]\nthen\n  echo \"$1 is not foo or bar\"\nfi"},"SC2253":{"code":"SC2253","title":"Use `-R` to recurse, or explicitly `a-r` to remove read permissions.","fix":"Many tools use `-r` for recursive operation, but in `chmod` this removes read permissions. If you wa","rationale":"Many tools use `-r` for recursive operation, but in `chmod` this removes read permissions. If you wanted to change permissions recursively, change the flag to `-R`. If you wanted to remove read permis","problematicCode":"chmod -r 0700 dir\nchmod -r file","correctCode":"chmod -R 0700 dir\nchmod a-r file"},"SC2254":{"code":"SC2254","title":"Quote expansions in case patterns to match literally rather than as a glob.","fix":"When unquoted variables and command expansions are used in case branch patterns, they will be interp","rationale":"When unquoted variables and command expansions are used in case branch patterns, they will be interpreted as globs. This can lead to some surprising behavior, such as `case $x in $x) trigger;; esac` n","problematicCode":"case $input in\n  -       ) echo \"Reading from stdin...\" ;;\n  $output ) echo \"Input should be different from output\" ;;\nesac","correctCode":"case $input in\n  -         ) echo \"Reading from stdin...\" ;;\n  \"$output\" ) echo \"Input should be different from output\" ;;\nesac"},"SC2255":{"code":"SC2255","title":"`[ ]` does not apply arithmetic evaluation. Evaluate with `$((..))` for numbers, or use string comparator for strings.","fix":"When using `[[ .. ]]` with numerical comparators (`-eq`, `-lt`, etc), the value on either side will ","rationale":"When using `[[ .. ]]` with numerical comparators (`-eq`, `-lt`, etc), the value on either side will be evaluated as an arithmetic expression. This means that `2*3` will be evaluated to `6`, and `x` wi","problematicCode":"[ 2*3 -eq array[i] ]","correctCode":"[ $((2*3)) -eq $((array[i])) ]"},"SC2256":{"code":"SC2256","title":"This translated string is the name of a variable. Flip leading `$` and `\"` if this should be a quoted substitution.","fix":"`$\"..\"` is a localized string, for example, `echo $\"Hello $USER\"` along with the proper translation ","rationale":"`$\"..\"` is a localized string, for example, `echo $\"Hello $USER\"` along with the proper translation files can be used to have the script say \"Bonjour, youruser\" in French locales. In this case, ShellC","problematicCode":"var=\"foo\"\necho $\"var\"","correctCode":"var=\"foo\"\necho \"$var\""},"SC2257":{"code":"SC2257","title":"Arithmetic modifications in command redirections may be discarded. Do them separately.","fix":"You are using an arithmetic expression that modifies a variable, e.g. `$((x+=1))` or `$((x++))`, in ","rationale":"You are using an arithmetic expression that modifies a variable, e.g. `$((x+=1))` or `$((x++))`, in the name of a file to redirect from/to, in a here document, or in a here string. The scope of these ","problematicCode":"curl \"$URL\" > \"image$((i++)).jpg\"","correctCode":"i=$((i+1))\ncurl \"$URL\" > \"image$i.jpg\""},"SC2258":{"code":"SC2258","title":"The trailing comma is part of the value, not a separator. Delete or quote it.","fix":"ShellCheck found a `for` loop where the items appear to be separated by commas. These will be treate","rationale":"ShellCheck found a `for` loop where the items appear to be separated by commas. These will be treated as literal commas. If the commas are part of the value, enclose them in quotes, or remove them.","problematicCode":"for f in foo, bar, baz\ndo\n  echo \"$f\"\ndone","correctCode":"for f in foo bar baz\ndo\n  echo \"$f\"\ndone"},"SC2259":{"code":"SC2259","title":"This redirection overrides piped input. To use both, merge or pass filenames.","fix":"A process only has a single standard input stream. Pipes and input redirections both overwrite it, s","rationale":"A process only has a single standard input stream. Pipes and input redirections both overwrite it, so you can't use both at the same time. If you try, the redirection takes precedence and the input pi","problematicCode":"gzcat yesterday.log.gz | grep \"$USER\" < today.log","correctCode":"# Specify non-piped inputs as filenames\ngzcat yesterday.log.gz | grep \"$USER\" - today.log\n# Or merge multiple inputs into a single stream\n{ gzcat yesterday.log.gz; cat today.log; } | grep \"$USER\""},"SC2260":{"code":"SC2260","title":"This redirection overrides the output pipe. Use `tee` to output to both.","fix":"A process only has a single standard output stream. Pipes and output redirections both overwrite it,","rationale":"A process only has a single standard output stream. Pipes and output redirections both overwrite it, so you can't use both at the same time. If you try, the redirection takes precedence and the output","problematicCode":"env > environment.txt | grep '^ANDROID'","correctCode":"env | tee environment.txt | grep '^ANDROID'"},"SC2261":{"code":"SC2261","title":"Multiple redirections compete for stdout. Use `cat`, `tee`, or pass filenames instead.","fix":"A file descriptor, whether stdin, stdout, stderr, or non-standard ones, can only point to a single f","rationale":"A file descriptor, whether stdin, stdout, stderr, or non-standard ones, can only point to a single file/pipe. For input, many commands support processing multiple filenames. In these cases you can jus","problematicCode":"grep foo < input1 < input2 > output1 > output2 > output3","correctCode":"# Merge inputs into a single stream, write outputs individually\ncat input1 input2 | grep foo | tee output1 output2 > output3\n# Pass inputs as filenames, write outputs individually\ngrep foo input1 inpu"},"SC2262":{"code":"SC2262","title":"This alias can't be defined and used in the same parsing unit. Use a function instead.","fix":"Alias expansion happens at parse time, which means to have an effect, the `alias` command must be ex","rationale":"Alias expansion happens at parse time, which means to have an effect, the `alias` command must be executed not just before the alias is invoked, but before the invocation is parsed. A shell will parse","problematicCode":"function checksum() {\n  type md5 && alias md5sum=md5\n  md5sum \"$@\"  # This calls `md5sum`, not `md5`\n}","correctCode":"function checksum() {\n  type md5 && md5sum() { md5 \"$@\"; }\n  md5sum \"$@\"  # Now this would call `md5` when applicable\n}"},"SC2263":{"code":"SC2263","title":"Since they're in the same parsing unit, this command will not refer to the previously mentioned alias.","fix":"Since they're in the same parsing unit, this command will not refer to the previously mentioned alias.","rationale":"","problematicCode":"","correctCode":""},"SC2264":{"code":"SC2264","title":"This function unconditionally re-invokes itself. Missing `command`?","fix":"ShellCheck found a function that immediately and unconditionally re-invokes itself, causing infinite","rationale":"ShellCheck found a function that immediately and unconditionally re-invokes itself, causing infinite recursion. This generally happens when writing a wrapper function with the same name as an existing","problematicCode":"ls() {\n  ls --color=always \"$@\"\n}\ncd() {\n  cd \"$@\" && ls\n}","correctCode":"ls() {\n  command ls --color=always \"$@\"\n}\ncd() {\n  command cd \"$@\" && ls\n}"},"SC2265":{"code":"SC2265","title":"Use `&&` for logical AND. Single `&` will background and return true.","fix":"ShellCheck found a `test` command followed by a `&`. This runs the test in the background, effective","rationale":"ShellCheck found a `test` command followed by a `&`. This runs the test in the background, effectively ignoring it. To specify \"logical AND\" between two commands, use `&&`.","problematicCode":"if [ \"$1\" = \"install\" ] & [ \"$USER\" != \"root\" ]\nthen\n  echo \"Must be root to install\"\nfi","correctCode":"if [ \"$1\" = \"install\" ] && [ \"$USER\" != \"root\" ]\nthen\n  echo \"Must be root to install\"\nfi"},"SC2266":{"code":"SC2266","title":"Use `||` for logical OR. Single `|` will pipe.","fix":"ShellCheck found a `test` command followed by a `|`. This was undoubtedly intended as a logical OR (","rationale":"ShellCheck found a `test` command followed by a `|`. This was undoubtedly intended as a logical OR (`||`).","problematicCode":"if [ \"$1\" = \"--verbose\" ] | [ \"$1\" = \"-v\" ]\nthen\n  verbose=1\nfi","correctCode":"if [ \"$1\" = \"--verbose\" ] || [ \"$1\" = \"-v\" ]\nthen\n  verbose=1\nfi"},"SC2267":{"code":"SC2267","title":"GNU `xargs -i` is deprecated in favor of `-I{}`","fix":"`xargs -i` is a GNU specific option. It has been deprecated in favor of the POSIX standard option `-","rationale":"`xargs -i` is a GNU specific option. It has been deprecated in favor of the POSIX standard option `-I`. Note that `-i` will implicitly use `{}` as a token if nothing is specified, while `-I` requires ","problematicCode":"# Implicit replacement string\nxargs -i ls {}\n# Explicit replacement string\nxargs -imyfilename ls myfilename","correctCode":"xargs -I {} ls {}\nxargs -I filename ls filename"},"SC2268":{"code":"SC2268","title":"Avoid x-prefix in comparisons as it no longer serves a purpose.","fix":"Some older shells would get confused if the first argument started with a dash, or consisted of `!` ","rationale":"Some older shells would get confused if the first argument started with a dash, or consisted of `!` or `(`. As a workaround, people would prefix variables and values to be compared with `x` to ensure ","problematicCode":"[ \"x$pass\" = \"xswordfish\" ]\ntest x\"$var\" = x","correctCode":"[ \"$pass\" = \"swordfish\" ] \ntest \"$var\" = \"\""},"SC2269":{"code":"SC2269","title":"This variable is assigned to itself, so the assignment does nothing.","fix":"ShellCheck found a variable that is assigned to itself, e.g. `x=$x`. This obviously has no effect. D","rationale":"ShellCheck found a variable that is assigned to itself, e.g. `x=$x`. This obviously has no effect. Double check what the assignment was supposed to do.","problematicCode":"var=\"$var\"","correctCode":"# If the goal is to do nothing\ntrue"},"SC2270":{"code":"SC2270","title":"To assign positional parameters, use `set -- first second ..` (or use `[ ]` to compare).","fix":"You have a command on the form `$2=value`. If the goal is to assign a new value to the positional pa","rationale":"You have a command on the form `$2=value`. If the goal is to assign a new value to the positional parameters, use the `set` builtin: `set -- one two ..` will cause `$1` to be \"one\" and `$2` to be \"two","problematicCode":"if [ -z \"$1\" ]\nthen\n  $1=\"help\"\nfi","correctCode":"if [ -z \"$1\" ]\nthen\n  set -- \"help\"\nfi"},"SC2271":{"code":"SC2271","title":"For indirection, use arrays, `declare \"var$n=value\"`, or (for sh) read/eval","fix":"`var$n=value` is not a valid way of assigning to a dynamically created variable name in any shell. P","rationale":"`var$n=value` is not a valid way of assigning to a dynamically created variable name in any shell. Please use one of the other methods to assign to names via expanded strings. [Wooledge BashFaq #6](ht","problematicCode":"n=1\nvar$n=\"hello\"","correctCode":"n=1\nvar[n]=\"hello\"\necho \"${var[n]}\""},"SC2272":{"code":"SC2272","title":"Command name contains `==`. For comparison, use `[ \"$var\" = value ]`.","fix":"ShellCheck found a command name that contains a `==`. Most likely, this was intended as a kind of co","rationale":"ShellCheck found a command name that contains a `==`. Most likely, this was intended as a kind of comparison. To compare two values, use `[ value1 = value2 ]`. Both the brackets and the spaces around ","problematicCode":"$a/$b==foo/bar","correctCode":"[ \"$a/$b\" = \"foo/bar\" ]"},"SC2273":{"code":"SC2273","title":"Sequence of `===`s found. Merge conflict or intended as a commented border?","fix":"ShellCheck found a series of `=======`s. If this was supposed to be a border or separator, use a com","rationale":"ShellCheck found a series of `=======`s. If this was supposed to be a border or separator, use a comment. However, it could also be left behind from a source control merge conflict: ```sh <<<<<<< HEAD","problematicCode":"=======","correctCode":"<<<<<<< HEAD\necho \"Goodbye World\"\n=======\necho \"Hello World!\"\n>>>>>>> mybranch"},"SC2274":{"code":"SC2274","title":"Command name starts with `===`. Intended as a commented border?","fix":"ShellCheck found a command that starts with a series of `===`s. This may have been intended as a bor","rationale":"ShellCheck found a command that starts with a series of `===`s. This may have been intended as a border, but is missing the `#` to turn it into a harmless comment.","problematicCode":"===================== MAIN SECTION =======================","correctCode":"# ===================== MAIN SECTION ======================="},"SC2275":{"code":"SC2275","title":"Command name starts with `=`. Bad line break?","fix":"ShellCheck found a command name starting with a `=`. This was likely not meant as a new command, but","rationale":"ShellCheck found a command name starting with a `=`. This was likely not meant as a new command, but instead a continuation from a previous line. Make sure the `=` is used correctly.","problematicCode":"my_variable\n  =value","correctCode":"myvariable=value"},"SC2276":{"code":"SC2276","title":"This is interpreted as a command name containing `=`. Bad assignment or comparison?","fix":"ShellCheck found a command name containing an unquoted equals sign `=`. This was likely intended as ","rationale":"ShellCheck found a command name containing an unquoted equals sign `=`. This was likely intended as either a comparison or an assignment. To compare two values, use e.g. `[ \"$var\" = \"42\" ]` To assign ","problematicCode":"\"$var\"=42\nif \"$var\"=42\nthen\n  true\nfi","correctCode":"var=42\nif [ \"$var\" = 42 ]\nthen\n  true\nfi"},"SC2277":{"code":"SC2277","title":"Use `BASH_ARGV0` to assign to `$0` in bash (or use `[ ]` to compare).","fix":"You appear to be trying to assign a new value to `$0` in a Bash script. To do this, instead assign t","rationale":"You appear to be trying to assign a new value to `$0` in a Bash script. To do this, instead assign to the special variable `BASH_ARGV0`. If you instead wanted to compare the value of `$0`, use a compa","problematicCode":"#!/bin/bash\n$0=myscriptname","correctCode":"#!/bin/bash\nBASH_ARGV0=myscriptname"},"SC2278":{"code":"SC2278","title":"`$0` can't be assigned in Ksh (but it does reflect the current function).","fix":"You appear to be trying to assign a new value to `$0` in Ksh. This is not possible. However, `$0` wi","rationale":"You appear to be trying to assign a new value to `$0` in Ksh. This is not possible. However, `$0` will reflect the current function name, so if you wrap your code in a function with your chosen name, ","problematicCode":"#!/bin/ksh\n$0=myname\necho \"Usage: $0 --help\"","correctCode":"#!/bin/ksh\nmyname() {\n  echo \"Usage: $0 --help\"\n}\nmyname"},"SC2279":{"code":"SC2279","title":"`$0` can't be assigned in Dash. This becomes a command name.","fix":"You appear to be trying to assign a new value to `$0` in Dash. Dash does not support this. Write aro","rationale":"You appear to be trying to assign a new value to `$0` in Dash. Dash does not support this. Write around it, or switch to Bash.","problematicCode":"#!/bin/dash\n$0=myname","correctCode":""},"SC2280":{"code":"SC2280","title":"`$0` can't be assigned this way, and there is no portable alternative.","fix":"You appear to be trying to assign a new value to `$0` in a `sh` script. There is no portable way to ","rationale":"You appear to be trying to assign a new value to `$0` in a `sh` script. There is no portable way to do this. Write around it, or switch to Bash.","problematicCode":"#!/bin/sh\n$0=myname","correctCode":""},"SC2281":{"code":"SC2281","title":"Don't use `$`/`${}` on the left side of assignments.","fix":"Unlike Perl or PHP, `$` is not used on the left-hand side of `=` when assigning to a variable.","rationale":"Unlike Perl or PHP, `$` is not used on the left-hand side of `=` when assigning to a variable.","problematicCode":"$greeting=\"Hello World\"\n${greeting}=\"Hello World\"","correctCode":"greeting=\"Hello World\""},"SC2282":{"code":"SC2282","title":"Variable names can't start with numbers, so this is interpreted as a command.","fix":"You appear to be assigning to a variable name that starts with a digit. This is not allowed: variabl","rationale":"You appear to be assigning to a variable name that starts with a digit. This is not allowed: variables must start with A-Z, a-z or _. Switch to a variable name that does not start with a digit.","problematicCode":"411toppm=true","correctCode":"_411toppm=true"},"SC2283":{"code":"SC2283","title":"Use `[ ]` to compare values, or remove spaces around `=` to assign (or quote `'='` if literal).","fix":"ShellCheck found an unquoted `=` after a word. If this was supposed to be a comparison, use square b","rationale":"ShellCheck found an unquoted `=` after a word. If this was supposed to be a comparison, use square brackets: `[ \"$var\" = value ]` If this was supposed to be an assignment, remove spaces around `=`: `v","problematicCode":"# Assignment\nvar = value\n# Comparison\nif $var = value\nthen\n  echo \"Match\"\nfi","correctCode":"# Assignment\nvar=value\n# Comparison\nif [ \"$var\" = value ]\nthen\n  echo \"Match\"\nfi"},"SC2284":{"code":"SC2284","title":"Use `[ x = y ]` to compare values (or quote `'=='` if literal).","fix":"ShellCheck found an unquoted `==` after a word. This was most likely supposed to be a comparison, so","rationale":"ShellCheck found an unquoted `==` after a word. This was most likely supposed to be a comparison, so use square brackets as in the correct code.","problematicCode":"if $var == value\nthen\n  echo \"Match\"\nfi","correctCode":"if [ \"$var\" = value ]\nthen\n  echo \"Match\"\nfi"},"SC2285":{"code":"SC2285","title":"Remove spaces around `+=` to assign (or quote `'+='` if literal).","fix":"ShellCheck found an unquoted `+=` after a word. To append text to a variable, remove spaces around `","rationale":"ShellCheck found an unquoted `+=` after a word. To append text to a variable, remove spaces around `+=` as in the example.","problematicCode":"var += \"my text\"","correctCode":"var+=\"my text\""},"SC2286":{"code":"SC2286","title":"This empty string is interpreted as a command name. Double check syntax (or use 'true' as a no-op).","fix":"ShellCheck found an empty string used as a command name. This is never valid. If the command is inte","rationale":"ShellCheck found an empty string used as a command name. This is never valid. If the command is intended to do nothing, use `true` aka `:` instead. Otherwise, determine why an empty string ended up as","problematicCode":"jq \n   ''\n   file.json","correctCode":"jq \\\n  '' \\\n  file.json"},"SC2287":{"code":"SC2287","title":"This is interpreted as a command name ending with '/'. Double check syntax.","fix":"ShellCheck found a command name that ends with `/`. Since directories are not valid commands, this i","rationale":"ShellCheck found a command name that ends with `/`. Since directories are not valid commands, this is always wrong. The most common reason is bad quoting or escaping, such as in the example where a sp","problematicCode":"df/","correctCode":"df /"},"SC2288":{"code":"SC2288","title":"This is interpreted as a command name ending with apostrophe. Double check syntax.","fix":"ShellCheck found a command name ending with a symbol, such as a comma, parenthesis, quote, or simila","rationale":"ShellCheck found a command name ending with a symbol, such as a comma, parenthesis, quote, or similar. This is almost always due to a syntax issue in the script. In the examples, bad quoting and inval","problematicCode":"var=$(\"wget 'http://www.shellcheck.net/'\")\necho Usage: $0 {start|stop|restart}\narray=val1, val2, val3","correctCode":"var=\"$(wget 'http://www.shellcheck.net/')\"\necho \"Usage: $0 {start|stop|restart}\"\narray=(val1 val2 val3)"},"SC2289":{"code":"SC2289","title":"This is interpreted as a command name containing a linefeed. Double check syntax.","fix":"ShellCheck found a command name containing an especially unusual character like a tab or linefeed. T","rationale":"ShellCheck found a command name containing an especially unusual character like a tab or linefeed. This is most likely due to a syntax issue. In the example, this was due to a Python style documentati","problematicCode":"'''\nThis script greets the planet\n'''\necho \"Hello World\"","correctCode":"# This script greets the planet\necho \"Hello World\""},"SC2290":{"code":"SC2290","title":"Remove spaces around = to assign.","fix":"Parameters to `export`, `declare`, `local`, `typeset` and `readonly` may not have spaces around the ","rationale":"Parameters to `export`, `declare`, `local`, `typeset` and `readonly` may not have spaces around the `=` or `+=` operator. This is the same as for regular variable assignments: export var = value   # I","problematicCode":"export LC_ALL = \"POSIX\"","correctCode":"export LC_ALL=\"POSIX\""},"SC2291":{"code":"SC2291","title":"Quote repeated spaces to avoid them collapsing into one.","fix":"ShellCheck found multiple unquoted spaces between words passed to `echo`. Due to the way arguments a","rationale":"ShellCheck found multiple unquoted spaces between words passed to `echo`. Due to the way arguments are interpreted and passed in the shell, these will collapse into a single space: ```console $ echo H","problematicCode":"echo Hello        World","correctCode":"echo \"Hello        World\""},"SC2292":{"code":"SC2292","title":"Prefer `[[ ]]` over `[ ]` for tests in Bash/Ksh.","fix":"ShellCheck has been explicitly asked to warn about uses of `[ .. ]` in favor of the extended Bash/Ks","rationale":"ShellCheck has been explicitly asked to warn about uses of `[ .. ]` in favor of the extended Bash/Ksh test `[[ .. ]]`. `[[ .. ]]` suppresses word splitting and globbing, supports a wider variety of te","problematicCode":"[ -e /etc/issue ]","correctCode":"[[ -e /etc/issue ]]"},"SC2293":{"code":"SC2293","title":"When eval'ing @Q-quoted words, use * rather than @ as the index.","fix":"ShellCheck noticed that you are calling `eval` and including an escaped array. However, the array is","rationale":"ShellCheck noticed that you are calling `eval` and including an escaped array. However, the array is passed as multiple arguments and relies on being implicitly joined together to form a single shell ","problematicCode":"eval \"$MYCOMMAND ${@@Q}\"","correctCode":"eval \"$MYCOMMAND ${*@Q}\""},"SC2294":{"code":"SC2294","title":"eval negates the benefit of arrays. Drop eval to preserve whitespace/symbols (or eval as string).","fix":"ShellCheck found `eval` used on an array (or equivalently, `\"$@\"`). This is problematic because it e","rationale":"ShellCheck found `eval` used on an array (or equivalently, `\"$@\"`). This is problematic because it effectively throws away all boundary information and rebuilds it from shell words. Let's say you invo","problematicCode":"check() {\n  eval \"$@\" || exit\n}","correctCode":"check() {\n  \"$@\" || exit\n}"},"SC2295":{"code":"SC2295","title":"Expansions inside `${..}` need to be quoted separately, otherwise they will match as a pattern.","fix":"When using expansions in a parameter expansion prefix/suffix expression, the expansion needs to be q","rationale":"When using expansions in a parameter expansion prefix/suffix expression, the expansion needs to be quoted separately or it will match as a pattern. The quotes around the outer parameter expansion do n","problematicCode":"relative_path() {\n  printf '%s\\n' \"${2#$1}\"\n}\n# Results in \"/tmp/King_Kong_[1933]/extras/trailer.mkv\" because the prefix fails to match\nrelative_path \"/tmp/King_Kong_[1933]/\" \"/tmp/King_Kong_[1933]/ex","correctCode":"relative_path() {\n  printf '%s\\n' \"${2#\"$1\"}\"\n}\n# Results in \"extras/trailer.mkv\" as expected\nrelative_path \"/tmp/King_Kong_[1933]/\" \"/tmp/King_Kong_[1933]/extras/trailer.mkv\"\n# Results in \"/tmp/King_"},"SC2296":{"code":"SC2296","title":"Parameter expansions can't start with `{`. Double check syntax.","fix":"ShellCheck found a parameter expansion `${something}` that starts with an invalid character. In the ","rationale":"ShellCheck found a parameter expansion `${something}` that starts with an invalid character. In the example, this was caused by accidentally duplicating the `{` in `${{name}`. Double check the syntax ","problematicCode":"echo \"Hello ${{name}\"","correctCode":"echo \"Hello ${name}\""},"SC2297":{"code":"SC2297","title":"Double quotes must be outside `${}`: `${\"invalid\"}` vs `\"${valid}\"`.","fix":"ShellCheck found a parameter expansion containing what appears to be a quoted variable name. While t","rationale":"ShellCheck found a parameter expansion containing what appears to be a quoted variable name. While the parameter expansion itself must be quoted, as in `\"${valid}\"`, the quotes may not appear inside t","problematicCode":"echo ${\"USER\"}","correctCode":"echo \"${USER}\""},"SC2298":{"code":"SC2298","title":"`${$x}` is invalid. For expansion, use ${x}. For indirection, use arrays, ${!x} or (for sh) eval.","fix":"ShellCheck found a parameter expansion `${..}` where the first element was a second parameter expans","rationale":"ShellCheck found a parameter expansion `${..}` where the first element was a second parameter expansion, either `${$x..}` or `${${x}..}`. This is not valid. In the first example, the extra `$` was uni","problematicCode":"# Expecting $RETRIES or 3 if unset\nretries=${$RETRIES:-3}","correctCode":"retries=${RETRIES:-3}"},"SC2299":{"code":"SC2299","title":"Parameter expansions can't be nested. Use temporary variables.","fix":"ShellCheck found what appears to be a nested parameter expansion. In the example, it was hoping to c","rationale":"ShellCheck found what appears to be a nested parameter expansion. In the example, it was hoping to combine `${var##*/}` to strip the directory and `${var%.*}` to strip the extension. Parameter expansi","problematicCode":"path=\"/path/to/MyFile.mp3\"\necho \"Playing ${${path##*/}%.*}\"    # Expect: Playing MyFile","correctCode":"path=\"/path/to/MyFile.mp3\"\ntmp=${path##*/}\necho \"Playing ${tmp%.*}\""},"SC2300":{"code":"SC2300","title":"Parameter expansion can't be applied to command substitutions. Use temporary variables.","fix":"ShellCheck found a parameter expansion that begins with a command substitution, such as `$(..)` or `","rationale":"ShellCheck found a parameter expansion that begins with a command substitution, such as `$(..)` or `` `..` ``. This is not valid. Parameter expansion only works on variables (normal or special). In th","problematicCode":"echo \"Building ${$(git rev-parse --show-toplevel)##*/}\"","correctCode":"tmp=$(git rev-parse --show-toplevel)\necho \"Building ${tmp##*/}\""},"SC2301":{"code":"SC2301","title":"Parameter expansion starts with unexpected quotes. Double check syntax.","fix":"ShellCheck found a parameter expansion `${...}` that contains an unexpected syntax element, such as ","rationale":"ShellCheck found a parameter expansion `${...}` that contains an unexpected syntax element, such as single or double quotes. In the example, this was due to wrapping a translated string `$\"..\"` with c","problematicCode":"echo ${\"Hello World\"}","correctCode":"echo $\"Hello World\""},"SC2302":{"code":"SC2302","title":"This loops over values. To loop over keys, use `\"${!array[@]}\"`.","fix":"ShellCheck found a `for` loop over array *values*, where the variable is used as an array *key*. In ","rationale":"ShellCheck found a `for` loop over array *values*, where the variable is used as an array *key*. In the problematic example, the loop will print `Value is foo` twice. On the second iteration, `v=bar`,","problematicCode":"array=(foo bar)\nfor v in \"${array[@]}\"\ndo\n  echo \"Value is ${array[$v]}\"\ndone","correctCode":"for v in \"${array[@]}\"\ndo\n  echo \"Value is $v\"\ndone"},"SC2303":{"code":"SC2303","title":"`i` is an array value, not a key. Use directly or loop over keys instead.","fix":"`i` is an array value, not a key. Use directly or loop over keys instead.","rationale":"","problematicCode":"","correctCode":""},"SC2304":{"code":"SC2304","title":"`*` must be escaped to multiply: `\\*`. Modern `$((x * y))` avoids this issue.","fix":"ShellCheck found an `expr` command whose operator is an unescaped asterisk `*`. When using `expr`, e","rationale":"ShellCheck found an `expr` command whose operator is an unescaped asterisk `*`. When using `expr`, each argument is expanded the same way as for any other command. This means that `expr 2 * 3` will tu","problematicCode":"result=$(expr 2 * 3)","correctCode":"# Modern, efficient, POSIX standard approach\nresult=$(( 2 * 3 ))\n# Older, slower approach\nresult=$(expr 2 \\* 3)"},"SC2305":{"code":"SC2305","title":"Quote regex argument to expr to avoid it expanding as a glob.","fix":"ShellCheck found an `expr` command using `:` to match a regex, but the regex is not quoted and there","rationale":"ShellCheck found an `expr` command using `:` to match a regex, but the regex is not quoted and therefore being treated as a glob. This means that if the problematic code is ever executed in a director","problematicCode":"expr \"$input\" : [0-9]*","correctCode":"expr \"$input\" : \"[0-9]*\""},"SC2306":{"code":"SC2306","title":"Escape glob characters in arguments to expr to avoid pathname expansion.","fix":"`expr` is a command so `expr 2 * 2` will consider `*` to mean \"all files in the current directory\". ","rationale":"`expr` is a command so `expr 2 * 2` will consider `*` to mean \"all files in the current directory\". This causes the expression to fail to evaluate unless you are in an empty directory with the `failgl","problematicCode":"f=$(expr \"$c\" * 9 / 5 + 32)","correctCode":"f=$((c * 9 / 5 + 32))"},"SC2307":{"code":"SC2307","title":"'expr' expects 3+ arguments but sees 1. Make sure each operator/operand is a separate argument, and escape <>&|.","fix":"ShellCheck found an `expr` command with 1 or 2 arguments. `expr` normally expects 3 or more. General","rationale":"ShellCheck found an `expr` command with 1 or 2 arguments. `expr` normally expects 3 or more. Generally, this happens for one of two reasons: * You are using an operator like `|`, `&`, `>`, `>=`, `<`, ","problematicCode":"# | not escaped\nexpr 1 | 2\n# > not escaped\nexpr \"$foo\" >= \"$bar\"\n# Missing spaces around +\nexpr 1+2\n# Unexpected quoting around an expression\nexpr \"1 + 2\"","correctCode":"expr 16 \\| 7\nexpr \"$foo\" \\>= \"$bar\"\nexpr 1 + 2"},"SC2308":{"code":"SC2308","title":"`expr length` has unspecified results. Prefer `${#var}`.","fix":"You are using a `expr` with `length`, `match`, `index`, or `substr`. These forms did not make it int","rationale":"You are using a `expr` with `length`, `match`, `index`, or `substr`. These forms did not make it into POSIX, and fail on platforms like MacOS and FreeBSD. Consider replacing them with portable equival","problematicCode":"# Find length of string\nlength=$(expr length \"$var\")\n# Match string against regex\nexpr match \"$input\" \"[0-9]*\"\n# Find character index in string\npos=$(expr index \"$input\" \":\")\n# Get substring by index\n","correctCode":"# Find length of string\nlength=${#var}\n# Match string against regex\nexpr \"$input\" : \"[0-9]*\"\n# Find character index in string\npos=${input%%:*} pos=$((${#pos}+1))\n# Get substring by index (bash)\nstr=\"f"},"SC2309":{"code":"SC2309","title":"-eq treats this as a variable. Use = to compare as string (or expand explicitly with $var)","fix":"ShellCheck found a string used as an argument to a numerical operator like `-eq`, `-ne`, `-lt`, `-ge","rationale":"ShellCheck found a string used as an argument to a numerical operator like `-eq`, `-ne`, `-lt`, `-ge`. Such strings will be treated as arithmetic expressions, meaning `n` will refer to a variable `$n`","problematicCode":"read -p \"Continue? [y/n] \" var\n[ \"$var\" -eq \"n\" ​] && exit 1","correctCode":"#read -p \"Continue? [y/n] \" var\n[ \"$var\" = \"n\" ​] && exit 1"},"SC2310":{"code":"SC2310","title":"This function is invoked in an 'if' condition so set -e will be disabled. Invoke separately if failures should cause the script to exit.","fix":"ShellCheck found a function used as a condition in a script where `set -e` is enabled. This means th","rationale":"ShellCheck found a function used as a condition in a script where `set -e` is enabled. This means that the function will run without `set -e`, and will power through any errors. This applies to `if`, ","problematicCode":"#!/bin/sh\n#shellcheck enable=check-set-e-suppressed\nset -e\nbackup() {\n  cp *.txt /backup\n  rm *.txt            # Runs even if copy fails!\n}\nif backup\nthen\n    echo \"Backup successful\"\nfi","correctCode":"#!/bin/sh\n#shellcheck enable=check-set-e-suppressed\nset -e\nbackup() {\n  cp *.txt /backup\n  rm *.txt\n}\nbackup\necho \"Backup successful\""},"SC2311":{"code":"SC2311","title":"Bash implicitly disabled set -e for this function invocation because it's inside a command substitution. Add set -e; before it or enable inherit_errexit.","fix":"ShellCheck found a Bash function invoked in a command substitution with `set -e` enabled. Unlike oth","rationale":"ShellCheck found a Bash function invoked in a command substitution with `set -e` enabled. Unlike other shells, Bash disables `set -e` in command substitution by default. This means that the function w","problematicCode":"#!/bin/bash\n#shellcheck enable=check-set-e-suppressed\nset -e\ndeploy() {\n    make -j \"$(nproc)\" foo test\n    cp ./foo /var/www/example.com/cgi-bin\n    ./foo --version 2>&1\n}\nversion=$(deploy)\necho \"Suc","correctCode":"#!/bin/bash\n#shellcheck enable=check-set-e-suppressed\nset -e\nshopt -s inherit_errexit\ndeploy() {\n    make -j \"$(nproc)\" foo test\n    cp ./foo /var/www/example.com/cgi-bin\n    ./foo --version 2>&1\n}\nve"},"SC2312":{"code":"SC2312","title":"Consider invoking this command separately to avoid masking its return value (or use '|| true' to ignore).","fix":"In the problematic example, the exit code for `get_chroot_dir` is ignored because it is used in a co","rationale":"In the problematic example, the exit code for `get_chroot_dir` is ignored because it is used in a command substitution in the argument of another command. If the command shows `error: Can't determine ","problematicCode":"set -e\ncd \"$(get_chroot_dir)/etc\"\ntar xf \"${config}\"","correctCode":"set -e\ndir=\"$(get_chroot_dir)\"\ncd \"${dir}/etc\"\ntar xf \"${config}\""},"SC2313":{"code":"SC2313","title":"Quote array indices to avoid them expanding as globs.","fix":"ShellCheck found an array element passed to read, where the `[]` was not quoted. This means the arra","rationale":"ShellCheck found an array element passed to read, where the `[]` was not quoted. This means the array index `[index]` will be treated as a glob range, and the word may be replaced or trigger `failglob","problematicCode":"read -r foo[index]","correctCode":"read -r \"foo[index]\""},"SC2314":{"code":"SC2314","title":"In bats, `!` does not cause a test failure.","fix":"Bats uses `set -e` and `trap ERR` to catch test failures as early as possible. Although the return c","rationale":"Bats uses `set -e` and `trap ERR` to catch test failures as early as possible. Although the return code of a `!` negated command is inverted, they will never trigger `errexit`, due to a bash design de","problematicCode":"#!/usr/bin/env bats\n@test \"test\" {\n    # ... code\n    ! test_file_exists\n    # ... more code\n}","correctCode":"#!/usr/bin/env bats\n@test \"test\" {\n    # ... code\n    run ! test_file_exists\n    # ... more code\n}"},"SC2315":{"code":"SC2315","title":"In bats, `!` does not cause a test failure. Fold the `!` into the conditional!","fix":"Bats uses `set -e` and `trap ERR` to catch test failures as early as possible. Although the return c","rationale":"Bats uses `set -e` and `trap ERR` to catch test failures as early as possible. Although the return code of a `!` negated command is inverted, they will never trigger `errexit`, due to a bash design de","problematicCode":"#!/usr/bin/env bats\n@test \"test\" {\n    # ... code\n    ! [ $status == 0 ]\n    # ... more code\n}","correctCode":"#!/usr/bin/env bats\n@test \"test\" {\n    # ... code\n    [ $status != 0 ]\n    # ... more code\n}"},"SC2316":{"code":"SC2316","title":"This applies local to the variable named readonly, which is probably not what you want. Use a separate command or the appropriate `declare` options instead.","fix":"In most languages, declaration modifiers like `public`/`static`/`const` are keywords and you can app","rationale":"In most languages, declaration modifiers like `public`/`static`/`const` are keywords and you can apply multiple to any declaration. In shell scripting they are instead command names, and anything afte","problematicCode":"local readonly foo=3\nreadonly export bar=4","correctCode":"local foo=3\nreadonly foo\nreadonly bar=4\nexport bar"},"SC2317":{"code":"SC2317","title":"Command appears to be unreachable. Check usage (or ignore if invoked indirectly).","fix":"Remove unreachable code | Use function that returns | Disable: # shellcheck disable=SC2317","rationale":"The problematic code wanted to exit with success if the user explicitly asked for `--help`. However, since the `usage` function already had an `exit 1`, this statement could never run. One possible so","problematicCode":"usage() {\n  echo >&2 \"Usage: $0 -i input\"\n  exit 1\n}     \nif [ \"$1\" = \"--help\" ]\nthen\n  usage\n  exit 0   # Unreachable\nfi","correctCode":"usage() {\n  echo >&2 \"Usage: $0 -i input\"\n}     \nif [ \"$1\" = \"--help\" ]\nthen\n  usage\n  exit 0\nfi"},"SC2318":{"code":"SC2318","title":"This assignment is used again in this `declare`, but won't have taken effect. Use two `declare`s.","fix":"When assigning variables via a command, such as `declare`, `typeset`, `local` etc, the expansion of ","rationale":"When assigning variables via a command, such as `declare`, `typeset`, `local` etc, the expansion of all arguments happen before all assignments. This means that you can't have a variable assigned and ","problematicCode":"declare -i first=$1 current=$first","correctCode":"declare -i first=$1\ndeclare -i current=$first"},"SC2319":{"code":"SC2319","title":"This `$?` refers to a condition, not a command. Assign to a variable to avoid it being overwritten.","fix":"ShellCheck found a `$?` that always refers to a condition like `[ .. ]`, `[[ .. ]]`, or `test`. This","rationale":"ShellCheck found a `$?` that always refers to a condition like `[ .. ]`, `[[ .. ]]`, or `test`. This most commonly happens when trying to inspect `$?` before doing something with it, such as inspectin","problematicCode":"mycommand\nif [ $? -ne 0 ] && [ $? -ne 14 ]\nthen\n  echo \"Command failed\"\nfi","correctCode":"mycommand\nret=$?\nif [ $ret -ne 0 ] && [ $ret -ne 14 ]\nthen\n  echo \"Command failed\"\nfi"},"SC2320":{"code":"SC2320","title":"This `$?` refers to echo/printf, not a previous command. Assign to variable to avoid it being overwritten.","fix":"ShellCheck found a `$?` that always refers to `echo` or `printf`. This most commonly happens when tr","rationale":"ShellCheck found a `$?` that always refers to `echo` or `printf`. This most commonly happens when trying to show `$?` before doing something with it, without realizing that any such action will also o","problematicCode":"mycommand\necho \"Command exited with $?\"\nif [ $? -ne 0 ]\nthen\n  echo \"Failed\"\nfi","correctCode":"mycommand\nret=$?\necho \"Command exited with $ret\"\nif [ $ret -ne 0 ]\nthen\n  echo \"Failed\"\nfi"},"SC2321":{"code":"SC2321","title":"Array indices are already arithmetic contexts. Prefer removing the `$((` and `))`.","fix":"In indexed arrays (but not associative ones), the array index is already an arithmetic context. Ther","rationale":"In indexed arrays (but not associative ones), the array index is already an arithmetic context. There is no point or value in wrapping it in an additional, explicit `$((..))`.","problematicCode":"values[$((i+1))]=1","correctCode":"values[i+1]=1"},"SC2322":{"code":"SC2322","title":"In arithmetic contexts, `((x))` is the same as `(x)`. Prefer only one layer of parentheses.","fix":"ShellCheck found doubly nested parentheses in an arithmetic expression. While the syntax for an arit","rationale":"ShellCheck found doubly nested parentheses in an arithmetic expression. While the syntax for an arithmetic expansion is `$((..))`, this does not imply that parentheses in the expression should also be","problematicCode":"value=$(( ((offset + index)) * 512 ))","correctCode":"value=$(( (offset + index) * 512 ))"},"SC2323":{"code":"SC2323","title":"`a[(x)]` is the same as `a[x]`. Prefer not wrapping in additional parentheses.","fix":"ShellCheck found an entire arithmetic expression wrapped in parentheses. This does not serve a purpo","rationale":"ShellCheck found an entire arithmetic expression wrapped in parentheses. This does not serve a purpose since the expression is already clearly delimited by the construct it's in, such as `array[..]=` ","problematicCode":"array[(x+1)]=val\necho $(( (x+1) ))","correctCode":"array[x+1]=val\necho $((x+1))"},"SC2324":{"code":"SC2324","title":"var+=1 will append, not increment. Use (( var += 1 )), declare -i var, or quote number to silence.","fix":"The problematic code attempts to add 2 and 3 to get 5. Instead, `+=` on a string variable will conca","rationale":"The problematic code attempts to add 2 and 3 to get 5. Instead, `+=` on a string variable will concatenate, so the result is 23.","problematicCode":"var=2 n=3\nvar+=$n","correctCode":"(( var += n ))"},"SC2325":{"code":"SC2325","title":"Multiple ! in front of pipelines are a bash/ksh extension. Use only 0 or 1.","fix":"POSIX (and Dash) does not allow multiple `!` pipeline negations in a row. It's also logically unnece","rationale":"POSIX (and Dash) does not allow multiple `!` pipeline negations in a row. It's also logically unnecessary. Use either zero or one `!`.","problematicCode":"#!/bin/sh\n! ! true","correctCode":"#!/bin/sh\ntrue"},"SC2326":{"code":"SC2326","title":"! is not allowed in the middle of pipelines. Use command group as in `cmd | { ! cmd; }` if necessary.","fix":"[POSIX specifies](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html#tag_18_0","rationale":"[POSIX specifies](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html#tag_18_09_02) that a status negation operator `!` is only used to negate the status of an entire pipeline, n","problematicCode":"cat | ! tee /dev/full","correctCode":"! cat | tee /dev/full"},"SC2327":{"code":"SC2327","title":"This command substitution will be empty because the command's output gets redirected away.","fix":"This command substitution will be empty because the command's output gets redirected away.","rationale":"","problematicCode":"","correctCode":""},"SC2328":{"code":"SC2328","title":"This redirection takes output away from the command substitution.","fix":"ShellCheck has found a command substitution (`$(..)`, `` `..` ``) that appears to never capture any ","rationale":"ShellCheck has found a command substitution (`$(..)`, `` `..` ``) that appears to never capture any output because the command's output is being redirected. Decide whether you want the output to be ca","problematicCode":"var=$(tr -d ':' < input.txt > output.txt)","correctCode":"# If the output should be captured INSTEAD OF being written to file\nvar=$(tr -d ':' < input.txt)\n# If the output should be captured IN ADDITION to being written to file\nvar=$(tr -d ':' < input.txt | t"},"SC2329":{"code":"SC2329","title":"This function is never invoked. Check usage (or ignored if invoked indirectly).","fix":"ShellCheck found a function that goes out of scope before it's ever invoked. Verify that the functio","rationale":"ShellCheck found a function that goes out of scope before it's ever invoked. Verify that the function is called. It could be misspelled, or its invocation could also be unreachable (as in `f() { ..; }","problematicCode":"#!/bin/sh\nf() {\n  echo \"Hello World\"\n}\nexit","correctCode":"#!/bin/sh\nf() {\n  echo \"Hello World\"\n}\nf\nexit"},"SC2330":{"code":"SC2330","title":"BusyBox `[[ .. ]]` does not support glob matching. Use a case statement.","fix":"You are using `[[ .. ]]` in BusyBox `sh` to match against a glob pattern. This is supported in Bash ","rationale":"You are using `[[ .. ]]` in BusyBox `sh` to match against a glob pattern. This is supported in Bash and Ksh, but not in BusyBox. Rewrite the match to use a `case` statement instead.","problematicCode":"#!/bin/busybox sh\nif [[ $1 == https:* ]]\nthen\n  echo \"Using URL $1\"\nfi","correctCode":"#!/bin/busybox sh\ncase \"$1\" in\n  https:*)\n    echo \"Using URL $1\" \n    ;;\nesac"},"SC2331":{"code":"SC2331","title":"For file existence, prefer standard -e over legacy -a.","fix":"The POSIX standard way to check whether a file exists is `[ -e file ]`. Bash and Ksh have historical","rationale":"The POSIX standard way to check whether a file exists is `[ -e file ]`. Bash and Ksh have historically allowed using `-a`, but this has no benefit and some potential downsides. For example, in Bash `[","problematicCode":"if [ -a ~/.bash_aliases ]\nthen\n  source ~/.bash_aliases\nfi","correctCode":"if [ -e ~/.bash_aliases ]\nthen\n  source ~/.bash_aliases\nfi"},"SC2332":{"code":"SC2332","title":"[ ! -o opt ] is always true because -o becomes logical OR. Use [[ ]] or ! [ -o opt ].","fix":"Bash interprets `[ ! -o opt ]` as `[ \"!\" ] || [ \"opt\" ]` instead of negating the condition. As a res","rationale":"Bash interprets `[ ! -o opt ]` as `[ \"!\" ] || [ \"opt\" ]` instead of negating the condition. As a result, the condition is always true. Avoid this by using `[[ ! -o opt ]]` or `! [ -o opt ]`. The same ","problematicCode":"if [ ! -o braceexpand ]\nthen \n  ..\nfi","correctCode":"if [[ ! -o braceexpand ]]\nthen \n  ..\nfi"},"SC2333":{"code":"SC2333","title":"You probably wanted || here, otherwise it's always false.","fix":"The problematic condition will never trigger because of a logic error. In English one think say \"I w","rationale":"The problematic condition will never trigger because of a logic error. In English one think say \"I want to check for the answers `yes` and `auto`\", but the test ends up checking that the answer is bot","problematicCode":"if [[ $answer == yes && $answer = auto ]]\nthen\n  echo \"Feature Enabled\"\nfi","correctCode":"if [[ $answer == yes || $answer = auto ]]\nthen\n  echo \"Feature Enabled\"\nfi"},"SC2334":{"code":"SC2334","title":"You probably wanted || here, otherwise it's always false.","fix":"The problematic condition will never trigger because of a logic error. In English one think say \"I w","rationale":"The problematic condition will never trigger because of a logic error. In English one think say \"I want to check for the numbers `137` and `143`\", but the test ends up checking that the number is both","problematicCode":"if (( ret == 137 && ret == 143 ))\nthen\n  echo \"Process killed by SIGTERM/SIGKILL\"\nfi","correctCode":"if (( ret == 137 || ret == 143 ))\nthen\n  echo \"Process killed by SIGTERM/SIGKILL\"\nfi"},"SC2335":{"code":"SC2335","title":"Use `[ \"$var\" -ne 1 ]` instead of `[ ! \"$var\" -eq 1 ]`","fix":"Double negation of such binary operators is unnecessary.","rationale":"Double negation of such binary operators is unnecessary.","problematicCode":"if [ ! \"$var\" -eq 1 ]; then :; fi\nif ! [ \"$var\" = foo ]; then :; fi","correctCode":"if [ \"$var\" -ne 1 ]; then :; fi\nif [ \"$var\" != foo ]; then :; fi"},"SC3001":{"code":"SC3001","title":"In POSIX sh, process substitution is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Process substitution is a ksh and bash extension. It does not work in sh or dash scripts.","problematicCode":"#!/bin/sh\nwhile IFS= read -r n\ndo\n  sum=$((sum+n))\ndone < <(program)","correctCode":"#!/bin/bash\nwhile IFS= read -r n\ndo\n  sum=$((sum+n))\ndone < <(program)"},"SC3002":{"code":"SC3002","title":"In POSIX sh, extglob is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Extglobs are extensions in bash and ksh, while your shebang says you're using `sh` or `dash`.","problematicCode":"#!/bin/sh\nrm !(*.hs)","correctCode":"#!/bin/sh\nfor file in *\ndo\n  case \"$file\" in\n    *.hs) true;;\n    *) rm \"$file\" ;;\n  esac\ndone"},"SC3003":{"code":"SC3003","title":"In POSIX sh, `$'..'` is undefined.","fix":"ANSI-C quoting, `$'..'`, is a `bash` extension, which is not supported by POSIX `sh`. To ensure the ","rationale":"ANSI-C quoting, `$'..'`, is a `bash` extension, which is not supported by POSIX `sh`. To ensure the script runs correctly on other systems, either switch to Bash, or rewrite it in a POSIX compatible w","problematicCode":"#!/bin/sh\nIFS=$' \\t\\n'","correctCode":"#!/bin/sh\n# Note: \\n can not be last, or it will be stripped by $()\nIFS=$(printf ' \\n\\t')"},"SC3004":{"code":"SC3004","title":"In POSIX sh, $\"..\" is undefined","fix":"Bash allows using `$\"...\"` to easily get a translated string according to the current locale. This i","rationale":"Bash allows using `$\"...\"` to easily get a translated string according to the current locale. This is powered by the gettext infrastructure. Neither `$\"..\"` nor the gettext infrastructure or the `gett","problematicCode":"printf $\"Hello, %s\\n\" \"$USER\"","correctCode":"# Check if gettext exists\nif ! type gettext > /dev/null 2>&1\nthen\n  # If not, create a dummy function that returns the input verbatim\n  gettext() { printf '%s' \"$1\"; }\nfi\n# Use gettext to get a transl"},"SC3005":{"code":"SC3005","title":"In POSIX sh, arithmetic for loops are undefined.","fix":"C-style arithmetic for loops are a Ksh/Bash feature that's not supported by POSIX sh or dash. Use a ","rationale":"C-style arithmetic for loops are a Ksh/Bash feature that's not supported by POSIX sh or dash. Use a `while` loop with separate initialization and incrementing instead.","problematicCode":"for ((i=0; i<10; i++))\ndo \n  echo \"$i\"\ndone","correctCode":"i=0\nwhile [ \"$i\" -lt 10 ]\ndo \n  echo \"$i\"\n  i=$((i+1))\ndone"},"SC3006":{"code":"SC3006","title":"In POSIX sh, standalone `((..))` is undefined.","fix":"In POSIX sh, standalone `((..))` is undefined.","rationale":"","problematicCode":"variable=1\nif ((variable)); then\n  echo variable is not zero\nfi","correctCode":"variable=1\nif [ \"${variable}\" != 0 ]; then\n  echo variable is not zero\nfi"},"SC3007":{"code":"SC3007","title":"In POSIX sh, `$[..]` in place of `$((..))` is undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, `$[ .. ]` can easi","problematicCode":"var=$[1 + 2]","correctCode":"var=$((1 + 2))"},"SC3008":{"code":"SC3008","title":"In POSIX sh, select loops are undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. There is no simple substitution ","problematicCode":"#!/bin/sh\necho \"Select file to show\"\nselect input in *\ndo\n  ls -l \"$input\"\ndone","correctCode":"#!/usr/bin/env bash\necho \"Select file to show\"\nselect input in *\ndo\n  ls -l \"$input\"\ndone"},"SC3009":{"code":"SC3009","title":"In POSIX `sh`, brace expansion is undefined.","fix":"In POSIX `sh`, brace expansion is undefined.","rationale":"","problematicCode":"#!/bin/sh\nfor i in {1..5}; do ...","correctCode":"#!/bin/sh\ni=1\n# while $i ≤ 5 ...\nwhile [ \"${i}\" -le 5 ]; do ...\n  # something that will occur 5 times\n  i=$((i + 1))\ndone"},"SC3010":{"code":"SC3010","title":"In POSIX sh, `[[ ]]` is undefined.","fix":"In POSIX sh, `[[ ]]` is undefined.","rationale":"","problematicCode":"","correctCode":""},"SC3011":{"code":"SC3011","title":"In POSIX sh, here-strings are undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this specific case, the here-","problematicCode":"#!/bin/sh\nwc <<< \"$1\"","correctCode":"#!/bin/sh\nwc << EOF\n$1\nEOF"},"SC3012":{"code":"SC3012","title":"In POSIX sh, lexicographical `\\<` is undefined.","fix":"The `test` binary operators `>`, `\\>`, `<`, and `\\<` are not part of POSIX and not guaranteed to be ","rationale":"The `test` binary operators `>`, `\\>`, `<`, and `\\<` are not part of POSIX and not guaranteed to be supported in scripts targeting `sh`. The `expr` functionality is specified by POSIX.","problematicCode":"#!/bin/sh\nx=\"aardvark\"\ny=\"zebra\"\nif [ $x \\< $y ]\nthen\n  echo \"$x comes before $y in the dictionary\"\nfi","correctCode":"#!/bin/sh\nx=\"aardvark\"\ny=\"zebra\"\nif expr \"'$x\" \\< \"'$y\" > /dev/null\nthen\n  echo \"$x comes before $y in the dictionary\"\nfi"},"SC3013":{"code":"SC3013","title":"In POSIX sh, `-nt` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`test -nt` is an extension in ksh, bash and dash, but it is [not specified by POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/test.html).","problematicCode":"#!/bin/sh\nif [ Foo.java -nt Foo.class ]\nthen\n  javac Foo.java\nfi","correctCode":"#!/bin/bash\nif [ Foo.java -nt Foo.class ]\nthen\n  javac Foo.java\nfi"},"SC3014":{"code":"SC3014","title":"In POSIX sh, `==` in place of `=` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`==` is used for equality comparison in C-like languages, and is also supported by `bash` and `ksh` in `[ .. ]` and `test` expressions. `sh` and `dash` only supports `=` for equality comparison, so us","problematicCode":"#!/bin/sh\nif [ $1 == \"-n\" ]\nthen\n  dry_run=1\nfi","correctCode":"#!/bin/sh\nif [ $1 = \"-n\" ]\nthen\n  dry_run=1\nfi"},"SC3015":{"code":"SC3015","title":"In POSIX sh, `=~` regex matching is undefined.","fix":"You are using `=~` in a script declared to be compatible with POSIX sh or Dash, but `=~` is [not spe","rationale":"You are using `=~` in a script declared to be compatible with POSIX sh or Dash, but `=~` is [not specified by POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/test.html) and is unlike","problematicCode":"#!/bin/sh\nif [ \"$var\" =~ foo[0-9]+ ]; then\n  echo matched\nfi","correctCode":"#!/bin/sh\n# Use the x-hack to handle strings like '('.\nif expr \"X$var\" : 'X.*foo[0-9]\\{1,\\}' >/dev/null; then\n  echo matched\nfi"},"SC3016":{"code":"SC3016","title":"In POSIX sh, unary `-v` (in place of `[ -n \"${var+x}\" ]`) is undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, `[ -v variable ]`,","problematicCode":"#!/bin/sh\nif [ -v STY ]\nthen\n  echo \"STY is set, you are using screen\"\nfi","correctCode":"#!/bin/sh\nif [ -n \"${STY+x}\" ]\nthen\n  echo \"STY is set, you are using screen\"\nfi"},"SC3017":{"code":"SC3017","title":"In POSIX sh, unary `-a` in place of `-e` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`-a` was used in some shells to check for the existence of a file, but the POSIX standard and better supported equivalent is `-e`.","problematicCode":"#!/bin/sh\nif [ -a file.txt ]\nthen\n  echo \"file.txt exists\"\nfi","correctCode":"#!/bin/sh\nif [ -e file.txt ]\nthen\n  echo \"file.txt exists\"\nfi"},"SC3018":{"code":"SC3018","title":"In POSIX sh, `++` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Prefix and postfix increment and decrement are extensions in bash and ksh. They're not supported in dash or POSIX sh, which is what your shebang says you're using.","problematicCode":"#!/bin/sh\ni=1\necho \"$((i++))\"","correctCode":"#!/bin/sh\ni=1\necho \"$i\"\ni=$((i+1))"},"SC3019":{"code":"SC3019","title":"In POSIX sh, exponentials are undefined.","fix":"In POSIX sh, exponentials are undefined.","rationale":"","problematicCode":"#!/bin/sh\necho $((2 ** 3)) # Using `**` for an exponent is undefined in POSIX sh.","correctCode":"#!/bin/sh\necho $((2 * 2 * 2))   # equivalent to `bash`'s `echo $((2 ** 3))`\necho '2 ^ 3' | bc     # piping the formula to `bc` to parse the output https://stackoverflow.com/a/13111995\nprintf '2 ^ 3\\n'"},"SC3020":{"code":"SC3020","title":"In POSIX sh, `&>` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`&>` is a bash and ksh extension for redirecting both stdout and stderr. In dash and POSIX sh, use `2>&1` explicitly.","problematicCode":"#!/bin/sh\nssh host cmd &> log","correctCode":"#!/bin/sh\nssh host cmd > log 2>&1"},"SC3021":{"code":"SC3021","title":"In POSIX sh, `>& filename` (as opposed to `>& fd`) is undefined.","fix":"There are two forms of the `>&` file descriptor operator: * `>& integer` such as `>& 3` is a POSIX c","rationale":"There are two forms of the `>&` file descriptor operator: * `>& integer` such as `>& 3` is a POSIX compatible synonym for `1>&3` * `>& filename` such as `>& log.txt` is a Bash specific synonym for `> ","problematicCode":"# Writing to a filename\nmycommand >& log.txt","correctCode":"mycommand > log.txt 2>&1"},"SC3022":{"code":"SC3022","title":"In POSIX sh, named file descriptors is undefined.","fix":"The syntax for opening an arbitrary file descriptor and assigning it to a variable for later use is ","rationale":"The syntax for opening an arbitrary file descriptor and assigning it to a variable for later use is a `bash` and `ksh` extension. It does not work on POSIX sh or `dash`.","problematicCode":"exec {n}> \"$output\"\necho \"Your fd is $n\"","correctCode":""},"SC3023":{"code":"SC3023","title":"In POSIX sh, FDs outside 0-9 are undefined.","fix":"The shell grammar is only required to recognize a single digit before redirections to set the file d","rationale":"The shell grammar is only required to recognize a single digit before redirections to set the file descriptor, which limits the portable range to 0-9.","problematicCode":"# Open FD #10 for writing\nexec 10> file","correctCode":""},"SC3024":{"code":"SC3024","title":"In POSIX sh, `+=` is undefined.","fix":"Using `+=` to concatenate to an existing variable is a ksh/bash extension. For POSIX sh or dash, wri","rationale":"Using `+=` to concatenate to an existing variable is a ksh/bash extension. For POSIX sh or dash, write out the full expression. Be careful to use braces if the text you append could be confused for a ","problematicCode":"var=\"Hello \"\nvar+=\"World\"","correctCode":"var=\"Hello \"\nvar=\"${var}World\""},"SC3025":{"code":"SC3025","title":"In POSIX sh, `/dev/{tcp,udp}` is undefined.","fix":"`/dev/tcp/$host/$port` and `/dev/udp/$host/$port` are recognized in redirections by bash and ksh, an","rationale":"`/dev/tcp/$host/$port` and `/dev/udp/$host/$port` are recognized in redirections by bash and ksh, and a socket connection is made instead of opening a file. They do not physically exist in `/dev`. POS","problematicCode":"echo foo > /dev/tcp/myhost/1234","correctCode":"echo foo | nc myhost 1234"},"SC3026":{"code":"SC3026","title":"In POSIX sh, `^` in place of `!` in glob bracket expressions is undefined.","fix":"`[^c]` is frequently used in most regular expression variants to mean \"any character except `c`\". Ks","rationale":"`[^c]` is frequently used in most regular expression variants to mean \"any character except `c`\". Ksh and Bash adopted it for globs as well. However, strictly speaking, the only range complement synta","problematicCode":"echo foo-[^0]*.jpg","correctCode":"echo foo-[!0]*.jpg"},"SC3028":{"code":"SC3028","title":"In POSIX sh, VARIABLE is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"The variable you are attempting to use is a special variable in bash or ksh. To get the same information from `dash` or POSIX `sh`, use an external command instead. For `PIPESTATUS`, the pipeline can ","problematicCode":"#!/bin/sh\necho \"$HOSTNAME $UID $RANDOM\"","correctCode":"#!/bin/sh\necho \"$(hostname) $(id -u) $(awk 'BEGIN { srand(); print int(rand()*32768) }' /dev/null)\""},"SC3029":{"code":"SC3029","title":"In POSIX sh, `|&` in place of `2>&1 |` is undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, the non-portable s","problematicCode":"#!/bin/sh\nmake |& tee ~/log","correctCode":"#!/bin/sh\nmake 2>&1 | tee ~/log"},"SC3030":{"code":"SC3030","title":"In POSIX sh, arrays are undefined.","fix":"Arrays are specific to Bash and Ksh, but you are using them in a script declared to run with `sh` or","rationale":"Arrays are specific to Bash and Ksh, but you are using them in a script declared to run with `sh` or `dash`. These shells do not support arrays at all. You should either switch to Bash/Ksh, or rewrite","problematicCode":"#!/bin/sh\nx=( foo bar \"Hello World\" )\nfor word in \"${x[@]}\"\ndo\n  touch \"$word.dat\"\ndone","correctCode":"set -- foo bar \"Hello World\"\nfor word in \"$@\"\ndo\n  touch \"$word.dat\"\ndone"},"SC3031":{"code":"SC3031","title":"In POSIX sh, redirecting from/to globs is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Shells differ in whether (and how) they do pathname expansion on names in redirections. Avoid the issue by expanding the glob yourself.","problematicCode":"#!/bin/sh\ndate > /tmp/log*.txt","correctCode":"#!/bin/sh\nset -- /tmp/log*.txt\n# PS: This is a good place to verify that there was exactly one match\ndate > \"$1\""},"SC3032":{"code":"SC3032","title":"In POSIX sh, coproc is undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. There is no simple substitution ","problematicCode":"#!/bin/sh\ncoproc nc shellcheck.net 80","correctCode":"#!/usr/bin/env bash\ncoproc nc shellcheck.net 80"},"SC3033":{"code":"SC3033","title":"In POSIX sh, naming functions outside [a-zA-Z_][a-zA-Z0-9_]* is undefined.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, you can rename you","problematicCode":"#!/bin/sh\nrun-tests() {\n  make test\n}","correctCode":"#!/bin/sh\nrun_tests() {\n  make test\n}"},"SC3034":{"code":"SC3034","title":"In POSIX sh, `$(<file)` is undefined.","fix":"In bash and ksh, `$(< file)` will expand to the full contents of the file. However, in dash and POSI","rationale":"In bash and ksh, `$(< file)` will expand to the full contents of the file. However, in dash and POSIX sh, it will instead expand to nothing. This is because it's not recognized as special, and simply ","problematicCode":"content=$(<file)","correctCode":"content=$(cat file)"},"SC3035":{"code":"SC3035","title":"In POSIX sh, `` `<file` `` is undefined.","fix":"In POSIX sh, `` `<file` `` is undefined.","rationale":"","problematicCode":"","correctCode":""},"SC3036":{"code":"SC3036","title":"In Dash, echo flags besides -n are not supported.","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, you can rewrite yo","problematicCode":"#!/bin/dash\necho -e \"Name:\\t$USER\"","correctCode":"#!/bin/dash\nprintf 'Name:\\t%s\\n' \"$USER\""},"SC3037":{"code":"SC3037","title":"In POSIX sh, echo flags are undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`echo` has historically behaved differently on different systems. Use `printf` instead to ensure compatibility between shells.","problematicCode":"#!/bin/sh\necho -ne 'Foo:\\tBar'","correctCode":"#!/bin/sh\nprintf 'Foo:\\tBar'"},"SC3038":{"code":"SC3038","title":"In POSIX sh, exec flags are undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Flags to `exec` are bash and ksh extensions. `dash` and POSIX `sh` does not support them.","problematicCode":"#!/bin/sh\nexec -ca ls busybox","correctCode":"#!/bin/bash\nexec -ca ls busybox"},"SC3039":{"code":"SC3039","title":"In POSIX sh, `let` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`let` is a command used for arithmetic in some shells, but it's not a standard command. Use `$((..))` instead.","problematicCode":"#!/bin/sh\nlet x=2+2","correctCode":"#!/bin/sh\nx=$((2+2))"},"SC3040":{"code":"SC3040","title":"In POSIX sh, set option *[name]* is undefined.","fix":"In a script declared to run with `sh` or `dash`, ShellCheck found `set -o` used with an option that ","rationale":"In a script declared to run with `sh` or `dash`, ShellCheck found `set -o` used with an option that is not defined by POSIX. The option may not be supported by these shells.","problematicCode":"#!/bin/sh\nset -o pipefail\nif cmd1 | cmd2 | cmd3\nthen\n  echo \"Success\"\nfi","correctCode":"fail=\"$(mktemp)\"\nif { cmd1 || echo > \"$fail\"; } |\n   { cmd2 || echo > \"$fail\"; } |\n     cmd3 && [ ! -s \"$fail\" ]\nthen\n  echo \"Success\"\nfi\nrm \"$fail\""},"SC3041":{"code":"SC3041","title":"In POSIX sh, set flag `-E` is undefined","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. Most shell specific `set` flags ","problematicCode":"#!/bin/sh\nset -EB","correctCode":"#!/bin/bash\nset -EB"},"SC3042":{"code":"SC3042","title":"In POSIX sh, set flag `--default` is undefined","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Ksh. Most shell specific `set` flags a","problematicCode":"#!/bin/ksh\nset --default","correctCode":"#!/bin/ksh\nset --default"},"SC3043":{"code":"SC3043","title":"In POSIX sh, `local` is undefined.","fix":"`local` is supported in many shells, including bash, ksh, dash, and BusyBox ash. However, [it is not","rationale":"`local` is supported in many shells, including bash, ksh, dash, and BusyBox ash. However, [it is not specified by POSIX](https://austingroupbugs.net/view.php?id=767).","problematicCode":"myfunc() {\n  local i=0\n  ..\n}","correctCode":"myfunc() {\n  _myfunc_i=0\n  ..\n}"},"SC3044":{"code":"SC3044","title":"In POSIX sh, `declare` is undefined.","fix":"The `declare` command is non-standard, and most of its functionality is not available across shells.","rationale":"The `declare` command is non-standard, and most of its functionality is not available across shells. Either find a POSIX replacement, or switch to a shell that is guaranteed to support them.","problematicCode":"#!/bin/sh\ndeclare var=\"value\"","correctCode":"var=\"value\""},"SC3045":{"code":"SC3045","title":"In POSIX sh, some-command-with-flag is undefined.","fix":"External commands (`grep`, `ls`, `find`) invoke a binary on the system and therefore accept the same","rationale":"External commands (`grep`, `ls`, `find`) invoke a binary on the system and therefore accept the same flags from all shells. However, some commands are instead built into the shell and therefore accept","problematicCode":"#!/bin/sh\nread   -e   # Using libreadline\nexport -f   # Exporting functions\nulimit -v   # Setting vspace limits\nwait   -n   # Waiting for a single process","correctCode":""},"SC3046":{"code":"SC3046","title":"In POSIX sh, `source` in place of `.` is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"`source` is a bash and ksh specific alias for the harder-to-pronounce/search command `.` (\"dot\") When writing for `dash` or `sh`, use `.` instead.","problematicCode":"#!/bin/sh\nsource mylib.sh","correctCode":"#!/bin/sh\n. mylib.sh"},"SC3047":{"code":"SC3047","title":"In POSIX sh, trapping ERR is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"You are trying to install a trap that is not supported by your current shell (`dash` or POSIX `sh`). There is no simple replacement. The script should be rewritten to avoid depending on the trap, or s","problematicCode":"#!/bin/sh\ntrap 'Command failed with $?' ERR","correctCode":"#!/bin/bash\ntrap 'Command failed with $?' ERR"},"SC3048":{"code":"SC3048","title":"In POSIX sh, prefixing signal names with 'SIG' is undefined.","fix":"POSIX does not allow specifying the \"SIG\" prefix of a signal name like \"INT\". Bash and Ksh supports ","rationale":"POSIX does not allow specifying the \"SIG\" prefix of a signal name like \"INT\". Bash and Ksh supports this as an extension. Simply remove the \"SIG\" from the signal name.","problematicCode":"trap 'handle_interrupt' SIGINT","correctCode":"trap 'handle_interrupt' INT"},"SC3049":{"code":"SC3049","title":"In POSIX sh, using lower/mixed case for signal names is undefined.","fix":"POSIX requires that signal names [be given in uppercase](https://pubs.opengroup.org/onlinepubs/96999","rationale":"POSIX requires that signal names [be given in uppercase](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html#tag_18_28): >The condition can be EXIT, 0 (equivalent to EXIT), or a ","problematicCode":"trap 'handle_sigint' int","correctCode":"trap 'handle_sigint' INT"},"SC3050":{"code":"SC3050","title":"In POSIX sh, `printf %q` is undefined.","fix":"`printf %q` is a bash and ksh extension. It's not supported on POSIX sh or dash.","rationale":"`printf %q` is a bash and ksh extension. It's not supported on POSIX sh or dash.","problematicCode":"ssh host \"cat $(printf \"%q\" \"$remotefile\")\"","correctCode":"escape() { printf \"'%s'\\\\n\" \"$(printf '%s' \"$1\" | sed -e \"s/'/'\\\\\\\\''/g\")\"; }\nssh host \"cat $(escape \"$remotefile\")\""},"SC3051":{"code":"SC3051","title":"In POSIX sh, `source` in place of `.` is undefined","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. `source` is a Bash/Ksh specific,","problematicCode":"#!/bin/sh\nsource /usr/lib/myfile.sh","correctCode":"#!/bin/sh\n. /usr/lib/myfile.sh"},"SC3052":{"code":"SC3052","title":"In POSIX sh, arithmetic base conversion is undefined","fix":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be porta","rationale":"Your script uses a shell feature not supported by the shebang. Either rewrite the script to be portable, or change the shebang to explicitly require a shell like Bash. In this case, using radix specif","problematicCode":"#!/bin/sh\necho \"Hex $1 is decimal $((16#$1))\"","correctCode":"#!/bin/sh\necho \"Hex $1 is decimal $(echo \"ibase=16; $1\" | bc)\""},"SC3053":{"code":"SC3053","title":"In POSIX sh, indirect expansion is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Indirection expansion is an extension in `bash` and `ksh`, and not supported in `dash` or POSIX `sh`. Either switch to a shell that supports them, or write around it with careful use of `eval`. Take c","problematicCode":"#!/bin/sh\nname=\"PATH\"\necho \"${!name}\"","correctCode":"#!/bin/bash\nname=\"PATH\"\necho \"${!name}\""},"SC3054":{"code":"SC3054","title":"In POSIX sh, array references are undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Arrays are supported in `bash` and `ksh`, but not in `dash` or POSIX `sh`. Either switch to a shell that supports them, or rewrite your script without relying on arrays. This may not be straight forwa","problematicCode":"#!/bin/sh\nvar=(foo bar)\necho \"${var[1]}\"","correctCode":"#!/bin/bash\nvar=(foo bar)\necho \"${var[1]}\""},"SC3055":{"code":"SC3055","title":"In POSIX sh, array key expansion is undefined.","fix":"In POSIX sh, array key expansion is undefined.","rationale":"","problematicCode":"","correctCode":""},"SC3056":{"code":"SC3056","title":"In POSIX sh, name matching prefixes are undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Using `${!name*}` to find all variable names with a given prefix is a bash and ksh extension that `dash` and POSIX `sh` does not support.","problematicCode":"#!/bin/sh\necho \"${!ANDROID_*}\"","correctCode":"#!/bin/bash\necho \"${!ANDROID_*}\""},"SC3057":{"code":"SC3057","title":"In POSIX sh, string indexing is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"String indexing is a bash and ksh extension, and does not work in `dash` or POSIX `sh`.","problematicCode":"#!/bin/sh\necho \"Your initial is ${USER:0:1}\"","correctCode":"#!/bin/sh\necho \"Your initial is $(printf '%s' \"$USER\" | cut -c 1)\""},"SC3059":{"code":"SC3059","title":"In POSIX sh, case modification is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"Case modification in parameter expansion (e.g., `${x^^}`, `${x,,}`, `${x^}`, `${x,[pattern]}`) is a bash and ksh extension. dash and POSIX sh do not support it.","problematicCode":"#!/bin/sh\nx='test'\necho \"${x^^[t]}\"","correctCode":"#!/bin/bash\nx='test'\necho \"${x^^[t]}\""},"SC3060":{"code":"SC3060","title":"In POSIX sh, string replacement is undefined.","fix":"# shellcheck disable=SC3000-SC4000","rationale":"String replacement in parameter expansion is a `bash` and `ksh` extension. `dash` and POSIX `sh` does not support it.","problematicCode":"#!/bin/sh\necho \"${var//foo/bar}\"","correctCode":"#!/bin/sh\necho \"$var\" | sed 's/foo/bar/g'"},"SC3061":{"code":"SC3061","title":"In POSIX sh, `read` without a variable is undefined.","fix":"This behavior is not allowed [in POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/r","rationale":"This behavior is not allowed [in POSIX](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/read.html).","problematicCode":"while read -r;\ndo\n    echo \"line: ${REPLY}\"\ndone < foolist","correctCode":"while read -r foo;\ndo\n    echo \"line: ${foo}\"\ndone < foolist"},"SC3062":{"code":"SC3062","title":"In POSIX sh, unary -o to check options is undefined.","fix":"`[ -o option ]` is a bash/ksh extension, while `$-` is standard POSIX. Do note that letters outside ","rationale":"`[ -o option ]` is a bash/ksh extension, while `$-` is standard POSIX. Do note that letters outside the POSIX set are not guaranteed to be compatible, such as `B` above.","problematicCode":"if [ -o braceexpand ]\nthen\n echo \"Brace expansion available and enabled.\"\nfi","correctCode":"case $- of\n  *B*) echo \"Brace expansion available and enabled.\"\nesac"},"SC3063":{"code":"SC3063","title":"In POSIX sh, test -R and namerefs in general are undefined.","fix":"`[ -R name ]` is used to check whether `name` is a `nameref`. Since POSIX sh has no namerefs, either","rationale":"`[ -R name ]` is used to check whether `name` is a `nameref`. Since POSIX sh has no namerefs, either use a shell that does, or rewrite to not need namerefs.","problematicCode":"if [ -R foo ]\nthen\n  ..\nfi","correctCode":""},"SC3064":{"code":"SC3064","title":"In POSIX sh, test -N is undefined.","fix":"`test -N` is not supported by POSIX, and there is no straight forward way of simulating it. If you n","rationale":"`test -N` is not supported by POSIX, and there is no straight forward way of simulating it. If you need this functionality, look into which non-POSIX features your system has to allow this. `stat -c`,","problematicCode":"if [ -N file ]\nthen\n  echo \"File has been modified since last read\"\nfi","correctCode":"if bash -c '[ -N file ]'\nthen\n  echo \"File has been modified since last read\"\nfi"},"SC3065":{"code":"SC3065","title":"In POSIX sh, test -k is undefined.","fix":"`test -k file` and `[ -k file ]` are not defined by POSIX. To ensure compatibility with all target s","rationale":"`test -k file` and `[ -k file ]` are not defined by POSIX. To ensure compatibility with all target systems, use `find` instead.","problematicCode":"if [ -k file ]\nthen\n  echo \"File has sticky bit set\"\nfi","correctCode":"if [ -n \"$(find file -prune -perm /1000)\" ]\nthen\n  echo \"File has sticky bit set\"\nfi"},"SC3066":{"code":"SC3066","title":"In POSIX sh, test -G is undefined.","fix":"`test -G` is a bash/ksh/dash/ash extension to check whether the file is owned by your effective grou","rationale":"`test -G` is a bash/ksh/dash/ash extension to check whether the file is owned by your effective group. To ensure compatibility with other shells, you can use `find -group \"$(id -g)\"`.","problematicCode":"if [ -G file ]\nthen\n  echo \"File is owned by your effective group\"\nfi","correctCode":"if [ -n \"$(find file -prune -group \"$(id -g)\")\" ]\nthen\n  echo \"File is owned by your effective group\"\nfi"},"SC3067":{"code":"SC3067","title":"In POSIX sh, test -O is undefined.","fix":"`test -O` is a bash/ksh/dash/ash extension to check whether the file is owned by you. To ensure comp","rationale":"`test -O` is a bash/ksh/dash/ash extension to check whether the file is owned by you. To ensure compatibility with other shells, you can use `find -user \"$(id -u)\"`.","problematicCode":"if [ -O file ]\nthen\n  echo \"File is owned by you\"\nfi","correctCode":"if [ -n \"$(find file -prune -user \"$(id -u)\")\" ]\nthen\n  echo \"File is owned by you\"\nfi"}}
